# S622_robotarm 原项目完整结构

共 26 个包（含 GraphExecuter 独立项目和 .vscode 配置）。

> STL 网格文件在 `fairino_description/meshes/` 下按 7 个机器人型号分目录，
> 每个型号含 base_link shoulder upperarm forearm wrist1-3 共 7 个 STL，
> 为简洁起见不逐文件列出。

---

## 分类一：硬件相关（vendor 层）

### fairino_description/ — 多型号 URDF + 网格
```
fairino_description/
├── CMakeLists.txt / package.xml
├── urdf/
│   ├── fairino3_v6.urdf   fairino5_v6.urdf
│   ├── fairino10_v6.urdf  fairino16_v6.urdf
│   ├── fairino20_v6.urdf  fairino30_v6.urdf
│   └── fairino3_mt_v6.urdf
├── meshes/                 # 7 个型号 × 7 个 link = 49 个 STL
├── launch/display.launch.py
└── rviz/urdf.rviz
```

### fairino_msgs/ — 自定义 ROS 2 消息
```
fairino_msgs/
├── CMakeLists.txt / package.xml
├── msg/RobotNonrtState.msg
├── srv/RemoteCmdInterface.srv
└── srv/RemoteScriptContent.srv
```

### fairino_hardware/ — ros2_control 硬件接口
```
fairino_hardware/
├── CMakeLists.txt / package.xml
├── include/fairino_hardware/
│   ├── fairino_hardware_interface.hpp
│   ├── command_server.hpp
│   ├── data_type_def.h
│   ├── visibility_control.h
│   └── version_control.h
├── src/
│   ├── fairino_hardware_interface.cpp
│   ├── command_server.cpp
│   └── command_server_node.cpp
├── libfairino/
│   ├── include/robot.h robot_error.h robot_types.h
│   └── lib/libfairino.so.2.2.2 libfairino.so.2.2.5
├── examples/
│   ├── include/srv_test_client.hpp
│   └── src/main.cpp srv_test_client.cpp
├── fairino_hardware.xml
└── fairino_remotecmdinterface_para.yaml
```

### s622_moveit_descriptions/ — S622 URDF（SolidWorks 导出）
```
s622_moveit_descriptions/
├── CMakeLists.txt / package.xml
├── urdf/
│   ├── s622_moveit_descriptions.urdf
│   └── camera/_d435.urdf.xacro camera.xacro
├── meshes/    # base_link shoulder upperarm forearm wrist1-3 finger1-2 camera (10 STL)
├── visual/    # d435.dae camera.dae
├── config/joint_names_s622_moveit_descriptions.yaml
├── launch/display.launch.py gazebo.launch
└── rviz/urdf.rviz
```

---

## 分类二：MoveIt2 配置

### fairino3_v6_moveit2_config/
```
fairino3_v6_moveit2_config/
├── CMakeLists.txt / package.xml
├── config/
│   ├── fairino3_v6_robot.srdf
│   ├── fairino3_v6_robot.urdf.xacro
│   ├── fairino3_v6_robot.ros2_control.xacro
│   ├── kinematics.yaml
│   ├── fairino_planning.yaml
│   ├── joint_limits.yaml
│   ├── initial_positions.yaml
│   ├── ros2_controllers.yaml
│   ├── moveit_controllers.yaml
│   ├── pilz_cartesian_limits.yaml
│   ├── moveit.rviz / mtc.rviz
├── launch/
│   ├── demo.launch.py
│   ├── move_group.launch.py
│   ├── moveit_rviz.launch.py
│   ├── rsp.launch.py
│   ├── setup_assistant.launch.py
│   ├── spawn_controllers.launch.py
│   ├── static_virtual_joint_tfs.launch.py
│   └── warehouse_db.launch.py
```

### s622_moveit_config/
```
s622_moveit_config/
├── CMakeLists.txt / package.xml
├── config/
│   ├── s622_moveit_descriptions.srdf
│   ├── s622_moveit_descriptions.urdf.xacro
│   ├── s622_moveit_descriptions.ros2_control.xacro
│   ├── kinematics.yaml
│   ├── joint_limits.yaml / initial_positions.yaml
│   ├── ros2_controllers.yaml / moveit_controllers.yaml
│   ├── ompl_planning.yaml
│   ├── pilz_cartesian_limits.yaml
│   ├── sensors_3d.yaml
│   ├── servo_parameters.yaml
│   └── moveit.rviz
├── launch/  # 同 fairino3_v6_moveit2_config 的 8 个 launch
```

---

## 分类三：自研规划（核心 ❤️）

### fairino_planning_core/ — 纯 C++ 运动学+规划，无 ROS 依赖
```
fairino_planning_core/
├── CMakeLists.txt / package.xml
├── include/fairino_planning_core/
│   ├── types.h
│   ├── dh_kinematics.h
│   ├── ik/fairino_ik.h ik_selector.h
│   ├── algorithms/planning_algorithm.h rrt_star.h bi_rrt_star.h
│   ├── samplers/mixed_sampler.h
│   ├── collision/collision_interface.h
│   ├── constraints/orientation_checker.h
│   ├── tree/rrt_tree.h
│   └── trajectory/path_shortcut.h trajectory_smoother.h
├── src/
│   ├── dh_kinematics.cpp
│   ├── ik/fairino_ik.cpp ik_selector.cpp
│   ├── algorithms/rrt_star.cpp bi_rrt_star.cpp
│   ├── samplers/mixed_sampler.cpp
│   ├── constraints/orientation_checker.cpp
│   ├── tree/rrt_tree.cpp
│   └── trajectory/path_shortcut.cpp trajectory_smoother.cpp
```

### fairino_planning_ros/ — MoveIt2 插件封装
```
fairino_planning_ros/
├── CMakeLists.txt / package.xml
├── include/fairino_planning_ros/
│   ├── fairino_planner_manager.h
│   ├── fairino_ik_plugin.h
│   └── moveit_collision_checker.h
├── src/
│   ├── fairino_planner_manager.cpp
│   ├── fairino_ik_plugin.cpp
│   ├── moveit_collision_checker.cpp
│   └── standalone_planner_node.cpp
├── config/ik_params.yaml planning_params.yaml
├── launch/demo.launch.py planning_pipeline.launch.py
└── plugins/fairino_planning_plugins.xml
```

---

## 分类四：仿真

### gz_launch/ — Gazebo 仿真启动 + 演示脚本
```
gz_launch/
├── CMakeLists.txt / package.xml
├── config/
│   ├── robot_gazebo.urdf.xacro
│   ├── robot_gazebo.friction.urdf.xacro
│   ├── controller_setting.yaml
│   ├── fairino_planning.yaml / movelt_cpp.yaml
│   ├── box.urdf case.urdf obstacle.urdf cube.sdf
├── launch/
│   ├── gazebo.launch.py
│   ├── gazebo_yolo.launch.py
│   ├── pick_block.launch.py
│   ├── servo_yolo_grasping_gz.launch.py
│   ├── pen_box_system.launch.py
│   ├── yolo_pick.launch.py
│   ├── yolo_detector.launch.py
│   └── stopmotion.launch.py
├── scripts/
│   ├── demo_pathplanning_node.py
│   ├── cube_controller_node.py
│   ├── yolo_Kalman_detector_obb_node.py
│   ├── yolo_detector_obb_node.py
│   ├── pen_box_grasping_node.py
│   ├── pick_drop_node.py / pick_drop_ik_node.py
│   ├── robot_control_from_UI_node.py
│   └── stopmotion_node.py
├── worlds/
│   ├── arm_on_the_table.sdf
│   ├── bolt/ ground_plane/ sun/ table/
└── rviz/  # 4 个 rviz 配置
```

---

## 分类五：感知与标定

### yolov8_obb/ — YOLOv8 OBB C++ 检测
```
├── CMakeLists.txt / package.xml
├── scripts/yolov8_obb_publisher.py yolov8_obb_subscriber.py best.pt
└── launch/yolov8_obb.launch.py
```

### yolov8_obb_msgs/ — OBB 消息定义（package.xml 中名为 yolov8_msgs）
```
├── CMakeLists.txt / package.xml
└── msg/InferenceResult.msg Yolov8Inference.msg
```

### yolov8_grasping/ — YOLOv8 Python 检测+抓取
```
yolov8_grasping/
├── package.xml / setup.py / setup.cfg
├── launch/  # gazebo pick_block pen_box_system perception stopmotion yolo_detector (6个)
├── yolov8_grasping/
│   ├── yolo_detector_node.py yolo_detector_node1.py
│   ├── yolo_detector_obb_node.py
│   ├── pen_box_grasping_node.py
│   ├── pick_drop_node.py / pick_drop_ik_node.py
│   ├── stopmotion_node.py
│   └── scripts/  # abort_manager keepout_manager pose_tools tf_tools trajectory_scoring
├── config/  # box/case URDF, movelt_cpp, friction xacro
└── rviz/    # 3 个 rviz
```



yolov8_obb	yolov8_grasping
做什么	吃图像，吐检测框	吃检测框，吐抓取动作
输入	/camera/color/image_raw（图像）	/yolov8/obb_detections（检测结果）
输出	YOLOv8 推理一组 OBB 框	机械臂运动指令（抓取位姿、轨迹）
核心能力	加载 .pt 模型、跑 model(image) 推理	从框算抓取点、角度筛选、IK、轨迹规划
简单说：yolov8_obb 是眼睛（看），yolov8_grasping 是手（抓）。你现在的 yolov8_obb_node.py 发假检测框就是为了先打通"眼睛→手"这条链，验证 grasping 能正确接收检测结果并生成抓取动作。

接下来该写 yolov8_grasping 了


### hand_eye_calibration/ — 手眼标定
```
hand_eye_calibration/
├── CMakeLists.txt / package.xml / setup.py
├── hand_eye_calibration.repos       # vcs: easy_handeye2
├── launch/calibrate.launch.py validate.launch.py
├── scripts/
│   ├── calibration_aruco_publisher.py
│   ├── handeye_publisher.py
│   ├── follow_aruco_marker.py
│   └── visualize_aruco_marker.py
├── config/aruco_parameters.yaml
└── rviz/  # 4 个 rviz
```


它是三个角色的中转站：


相机看到标记 → hand_eye_calibration → 算出相机在机械臂上的位姿
没有标定的问题： YOLOv8 检测到的物体坐标在相机坐标系下，但机械臂在基座坐标系下运动。没有手眼标定，你不知道"相机看到的东西在机械臂的什么位置"。

做了什么：

步骤	哪个组件	干什么
1	ros2_aruco（外部包）	检测画面里的 ArUco 标记，算出标记在相机坐标系下的位姿
2	hand_eye_calibration 的 launch	机械臂移动到 N 个姿态，每个姿态记录一对数据：末端位姿 + 标记位姿
3	easy_handeye2（外部包）	用 N 对位姿求解 camera→tool0 或 camera→base_link 的变换矩阵
4	发布 TF	之后 YOLOv8 检测到的物体位置就能通过 TF 转到 base_link，机械臂才知道抓哪
和你的开发顺序关系： 标定依赖实机或至少仿真中的整个链路（相机+机械臂+标记+多个姿态），是阶段五最后一个环节。先搞 yolov8_grasping（能算抓取位姿），标定后面再做。

---

## 分类六：抓取

### visual_servo/ — 视觉伺服抓取状态机（最复杂）
```
visual_servo/
├── package.xml / setup.py / setup.cfg
├── visual_servo/
│   ├── servo_yolo_grasping_node.py      # 真机版 13 状态机
│   ├── servo_gazebo_grasping_node.py    # 仿真版
│   ├── yolo_detector_obb_node.py
│   ├── stopmotion_node.py
│   ├── controllers/
│   │   ├── servo_controller.py          # 伺服控制器基类
│   │   ├── servo_controller_gazebo.py
│   │   ├── pd_controller.py
│   │   ├── ladrc_controller.py          # 线性自抗扰
│   │   ├── mpc_controller.py            # 模型预测控制
│   │   └── ema_filter.py
│   └── scripts/
│       ├── moveit_motion.py             # MoveIt 运动封装
│       ├── detection_cache.py           # 检测缓存
│       ├── target_selector.py           # 目标选择
│       ├── trajectory_scoring.py        # 轨迹评分
│       ├── abort_manager.py             # 中止管理
│       ├── keepout_manager.py           # 禁区管理
│       ├── grasp_latch.py               # 抓取锁定（部分文件）
│       ├── pose_factory.py / pose_tools.py
│       ├── tf_tools.py / param_tools.py / publishers.py
├── launch/  # servo_yolo_grasping perception stopmotion (3个)
└── rviz/    # 4 个 rviz
```

### octomap_yolo_grasping/ — OctoMap + YOLO 融合抓取
```
octomap_yolo_grasping/
├── package.xml / setup.py / setup.cfg
├── octomap_yolo_grasping/
│   ├── octomap_yolo_grasping_node.py
│   ├── dynamic_collision_objects_node.py
│   ├── semantic_octomap_cloud_filter_node.py
│   ├── yolo_detector_obb_node.py
│   ├── stopmotion_node.py
│   └── scripts/  # abort_manager keepout_manager pose_tools tf_tools trajectory_scoring
├── launch/  # octomap_yolo_grasping perception stopmotion (3个)
└── rviz/    # 3 个 rviz
```

### graspnet_grasping/ — GraspNet 抓取姿势规划
```
graspnet_grasping/
├── package.xml / setup.py / setup.cfg
├── graspnet_grasping/
│   ├── grasp_planner_node.py
│   └── realsense_capture_node.py
└── launch/graspnet_system.launch.py
```

---

## 分类七：工具

### trajectory_retime_server/ — 轨迹重定时
```
├── CMakeLists.txt / package.xml
├── srv/RetimeTrajectory.srv
├── src/retime_server.cpp
└── launch/retime_server.launch.py
```

### control_servers/ — FastAPI Web 控制
```
├── package.xml / setup.py / setup.cfg
├── control_servers/joint_control_app.py
└── app/
    ├── main.py / requirements.txt
    ├── templates/index.html
    └── static/css/style.css static/js/script.js static/favicon.svg
```

### data_monitor/ — 关节数据监控
```
├── package.xml / setup.py / setup.cfg
├── data_monitor/
│   ├── joint_trajectory_monitor_server.py
│   └── get_end_position.py
└── launch/data_monitor.launch.py
```

---

## 分类八：外部开源

### pymoveit2/ — Andrej Orsula 的 MoveIt2 Python 接口
```
├── pymoveit2/moveit2.py moveit2_gripper.py moveit2_servo.py
│             gripper_command.py gripper_interface.py
│             robots/{panda ur kinova lbr crane_x7 phantomx_pincher}.py
├── examples/  # ex_joint_goal ex_pose_goal ex_ik ex_fk ex_servo ex_gripper 等 11 个
```

### easy_handeye2/ — IFL-CAMP 手眼标定库
```
├── easy_handeye2/   # 核心 Python 包（handeye_calibration handeye_server handeye_sampler 等）
├── easy_handeye2_msgs/  # 标定相关消息（5 个 .msg）
```

### ros2_aruco/ — ravijo 的 ArUco 标记检测
```
├── ros2_aruco/            # Python 包（aruco_node）
├── ros2_aruco_interfaces/  # 消息定义（ArucoMarkers.msg）
```

### realsense2_gz_description/ — RealSense D435 相机仿真
```
├── urdf/  # _d405 _d415 _d435 _d455 gazebo + rgb_camera rgbd_camera gazebo xacro
├── launch/example_realsense_gazebo.launch.py
├── rviz/example.rviz
└── world/example.sdf
```

---

## 分类九：独立项目

### GraphExecuter/ — PySide6 可视化工作流编辑器
```
├── COLCON_IGNORE                     # 不参与 colcon 编译
├── graph_executer/
│   ├── main.py
│   ├── nodes/                        # 功能节点
│   ├── src/mainwindow.py messageconsole.py updatelog.py
│   ├── ui/                           # Qt Designer UI 文件
│   ├── utils/general.py
│   └── bin/                          # Python 虚拟环境（含 ultralytics PySide6 edge-tts 等）
```

---

## 其他

- **panda_arm_msg/** — Panda 机械臂消息定义（遗留/测试用），3 个 .srv
- **src/.vscode/** — VSCode 工作区配置
- **src/API说明.md** — 接口说明文档
