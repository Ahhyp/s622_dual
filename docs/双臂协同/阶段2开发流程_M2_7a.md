## M2.7 拆分策略

好,先商量,不急着写代码。M2.7 复杂度高,一次性推容易出问题。我按信息密度和风险从高到低把它切成几个 sub-milestone,逐个交付验证。

## M2.7 拆分方案

### M2.7.0: 目标与不做的事

**目标**:双臂各自能独立跑通完整 pick-place(不 handover)。

**具体成果**:

* 一份 XML,通过 blackboard 参数 `arm` 选左或右
* 或两份 XML(`pick_place_left.xml` / `pick_place_right.xml`)
* 单臂 pick-place 序列不变,只是内部所有 action/service 加 `arm_prefix`

**明确不做**:

* Handover(M2.8)
* 双臂并发执行(M2.7 阶段两臂**串行**跑 pick-place 各自的 XML)
* YOLO 模型换 double-arm(继续用现有 cube 检测)

### M2.7 → 5 个 sub-milestone

| Sub    | 任务                               | 涉及文件                                                                                       | 主要风险                        |
| ------ | ---------------------------------- | ---------------------------------------------------------------------------------------------- | ------------------------------- |
| M2.7.1 | BT node 层参数化 `arm_prefix` port | motion_nodes / gripper_nodes / servo_nodes / perception_nodes / grasp_nodes / scene_nodes 全部 | 面广但改法机械,单个 node 改动小 |
| M2.7.2 | 相机反投影双臂化                   | grasp_nodes.cpp (GenerateGraspCandidate) + 可能 error_estimator                                | 左右 base 系不同,数学需要重推   |
| M2.7.3 | 双臂 grasp_cube 位姿校准           | left/right_arm_config.yaml, 可能 SRDF group_state                                              | 需要 sim 里手动 teach 双臂      |
| M2.7.4 | XML 双臂化 + Executor 传参         | pick_place_left.xml / pick_place_right.xml, bt_executor_node.cpp                               | XML 语法 + blackboard 传值      |
| M2.7.5 | 端到端验证                         | (无新文件,只跑)                                                                                | 集成故障排查                    |

我建议**先完成 M2.7.1 再看**——node 参数化后就有底座,后面几个 sub 都建在它上面。

## M2.7.1 优先做,原因

* **改动机械**:每个 node 加一个 `arm_prefix` port + 按 M2.6 里 `servo_nodes.cpp` 同样的懒建 client 模式。
* **单个 node 改动量小**:每个 5-10 行代码
* **可独立验证**:单臂调用(`arm_prefix=""`)沿用 M1.7 行为——不影响现有功能;双臂调用(`arm_prefix="left"`)拼路径 → M2.6 已验证的 action/service 就能连上
* **能立刻拉通 XML 骨架测试**:M2.7.4 可先跑 "只有 MoveToPose 的 minimal XML" 验证参数传递,再往里塞感知/servo/gripper

## M2.7.1 的 node 分批

7 个 cpp 文件(除 bt_executor_node)按依赖顺序:

**批次 1**:`motion_nodes.cpp` + `gripper_nodes.cpp`

* MoveToPose / SetGripper — 这两个足以跑 "MoveToPose home + Gripper open" 的 minimal 测试
* **建议先贴这两个 cpp + 对应 hpp**

**批次 2**:`servo_nodes.cpp`

* 已经在 M2.6 改过,VisualAlign + StopServo,状态是**已完成 M2.6 的参数化**
* M2.7.1 只需 grep 一下确认没漏

**批次 3**:`scene_nodes.cpp`

* AttachObject / DetachObject — planning_scene_service 是全局单例,touch_links 参数化,不必加 arm_prefix。但可能需要传 EE link name(left/right_grasp_frame)。
* 这批放中间,因为它简单

**批次 4**:`perception_nodes.cpp` + `grasp_nodes.cpp`

* DetectObject / LockTargetPixel / AlignLockCheck / GenerateGraspCandidate / GeneratePlaceCandidate
* **风险最高的批次**——涉及像素→base 反投影双臂化,数学要变
* M2.7.2 深入处理反投影,M2.7.1 里先只加 arm_prefix port(把 target 输出 blackboard key 参数化)

**批次 5**:`bt_executor_node.cpp`

* 顶层 executor,读 XML,启动 tree
* 加 `arm` 命令行参数或 blackboard 传入
* 最后处理

## 我需要你先贴的

**M2.7.1 批次 1 起步**,贴:

1. `s622_bt_manager/src/motion_nodes.cpp`
2. `s622_bt_manager/include/s622_bt_manager/motion_nodes.hpp`
3. `s622_bt_manager/src/gripper_nodes.cpp`
4. `s622_bt_manager/include/s622_bt_manager/gripper_nodes.hpp`

看完这 4 个我给批次 1 的完整 diff。

批次 1 通过后,你依次贴批次 2-5 的文件,我逐批给 diff。

## 顺便定几个约定

**约定 1: `arm_prefix` port 的语义**

* `""`(默认,空串):使用根路径(M1.7 单臂兼容)
* `"left"` / `"right"`:使用对应 namespace
* 拼接规则:`/{arm_prefix}/action_name` 或 `/action_name`(prefix 空时)

**约定 2: BT 里 `arm_prefix` 从哪来**
建议用 **blackboard entry** + XML port 双通路:

* XML 直接写:`<MoveToPose arm_prefix="left" ... />`
* 或从 blackboard 拿:`<MoveToPose arm_prefix="{arm}" ... />`,由 executor 或前面某个 SetBlackboard 塞入

**约定 3: EE 相关 frame(如 grasp_frame)**
`grasp_frame` 之类硬编码字符串,在 M2.7 里也要参数化。可能加 `ee_frame_name` port,或直接从 blackboard 拿。这个先记着,批次 1 里 motion_nodes 不涉及,批次 3 scene_nodes(AttachObject 的 link_name)会用到。

**约定 4: 编号命名**

* BT 节点里的 blackboard entry(如 `{u}` `{v}`):双臂共享同一份 XML 时可能冲突。这属于 M2.7.4 XML 层面的问题,批次 1 不管。

---

先贴 motion_nodes 和 gripper_nodes 的 4 个文件(hpp + cpp × 2),我给 M2.7.1 批次 1 的 diff。



#
perception_nodes 不必改。
大多数 nodes 文件改写比较简单。
grasp_nodes 改写困难.
## M2.7.1 批次 4b: grasp_nodes 参数化

### 分析要点

**数学层面**:表面平面交线法内部用 TF lookup(`base_frame → camera_frame`),只要 frame 名参数化,双臂自动对。数学不改。

**架构层面**:

* `GenerateGraspCandidate`: 加 `arm_prefix` port,arm_prefix 非空时自动派生 `base_frame`/`ee_frame` 默认值;`camera_x_in_base`/`camera_y_in_base`(fallback 分支用)仍可显式传参
* `GeneratePlaceCandidate`: 按 arm_prefix 分组读参数(`left_place_*` / `right_place_*` / `place_*` M1.7 兼容),构造函数不 load,tick 中懒加载 + 缓存

**双臂几何值**(备查,批次 5 XML 里传入):

| arm_prefix      | camera_x_in_base | camera_y_in_base |
| --------------- | ---------------- | ---------------- |
| `""`(M1.7 单臂) | 0.3825           | 0.4838           |
| `"left"`        | 0.35             | -0.5             |
| `"right"`       | 0.35             | 0.5              |

推导:left_base 在 world (0.35, 0, 0) yaw=π,相机在 world (0, 0.5, 0.9)。相机在 left_base 系 = R^T·(-0.35, 0.5, 0.9) = (0.35, -0.5, 0.9)。右臂 base 在 (-0.35, 0, 0) yaw=0,直接减:(0.35, 0.5, 0.9)。

---

### 1. `grasp_nodes.hpp` diff

```cpp
#pragma once

#include <map>
#include <behaviortree_cpp/bt_factory.h>
#include <geometry_msgs/msg/pose_stamped.hpp>
#include "s622_bt_manager/perception_nodes.hpp"

namespace s622_bt
{

    class GenerateGraspCandidate : public BT::SyncActionNode
    {
    public:
        GenerateGraspCandidate(const std::string &name,
                               const BT::NodeConfig &config,
                               RosContextPtr ros);
        static BT::PortsList providedPorts();
        BT::NodeStatus tick() override;

    private:
        RosContextPtr ros_;
    };

    void registerGraspNodes(BT::BehaviorTreeFactory &factory, RosContextPtr ros);

    class GeneratePlaceCandidate : public BT::SyncActionNode
    {
    public:
        GeneratePlaceCandidate(const std::string &name,
                               const BT::NodeConfig &config,
                               rclcpp::Node::SharedPtr node);
        static BT::PortsList providedPorts()
        {
            return {
                BT::InputPort<std::string>("arm_prefix", "",
                    "'' -> place_*; 'left' -> left_place_*"),  // ← 新增
                BT::OutputPort<geometry_msgs::msg::PoseStamped>("place_pose"),
                BT::OutputPort<geometry_msgs::msg::PoseStamped>("pre_place_pose"),
            };
        }
        BT::NodeStatus tick() override;

    private:
        rclcpp::Node::SharedPtr node_;
        // ← 改为按 arm_prefix 缓存 pose (支持一个 BT process 里多个 arm)
        std::map<std::string, geometry_msgs::msg::PoseStamped> place_cache_;
        std::map<std::string, geometry_msgs::msg::PoseStamped> pre_place_cache_;

        // helper: 按 arm_prefix 加载并缓存
        void ensure_loaded(const std::string &arm_prefix);
    };
} // namespace s622_bt
```

### 2. `grasp_nodes.cpp` diff

```cpp
#include "s622_bt_manager/grasp_nodes.hpp"

#include <algorithm>
#include <cmath>
#include <vector>

#include <tf2/utils.h>
#include <tf2_geometry_msgs/tf2_geometry_msgs.hpp>
#include <geometry_msgs/msg/pose_array.hpp>
#include <geometry_msgs/msg/point_stamped.hpp>

namespace s622_bt
{

    namespace
    {
        // ---- 保留原有 samplePixelDepth / normalizeAngle / selectBestYaw / makePose ----
        // ... (完全不变, 略) ...

        double samplePixelDepth(const sensor_msgs::msg::Image &depth_img,
                                int u, int v, int kernel = 5)
        {
            if (depth_img.encoding != "32FC1")
                return std::nan("");
            std::vector<float> samples;
            int half = kernel / 2;
            int w = depth_img.width;
            int h = depth_img.height;
            const float *data = reinterpret_cast<const float *>(depth_img.data.data());
            for (int dy = -half; dy <= half; ++dy)
            {
                for (int dx = -half; dx <= half; ++dx)
                {
                    int x = u + dx, y = v + dy;
                    if (x < 0 || x >= w || y < 0 || y >= h)
                        continue;
                    float z = data[y * w + x];
                    if (std::isfinite(z) && z > 0.01f && z < 5.0f)
                        samples.push_back(z);
                }
            }
            if (samples.empty())
                return std::nan("");
            std::nth_element(samples.begin(),
                             samples.begin() + samples.size() / 2,
                             samples.end());
            return samples[samples.size() / 2];
        }

        double normalizeAngle(double a)
        {
            while (a > M_PI) a -= 2 * M_PI;
            while (a < -M_PI) a += 2 * M_PI;
            return a;
        }

        double selectBestYaw(double obj_yaw, double current_yaw)
        {
            double best = normalizeAngle(obj_yaw);
            double best_err = std::abs(normalizeAngle(best - current_yaw));
            for (int k = 1; k < 4; ++k)
            {
                double cand = normalizeAngle(obj_yaw + k * M_PI / 2.0);
                double err = std::abs(normalizeAngle(cand - current_yaw));
                if (err < best_err) { best_err = err; best = cand; }
            }
            return best;
        }

        geometry_msgs::msg::Pose makePose(double x, double y, double z,
                                          double roll, double pitch, double yaw)
        {
            geometry_msgs::msg::Pose p;
            p.position.x = x; p.position.y = y; p.position.z = z;
            tf2::Quaternion q;
            q.setRPY(roll, pitch, yaw);
            p.orientation = tf2::toMsg(q);
            return p;
        }

        // ---- 新增 helper: 按 arm_prefix 派生 frame 名 ----
        std::string derive_frame(const std::string &explicit_frame,
                                  const std::string &arm_prefix,
                                  const std::string &suffix,
                                  const std::string &m17_default)
        {
            if (!explicit_frame.empty())
                return explicit_frame;
            if (arm_prefix.empty())
                return m17_default;
            return arm_prefix + "_" + suffix;
        }

    } // namespace

    GenerateGraspCandidate::GenerateGraspCandidate(const std::string &name,
                                                   const BT::NodeConfig &config,
                                                   RosContextPtr ros)
        : BT::SyncActionNode(name, config), ros_(std::move(ros)) {}

    BT::PortsList GenerateGraspCandidate::providedPorts()
    {
        return {
            BT::InputPort<double>("u"),
            BT::InputPort<double>("v"),
            BT::InputPort<double>("object_yaw"),
            BT::InputPort<std::string>("arm_prefix", "",  // ← 新增
                "'' | 'left' | 'right', derives base/ee frames if not given"),
            BT::InputPort<double>("table_z", 0.0, "table z in base frame"),
            BT::InputPort<double>("grasp_height_above_table", 0.030, ""),
            BT::InputPort<double>("pregrasp_height_above_table", 0.16, ""),
            BT::InputPort<double>("pregrasp_camera_offset", 0.03, ""),
            // base_frame/ee_frame 空时按 arm_prefix 派生
            BT::InputPort<std::string>("base_frame", "", ""),
            BT::InputPort<std::string>("ee_frame", "", ""),
            BT::InputPort<double>("camera_x_in_base", 0.3825,
                "cam pos in base frame, used by depth fallback"),
            BT::InputPort<double>("camera_y_in_base", 0.4838, ""),
            BT::OutputPort<geometry_msgs::msg::PoseStamped>("tcp_grasp_pose"),
            BT::OutputPort<geometry_msgs::msg::PoseStamped>("tcp_pregrasp_pose"),
            BT::OutputPort<double>("grasp_yaw"),
            BT::OutputPort<double>("grasp_pose_x"),
            BT::OutputPort<double>("grasp_pose_y"),
        };
    }

    BT::NodeStatus GenerateGraspCandidate::tick()
    {
        double u, v, obj_yaw;
        if (!getInput("u", u) || !getInput("v", v) || !getInput("object_yaw", obj_yaw))
        {
            RCLCPP_ERROR(ros_->node->get_logger(),
                         "GenerateGraspCandidate: missing inputs");
            return BT::NodeStatus::FAILURE;
        }
        double table_z, grasp_dz, pregrasp_dz, cam_offset;
        double cam_x_base, cam_y_base;
        std::string base_frame_raw, ee_frame_raw, arm_prefix;
        getInput("table_z", table_z);
        getInput("grasp_height_above_table", grasp_dz);
        getInput("pregrasp_height_above_table", pregrasp_dz);
        getInput("pregrasp_camera_offset", cam_offset);
        getInput("arm_prefix", arm_prefix);         // ← 新增
        getInput("base_frame", base_frame_raw);
        getInput("ee_frame", ee_frame_raw);
        getInput("camera_x_in_base", cam_x_base);
        getInput("camera_y_in_base", cam_y_base);

        // ---- 按 arm_prefix 派生 frame ----
        const std::string base_frame = derive_frame(base_frame_raw, arm_prefix,
                                                     "base_link", "base_link");
        const std::string ee_frame = derive_frame(ee_frame_raw, arm_prefix,
                                                   "grasp_frame", "grasp_frame");

        // 1) camera_info
        sensor_msgs::msg::CameraInfo::SharedPtr caminfo;
        {
            std::lock_guard<std::mutex> lk(ros_->caminfo_mutex);
            caminfo = ros_->latest_caminfo;
        }
        if (!caminfo)
        {
            RCLCPP_WARN(ros_->node->get_logger(), "no camera_info yet");
            return BT::NodeStatus::FAILURE;
        }
        double fx = caminfo->k[0], fy = caminfo->k[4];
        double cx = caminfo->k[2], cy = caminfo->k[5];
        std::string camera_frame = caminfo->header.frame_id;
        if (camera_frame.empty())
            camera_frame = "camera_color_optical_frame";

        // 2) TF: base ← camera
        geometry_msgs::msg::TransformStamped cam_tf;
        try
        {
            cam_tf = ros_->tf_buffer->lookupTransform(
                base_frame, camera_frame, tf2::TimePointZero,
                tf2::durationFromSec(0.5));
        }
        catch (const tf2::TransformException &e)
        {
            RCLCPP_WARN(ros_->node->get_logger(),
                        "TF %s->%s failed (arm=%s): %s, fallback to depth sensor",
                        base_frame.c_str(), camera_frame.c_str(),
                        arm_prefix.c_str(), e.what());
            goto use_depth;
        }

        // 3) 表面平面交线法
        {
            double dx = (u - cx) / fx;
            double dy = (v - cy) / fy;

            tf2::Quaternion q;
            tf2::fromMsg(cam_tf.transform.rotation, q);
            tf2::Matrix3x3 R(q);

            double rx = R[0][0] * dx + R[0][1] * dy + R[0][2] * 1.0;
            double ry = R[1][0] * dx + R[1][1] * dy + R[1][2] * 1.0;
            double rz = R[2][0] * dx + R[2][1] * dy + R[2][2] * 1.0;

            double cam_z = cam_tf.transform.translation.z;
            double target_z = table_z + grasp_dz;
            if (std::abs(rz) < 1e-6)
                goto use_depth;

            double t = (target_z - cam_z) / rz;
            if (t <= 0)
                goto use_depth;

            double obj_x = cam_tf.transform.translation.x + t * rx;
            double obj_y = cam_tf.transform.translation.y + t * ry;

            RCLCPP_INFO(ros_->node->get_logger(),
                        "Table-plane[arm=%s]: t=%.3f -> base=(%.3f,%.3f)",
                        arm_prefix.c_str(), t, obj_x, obj_y);

            // 5) 当前 EE yaw
            double current_ee_yaw = 0.0;
            try
            {
                auto tf = ros_->tf_buffer->lookupTransform(base_frame, ee_frame,
                                                           tf2::TimePointZero,
                                                           tf2::durationFromSec(0.2));
                tf2::Quaternion eq;
                tf2::fromMsg(tf.transform.rotation, eq);
                double r, p, y;
                tf2::Matrix3x3(eq).getRPY(r, p, y);
                current_ee_yaw = y;
            }
            catch (const tf2::TransformException &e)
            {
                RCLCPP_WARN(ros_->node->get_logger(),
                            "TF %s->%s failed (arm=%s), default ee_yaw=0: %s",
                            base_frame.c_str(), ee_frame.c_str(),
                            arm_prefix.c_str(), e.what());
            }

            double best_yaw = selectBestYaw(obj_yaw, current_ee_yaw);
            double obj_z_grasp = table_z + grasp_dz;

            geometry_msgs::msg::PoseStamped grasp;
            grasp.header.frame_id = base_frame;
            grasp.header.stamp = ros_->node->now();
            grasp.pose = makePose(obj_x, obj_y, obj_z_grasp, M_PI, 0.0, best_yaw);

            double ddx = obj_x - cam_tf.transform.translation.x;
            double ddy = obj_y - cam_tf.transform.translation.y;
            double norm = std::hypot(ddx, ddy);
            double off_x = norm > 1e-6 ? ddx / norm * cam_offset : 0.0;
            double off_y = norm > 1e-6 ? ddy / norm * cam_offset : 0.0;

            geometry_msgs::msg::PoseStamped pregrasp;
            pregrasp.header.frame_id = base_frame;
            pregrasp.header.stamp = grasp.header.stamp;
            pregrasp.pose = makePose(obj_x + off_x, obj_y + off_y,
                                     table_z + pregrasp_dz, M_PI, 0.0, best_yaw);

            setOutput("tcp_grasp_pose", grasp);
            setOutput("tcp_pregrasp_pose", pregrasp);
            setOutput("grasp_yaw", best_yaw);
            setOutput("grasp_pose_x", obj_x);
            setOutput("grasp_pose_y", obj_y);

            RCLCPP_INFO(ros_->node->get_logger(),
                        "Grasp[arm=%s plane]: uv=(%.1f,%.1f) -> base=(%.3f,%.3f,%.3f) "
                        "yaw=%.3f pregrasp=(%.3f,%.3f,%.3f)",
                        arm_prefix.c_str(), u, v, obj_x, obj_y, obj_z_grasp,
                        best_yaw,
                        pregrasp.pose.position.x, pregrasp.pose.position.y,
                        pregrasp.pose.position.z);

            if (ros_->grasp_viz_pub)
            {
                geometry_msgs::msg::PoseArray arr;
                arr.header = pregrasp.header;
                arr.poses.push_back(pregrasp.pose);
                arr.poses.push_back(grasp.pose);
                ros_->grasp_viz_pub->publish(arr);
            }

            return BT::NodeStatus::SUCCESS;
        }

    use_depth:
        // === 深度传感器回退路径 ===
        {
            sensor_msgs::msg::Image::SharedPtr depth;
            {
                std::lock_guard<std::mutex> lk(ros_->depth_mutex);
                depth = ros_->latest_depth;
            }
            if (!depth)
            {
                RCLCPP_WARN(ros_->node->get_logger(), "no depth yet");
                return BT::NodeStatus::FAILURE;
            }
            double z_cam = samplePixelDepth(*depth,
                                             int(std::round(u)), int(std::round(v)));
            if (!std::isfinite(z_cam) || z_cam <= 0)
            {
                RCLCPP_WARN(ros_->node->get_logger(),
                            "invalid depth at (%.1f,%.1f)", u, v);
                return BT::NodeStatus::FAILURE;
            }
            RCLCPP_WARN(ros_->node->get_logger(),
                        "Using depth fallback[arm=%s]: z_cam=%.3f",
                        arm_prefix.c_str(), z_cam);

            double x_cam = (u - cx) * z_cam / fx;
            double y_cam = (v - cy) * z_cam / fy;

            geometry_msgs::msg::PointStamped p_cam, p_base;
            p_cam.header.frame_id = camera_frame;
            p_cam.header.stamp = depth->header.stamp;
            p_cam.point.x = x_cam;
            p_cam.point.y = y_cam;
            p_cam.point.z = z_cam;
            try
            {
                p_base = ros_->tf_buffer->transform(p_cam, base_frame,
                                                    tf2::durationFromSec(0.2));
            }
            catch (const tf2::TransformException &e)
            {
                RCLCPP_WARN(ros_->node->get_logger(),
                            "TF %s->%s failed: %s",
                            camera_frame.c_str(), base_frame.c_str(), e.what());
                return BT::NodeStatus::FAILURE;
            }
            double obj_x = p_base.point.x, obj_y = p_base.point.y;

            double current_ee_yaw = 0.0;
            try
            {
                auto tf = ros_->tf_buffer->lookupTransform(base_frame, ee_frame,
                                                           tf2::TimePointZero,
                                                           tf2::durationFromSec(0.2));
                tf2::Quaternion q;
                tf2::fromMsg(tf.transform.rotation, q);
                double r, p, y;
                tf2::Matrix3x3(q).getRPY(r, p, y);
                current_ee_yaw = y;
            }
            catch (const tf2::TransformException &e)
            {
                RCLCPP_WARN(ros_->node->get_logger(),
                            "TF %s->%s failed (arm=%s), default ee_yaw=0: %s",
                            base_frame.c_str(), ee_frame.c_str(),
                            arm_prefix.c_str(), e.what());
            }

            double best_yaw = selectBestYaw(obj_yaw, current_ee_yaw);

            geometry_msgs::msg::PoseStamped grasp;
            grasp.header.frame_id = base_frame;
            grasp.header.stamp = ros_->node->now();
            grasp.pose = makePose(obj_x, obj_y, table_z + grasp_dz,
                                  M_PI, 0.0, best_yaw);

            // pregrasp: 用 cam_x_base/cam_y_base 参数(port 或默认值)
            double dx = obj_x - cam_x_base, dy = obj_y - cam_y_base;
            double norm = std::hypot(dx, dy);
            double off_x = norm > 1e-6 ? dx / norm * cam_offset : 0.0;
            double off_y = norm > 1e-6 ? dy / norm * cam_offset : 0.0;

            geometry_msgs::msg::PoseStamped pregrasp;
            pregrasp.header.frame_id = base_frame;
            pregrasp.header.stamp = grasp.header.stamp;
            pregrasp.pose = makePose(obj_x + off_x, obj_y + off_y,
                                     table_z + pregrasp_dz, M_PI, 0.0, best_yaw);

            setOutput("tcp_grasp_pose", grasp);
            setOutput("tcp_pregrasp_pose", pregrasp);
            setOutput("grasp_yaw", best_yaw);
            setOutput("grasp_pose_x", obj_x);
            setOutput("grasp_pose_y", obj_y);

            RCLCPP_INFO(ros_->node->get_logger(),
                        "Grasp[arm=%s depth]: uv=(%.1f,%.1f) z_cam=%.3f -> "
                        "base=(%.3f,%.3f,%.3f) yaw=%.3f",
                        arm_prefix.c_str(), u, v, z_cam,
                        obj_x, obj_y, table_z + grasp_dz, best_yaw);

            if (ros_->grasp_viz_pub)
            {
                geometry_msgs::msg::PoseArray arr;
                arr.header = pregrasp.header;
                arr.poses.push_back(pregrasp.pose);
                arr.poses.push_back(grasp.pose);
                ros_->grasp_viz_pub->publish(arr);
            }

            return BT::NodeStatus::SUCCESS;
        }
    }  // tick()

    void registerGraspNodes(BT::BehaviorTreeFactory &factory, RosContextPtr ros)
    {
        factory.registerBuilder<GenerateGraspCandidate>(
            "GenerateGraspCandidate",
            [ros](const std::string &name, const BT::NodeConfig &config)
            {
                return std::make_unique<GenerateGraspCandidate>(name, config, ros);
            });

        factory.registerBuilder<GeneratePlaceCandidate>(
            "GeneratePlaceCandidate",
            [ros](const std::string &name, const BT::NodeConfig &config)
            { return std::make_unique<GeneratePlaceCandidate>(name, config, ros->node); });
    }

    // helper: rpy -> quaternion (原有, 不变)
    static void rpy_to_quat(double r, double p, double y,
                             geometry_msgs::msg::Quaternion &q)
    {
        double cr = std::cos(r * 0.5), sr = std::sin(r * 0.5);
        double cp = std::cos(p * 0.5), sp = std::sin(p * 0.5);
        double cy = std::cos(y * 0.5), sy = std::sin(y * 0.5);
        q.w = cr * cp * cy + sr * sp * sy;
        q.x = sr * cp * cy - cr * sp * sy;
        q.y = cr * sp * cy + sr * cp * sy;
        q.z = cr * cp * sy - sr * sp * cy;
    }

    // ---- 改写: 按 arm_prefix 决定参数前缀 ----
    static geometry_msgs::msg::PoseStamped
    load_pose_from_params(rclcpp::Node::SharedPtr node,
                           const std::string &arm_prefix,
                           const std::string &pose_name)
    {
        // arm_prefix="" -> "place_*"; "left" -> "left_place_*"
        const std::string p = arm_prefix.empty()
                              ? pose_name
                              : arm_prefix + "_" + pose_name;
        const std::string default_frame = arm_prefix.empty()
                                          ? "base_link"
                                          : arm_prefix + "_base_link";

        // 幂等 declare (has_parameter check)
        if (!node->has_parameter(p + "_frame_id"))
            node->declare_parameter<std::string>(p + "_frame_id", default_frame);
        if (!node->has_parameter(p + "_position"))
            node->declare_parameter<std::vector<double>>(
                p + "_position", {0.0, 0.0, 0.0});
        if (!node->has_parameter(p + "_rpy"))
            node->declare_parameter<std::vector<double>>(
                p + "_rpy", {0.0, 0.0, 0.0});

        auto frame = node->get_parameter(p + "_frame_id").as_string();
        auto pos = node->get_parameter(p + "_position").as_double_array();
        auto rpy = node->get_parameter(p + "_rpy").as_double_array();

        geometry_msgs::msg::PoseStamped ps;
        ps.header.frame_id = frame;
        ps.pose.position.x = pos.size() > 0 ? pos[0] : 0.0;
        ps.pose.position.y = pos.size() > 1 ? pos[1] : 0.0;
        ps.pose.position.z = pos.size() > 2 ? pos[2] : 0.0;
        rpy_to_quat(rpy.size() > 0 ? rpy[0] : 0.0,
                    rpy.size() > 1 ? rpy[1] : 0.0,
                    rpy.size() > 2 ? rpy[2] : 0.0,
                    ps.pose.orientation);
        RCLCPP_INFO(node->get_logger(),
                    "loaded %s: frame=%s (%.3f, %.3f, %.3f) rpy=(%.3f, %.3f, %.3f)",
                    p.c_str(), frame.c_str(),
                    ps.pose.position.x, ps.pose.position.y, ps.pose.position.z,
                    rpy[0], rpy[1], rpy[2]);
        return ps;
    }

    GeneratePlaceCandidate::GeneratePlaceCandidate(
        const std::string &name, const BT::NodeConfig &config,
        rclcpp::Node::SharedPtr node)
        : BT::SyncActionNode(name, config), node_(node)
    {
        // 不在构造函数 load, 移到 tick 中懒加载
    }

    void GeneratePlaceCandidate::ensure_loaded(const std::string &arm_prefix)
    {
        if (place_cache_.count(arm_prefix) == 0)
        {
            place_cache_[arm_prefix] = load_pose_from_params(node_, arm_prefix, "place");
            pre_place_cache_[arm_prefix] = load_pose_from_params(node_, arm_prefix, "pre_place");
        }
    }

    BT::NodeStatus GeneratePlaceCandidate::tick()
    {
        std::string arm_prefix;
        getInput("arm_prefix", arm_prefix);
        ensure_loaded(arm_prefix);

        auto stamp = node_->now();
        auto place_pose = place_cache_[arm_prefix];
        auto pre_place_pose = pre_place_cache_[arm_prefix];
        place_pose.header.stamp = stamp;
        pre_place_pose.header.stamp = stamp;
        setOutput("place_pose", place_pose);
        setOutput("pre_place_pose", pre_place_pose);
        return BT::NodeStatus::SUCCESS;
    }

} // namespace s622_bt
```

---

## 关于 `place_pose` 参数的配置

现有 M1.7 通过 ROS 参数配 `place_frame_id` / `place_position` / `place_rpy` / `pre_place_*`。

M2.7 双臂化后需要为左右各配一套。这些参数通常在 bt_executor 的 launch 或 config yaml 里。批次 5 处理时会具体给出配法。**先不管**——代码支持懒加载,只要 M2.7.4 XML 传对 `arm_prefix`,后面加参数就行。

M1.7 兼容:XML 里 `<GeneratePlaceCandidate />`(不写 arm_prefix)→ 走原有 `place_*` 参数,不受影响。

## 编译

```bash
cd ~/my_S622
colcon build --packages-select s622_bt_manager
source install/setup.bash
```



## M2.7.1 批次 5: bt_executor_node 参数化

**核心思路**: executor 加 `arm` 参数,启动 tree 时塞到 root blackboard。XML 里所有 node 用 `arm_prefix="{arm}"` 引用。一份 XML 兼容左右。

### 修改点(diff-style)

1. `declare_parameter` 段加 `arm`
2. `runTree()` 里 `factory_.createTree(...)` 之后塞 blackboard
3. 顺带打个日志方便排查

完整替换后的文件:

```cpp
#include <atomic>
#include <chrono>
#include <memory>
#include <string>
#include <thread>

#include <rclcpp/rclcpp.hpp>
#include <rclcpp/executors/multi_threaded_executor.hpp>
#include <std_msgs/msg/bool.hpp>
#include <ament_index_cpp/get_package_share_directory.hpp>

#include <behaviortree_cpp/bt_factory.h>
#include <behaviortree_cpp/loggers/groot2_publisher.h>
#include <behaviortree_cpp/loggers/bt_cout_logger.h>
#include <behaviortree_cpp/loggers/bt_observer.h>

#include "s622_bt_manager/dummy_nodes.hpp"
#include "s622_bt_manager/perception_nodes.hpp"
#include "s622_bt_manager/grasp_nodes.hpp"
#include "s622_bt_manager/bt_executor.hpp"
#include "s622_bt_manager/motion_nodes.hpp"
#include "s622_bt_manager/gripper_nodes.hpp"
#include "s622_bt_manager/servo_nodes.hpp"
#include "s622_bt_manager/scene_nodes.hpp"

using namespace std::chrono_literals;

class BTExecutor : public rclcpp::Node
{
public:
    BTExecutor() : rclcpp::Node("bt_executor")
    {
        declare_parameter<std::string>("tree_file", "dummy_tree.xml");
        declare_parameter<std::string>("tree_id", "DummyTree");
        declare_parameter<int>("tick_rate_hz", 10);
        declare_parameter<bool>("auto_start", false);
        declare_parameter<int>("groot2_port", 1667);
        declare_parameter<std::string>("yolo_topic", "/yolov8/obb_detections");
        declare_parameter<std::string>("depth_topic", "/camera/depth/image_raw");
        declare_parameter<std::string>("caminfo_topic", "/camera/color/camera_info");
        declare_parameter<std::string>("grasp_viz_topic", "/grasp_visualization");
        // ---- 新增: arm 参数, 空 = M1.7 单臂兼容; "left"/"right" = 双臂化 ----
        declare_parameter<std::string>("arm", "");

        tick_rate_hz_ = get_parameter("tick_rate_hz").as_int();

        trigger_sub_ = create_subscription<std_msgs::msg::Bool>(
            "/bt_trigger", 10,
            std::bind(&BTExecutor::onTrigger, this, std::placeholders::_1));
    }

    ~BTExecutor()
    {
        stop_request_ = true;
        if (bt_thread_.joinable())
            bt_thread_.join();
    }

    void init()
    {
        ros_ctx_ = std::make_shared<s622_bt::RosContext>();
        ros_ctx_->node = shared_from_this();
        ros_ctx_->perception_cb_group =
            create_callback_group(rclcpp::CallbackGroupType::Reentrant);

        ros_ctx_->initYoloSubscription(get_parameter("yolo_topic").as_string());
        ros_ctx_->initDepthSubscription(get_parameter("depth_topic").as_string());
        ros_ctx_->initCameraInfoSubscription(get_parameter("caminfo_topic").as_string());
        ros_ctx_->initTf();
        ros_ctx_->initGraspVizPublisher(get_parameter("grasp_viz_topic").as_string());

        s622_bt::registerDummyNodes(factory_);
        s622_bt::registerPerceptionNodes(factory_, ros_ctx_);
        s622_bt::registerGraspNodes(factory_, ros_ctx_);
        s622_bt::registerMotionNodes(factory_, shared_from_this());
        s622_bt::registerGripperNodes(factory_, shared_from_this());
        s622_bt::registerServoNodes(factory_, shared_from_this());
        s622_bt::registerSceneNodes(factory_, shared_from_this());

        auto pkg_share = ament_index_cpp::get_package_share_directory("s622_bt_manager");
        auto tree_path = pkg_share + "/behavior_trees/" + get_parameter("tree_file").as_string();
        RCLCPP_INFO(get_logger(), "Loading tree: %s", tree_path.c_str());
        factory_.registerBehaviorTreeFromFile(tree_path);

        // ---- 打印 arm 配置 ----
        const auto arm = get_parameter("arm").as_string();
        if (arm.empty())
            RCLCPP_INFO(get_logger(), "arm parameter: '' (single-arm compat mode)");
        else
            RCLCPP_INFO(get_logger(), "arm parameter: '%s'", arm.c_str());

        if (get_parameter("auto_start").as_bool())
        {
            startTreeAsync();
        }
        else
        {
            RCLCPP_INFO(get_logger(), "Waiting for trigger on /bt_trigger ...");
        }
    }

private:
    void onTrigger(const std_msgs::msg::Bool::SharedPtr msg)
    {
        if (!msg->data)
            return;
        if (running_.load())
        {
            RCLCPP_WARN(get_logger(), "Tree already running, ignoring trigger");
            return;
        }
        startTreeAsync();
    }

    void startTreeAsync()
    {
        if (bt_thread_.joinable())
            bt_thread_.join();
        running_ = true;
        bt_thread_ = std::thread(&BTExecutor::runTree, this);
    }

    void runTree()
    {
        auto tree_id = get_parameter("tree_id").as_string();
        auto groot_port = get_parameter("groot2_port").as_int();
        // ---- 新增: 每次跑 tree 时重新读 arm (支持运行时改) ----
        auto arm = get_parameter("arm").as_string();

        RCLCPP_INFO(get_logger(),
                    "=== Starting BT [%s] arm='%s' ===",
                    tree_id.c_str(), arm.c_str());
        auto tree = factory_.createTree(tree_id);

        // ---- 新增: 把 arm 塞到 root blackboard, XML 里可用 {arm} 引用 ----
        tree.rootBlackboard()->set("arm", arm);

        BT::Groot2Publisher groot_pub(tree, groot_port);
        BT::StdCoutLogger cout_logger(tree);
        BT::TreeObserver observer(tree);

        auto t_start = now();
        auto status = BT::NodeStatus::RUNNING;
        rclcpp::Rate rate(tick_rate_hz_);
        while (rclcpp::ok() && !stop_request_.load() && status == BT::NodeStatus::RUNNING)
        {
            status = tree.tickOnce();
            rate.sleep();
        }

        if (status == BT::NodeStatus::FAILURE) {
            std::string failed_node = "unknown";
            for (const auto& [uid, path] : observer.uidToPath()) {
                const auto& stats = observer.getStatistics(uid);
                if (stats.last_result == BT::NodeStatus::FAILURE) {
                    failed_node = path;
                }
            }
            tree.rootBlackboard()->set("last_failure_node", failed_node);
            RCLCPP_ERROR(get_logger(),
                         "BT FAILURE at node: %s", failed_node.c_str());
        }

        double duration = (now() - t_start).seconds();
        RCLCPP_INFO(get_logger(), "=== BT finished: %s ===", BT::toStr(status).c_str());
        RCLCPP_INFO(get_logger(),
                    "[TRIAL_RESULT] arm=%s status=%s duration=%.1f",
                    arm.c_str(), BT::toStr(status).c_str(), duration);
        running_ = false;
    }

    BT::BehaviorTreeFactory factory_;
    rclcpp::Subscription<std_msgs::msg::Bool>::SharedPtr trigger_sub_;
    s622_bt::RosContextPtr ros_ctx_;
    std::thread bt_thread_;
    std::atomic<bool> running_{false};
    std::atomic<bool> stop_request_{false};
    int tick_rate_hz_;
};

int main(int argc, char **argv)
{
    rclcpp::init(argc, argv);
    auto node = std::make_shared<BTExecutor>();
    node->init();
    rclcpp::executors::MultiThreadedExecutor executor(rclcpp::ExecutorOptions(), 3);
    executor.add_node(node);
    executor.spin();
    rclcpp::shutdown();
    return 0;
}
```

**变化点**只有 4 行代码:

1. `declare_parameter<std::string>("arm", "");`
2. `init()` 里打印 arm 值
3. `runTree()` 里读 arm
4. `tree.rootBlackboard()->set("arm", arm);`

---

## M2.7.1 批次 5 完成 = 全部批次完成

**回顾 M2.7.1 完成的 BT node 参数化**:

| Node                                            | arm_prefix 用途                                            |
| ----------------------------------------------- | ---------------------------------------------------------- |
| MoveToPose                                      | 派生 action 名 `/{arm}/move_to_pose`                       |
| SetGripper                                      | 派生 service 名 `/{arm}/set_gripper`                       |
| VerifyGrasp                                     | 派生 feedback_joint 名 `{arm}_finger1_joint`               |
| VisualAlign / StopServo (M2.6 已改)             | 派生 action 名                                             |
| AttachObject                                    | 派生 link_name `{arm}_grasp_frame`                         |
| DetachObject                                    | 仅用于日志                                                 |
| DetectObject / LockTargetPixel / AlignLockCheck | 无(纯像素域)                                               |
| GenerateGraspCandidate                          | 派生 base_frame / ee_frame,camera_x/y_in_base 保留显式传入 |
| GeneratePlaceCandidate                          | 派生参数名 `{arm}_place_*`                                 |
| bt_executor                                     | 从 ROS 参数读 arm,塞到 blackboard                          |

## 编译

```bash
cd ~/my_S622
colcon build --packages-select s622_bt_manager
source install/setup.bash
```

---

## 下一步: M2.7.2 双臂反投影校准 + M2.7.4 XML 双臂化

### 立即要做

**M2.7.2 前置**:确认双臂 launch 里 camera 位置和 base 相对关系。快速验证:

```bash
ros2 launch gz_launch s622_dual_arm.launch.py
# 等 25s

# 相机在 left_base_link 系下位置
ros2 run tf2_ros tf2_echo left_base_link camera_color_optical_frame

# 相机在 right_base_link 系下位置
ros2 run tf2_ros tf2_echo right_base_link camera_color_optical_frame
```

期望左边 translation 大概是 `[0.35, -0.5, 0.9]`,右边 `[0.35, 0.5, 0.9]`(或类似,看 y 号)。这决定了 XML 里给 `GenerateGraspCandidate` 传的 `camera_x_in_base` / `camera_y_in_base`(仅深度 fallback 用)。

**M2.7.4 XML**:

写 `pick_place_dual.xml`(单份文件,`arm_prefix="{arm}"` 引用 blackboard):

```xml
<root BTCPP_format="4">
  <BehaviorTree ID="PickPlaceDual">
    <Fallback name="MainWithRecovery">
      <Sequence name="MainTask">
        <SetGripper arm_prefix="{arm}" command="open" timeout_sec="5.0" />
        <MoveToPose arm_prefix="{arm}" named_pose="home"
                    velocity_scale="0.3" ensure_servo_stopped="true" timeout_sec="30.0" />
        <!-- ... 中间感知 + servo align + grasp 全用 arm_prefix="{arm}" ... -->
      </Sequence>
      <Sequence name="Recovery">
        <!-- 类似 M1.7, 全部 arm_prefix="{arm}" -->
      </Sequence>
    </Fallback>
  </BehaviorTree>
</root>
```

跑法:

```bash
ros2 launch s622_bt_manager bt_executor.launch.py \
    tree_file:=pick_place_dual.xml \
    tree_id:=PickPlaceDual \
    arm:=left
```

跑完再:

```bash
# 修改参数并 trigger, 或直接重新 launch
ros2 param set /bt_executor arm right
ros2 topic pub --once /bt_trigger std_msgs/Bool "{data: true}"
```

**顺带一个问题**:M1.7 是不是有 `bt_executor.launch.py`?你现在跑 M1.7 pick-place 的 launch 是哪个?贴出来我基于它写 M2.7 的双臂化 launch。

**同时**:请贴当前跑 M1.7 pick-place 时装 place_pose 参数用的 config yaml(应该是 s622_bt_manager 或 gz_launch 下的某个 yaml,含 `place_frame_id / place_position / place_rpy / pre_place_*`),M2.7 需要在里面加左右两套。

先编译过 M2.7.1 批次 5,确认整个 s622_bt_manager 能通过,然后贴 launch + config yaml,推 M2.7.2 + M2.7.4。

