# M2.6 阶段做多少?建议分两步:

M2.6a: 落地方案 A(改 moveit_planner.py 里 execute 逻辑走 controller action)—— 让 V7 独立并发通过

M2.6b: 落地方案 D 骨架(SRDF 加 group + dual_move_server + 骨架 BT node)—— 但不做完整 handover 逻辑,只做 named_pose='both_home' 之类的联合 joint goal 验证

M2.7 pick-place 主要用 A(独立并行),M2.8 handover 主要用 D。


# 方案A 简述: 用 plan_only + 双 controller 直接执行

绕开 move_group 的执行环节。 arm_actions server 从 move_group 拿 plan,自己把 trajectory 发给 /left_arm_controller/follow_joint_trajectory action。

规划:走 move_group plan_kinematic_path service — 可并发(service 请求 MoveIt 内部并发处理没问题)

执行:走 left_arm_controller/follow_joint_trajectory action 和 right_arm_controller/follow_joint_trajectory action — 两个 controller 完全独立,天然并发

改动:moveit_planner.py(在 s622_arm_actions/utils/ 或类似位置)里执行那段,从"call execute_trajectory action"换成"call 各自 controller 的 follow_joint_trajectory action"。controller 名从参数读(arm_controller_action),配置里两侧不同。

目前的情况是：1 个controller_manager
controller_manager (单实例)
├── joint_state_broadcaster
├── left_arm_controller
├── left_hand_controller
├── right_arm_controller
└── right_hand_controller


| Controller              | 类型                        |
| ----------------------- | --------------------------- |
| left_arm_controller     | joint_trajectory_controller |
| left_hand_controller    | joint_trajectory_controller |
| right_arm_controller    | joint_trajectory_controller |
| right_hand_controller   | joint_trajectory_controller |
| joint_state_broadcaster | joint_state_broadcaster     |

在同一个 controller_manager 实例下，通过 dual_arm_controllers.yaml 注册。
move_group 端 dual_arm_moveit_controllers.yaml 也配了四个 FollowJointTrajectory controller 对应收这四个 action。


当前（串行）：
  left  →  move_group → Plan → Execute → left_arm_controller
  right →  move_group → Plan → Execute → right_arm_controller
                                ↑
                    同一时刻只能执行一个

方案A（并行）：
  left  →  /plan_kinematic_path (service) → 拿到轨迹 → 直接发 /left_arm_controller/follow_joint_trajectory
  right →  /plan_kinematic_path (service) → 拿到轨迹 → 直接发 /right_arm_controller/follow_joint_trajectory
                                                    ↑
                                          两个 action server 完全独立，互不阻塞



## M2.6a: 方案 A 落地 diff

**核心思路**:

* 用 `moveit_msgs/GetMotionPlan` service(即 `/plan_kinematic_path`)只规划不执行——规划是无状态的,MoveIt 内部可并发
* 拿到 trajectory 后直接发到 **各自 arm controller 的 FollowJointTrajectory action** —— 两个 controller 独立,天然并发
* 完全绕开 `/execute_trajectory` action(即 move_group 单例执行队列)

### 产出

| #   | 文件                     | 变动                                                                                    |
| --- | ------------------------ | --------------------------------------------------------------------------------------- |
| 1   | `moveit_planner.py`      | 加 `_plan_joint_target()` + `_execute_via_controller()`, 重写 `plan_to_joint_positions` |
| 2   | `move_to_pose_server.py` | 加参数 `arm_controller_action`, 传给 planner                                            |
| 3   | `left_arm_config.yaml`   | 加 `arm_controller_action: '/left_arm_controller/follow_joint_trajectory'`              |
| 4   | `right_arm_config.yaml`  | 加 `arm_controller_action: '/right_arm_controller/follow_joint_trajectory'`             |

---

### 1. `moveit_planner.py` 修改

在 imports 段加:

```python
from rclpy.action import ActionClient
from moveit_msgs.srv import GetMotionPlan, GetPositionIK
from moveit_msgs.msg import (
    RobotState, MotionPlanRequest, Constraints, JointConstraint,
    RobotTrajectory,
)
from control_msgs.action import FollowJointTrajectory
```

`__init__` 签名和 body 加两处:

```python
def __init__(
    self,
    node: Node,
    joint_names: List[str],
    base_link: str,
    end_effector: str,
    group_name: str,
    callback_group: Optional[CallbackGroup] = None,
    max_vel: float = 0.2,
    max_acc: float = 0.2,
    planner_id: str = "RRTConnect",
    pipeline_id: str = "fairino",
    arm_controller_action: str = "",   # ← 新增
):
    self.node = node
    self.joint_names = joint_names
    self.base_link = base_link
    self.end_effector = end_effector
    self.group_name = group_name
    self.arm_controller_action = arm_controller_action   # ← 新增
    cb = callback_group or ReentrantCallbackGroup()
    self._cb = cb

    self.moveit2 = MoveIt2(...)  # 保留不动, 用于 plan_to_pose fallback
    ...
    # ---- 关节安全限位 ----
    ...
    # ---- joint state cache ----
    ...
    # ---- IK service clients ----
    ...
    
    # ---- 新增: plan-only service + controller action client ----
    self._plan_client = node.create_client(
        GetMotionPlan, '/plan_kinematic_path', callback_group=cb)
    if arm_controller_action:
        self._exec_client = ActionClient(
            node, FollowJointTrajectory, arm_controller_action,
            callback_group=cb)
    else:
        self._exec_client = None
        node.get_logger().warn(
            'arm_controller_action not set; will fallback to pymoveit2 execute')
```

在类里加两个新方法(放 `plan_to_joint_positions` 之前):

```python
    # ============ 方案 A: plan-only + controller-execute ============
    def _plan_joint_target(self, positions: List[float],
                            current: List[float],
                            timeout_s: float = 8.0
                           ) -> Optional[RobotTrajectory]:
        """走 /plan_kinematic_path service, 只规划返回 trajectory."""
        if not self._plan_client.service_is_ready():
            if not self._plan_client.wait_for_service(timeout_sec=2.0):
                self.node.get_logger().error(
                    '/plan_kinematic_path not available')
                return None

        req = GetMotionPlan.Request()
        mpr = req.motion_plan_request
        mpr.group_name = self.group_name
        mpr.planner_id = self.moveit2.planner_id
        mpr.pipeline_id = self.moveit2.pipeline_id
        mpr.num_planning_attempts = 10
        mpr.allowed_planning_time = 5.0
        mpr.max_velocity_scaling_factor = float(self.moveit2.max_velocity)
        mpr.max_acceleration_scaling_factor = float(self.moveit2.max_acceleration)

        rs = RobotState()
        rs.joint_state.name = list(self.joint_names)
        rs.joint_state.position = list(current)
        rs.is_diff = False   # 明确不是增量, 免得 planning_scene 状态污染
        mpr.start_state = rs

        goal = Constraints()
        for name, pos in zip(self.joint_names, positions):
            jc = JointConstraint()
            jc.joint_name = name
            jc.position = float(pos)
            jc.tolerance_above = 0.001
            jc.tolerance_below = 0.001
            jc.weight = 1.0
            goal.joint_constraints.append(jc)
        mpr.goal_constraints.append(goal)

        future = self._plan_client.call_async(req)
        res = self._wait_future(future, timeout_s)
        if res is None:
            self.node.get_logger().error('plan_kinematic_path timeout')
            return None
        code = res.motion_plan_response.error_code.val
        if code != 1:   # SUCCESS = 1
            self.node.get_logger().warn(
                f'plan_kinematic_path failed: error_code={code}')
            return None
        return res.motion_plan_response.trajectory

    def _execute_via_controller(self, trajectory: RobotTrajectory,
                                 timeout_s: float = 30.0) -> bool:
        """把 trajectory 发到 arm controller 的 FollowJointTrajectory action.
        绕开 /execute_trajectory 单例, 双臂天然并发."""
        if self._exec_client is None:
            self.node.get_logger().error(
                'exec_client not initialized (arm_controller_action empty)')
            return False
        if not self._exec_client.wait_for_server(timeout_sec=3.0):
            self.node.get_logger().error(
                f'controller action unavailable: {self.arm_controller_action}')
            return False

        goal = FollowJointTrajectory.Goal()
        goal.trajectory = trajectory.joint_trajectory

        goal_future = self._exec_client.send_goal_async(goal)
        goal_handle = self._wait_future(goal_future, timeout_s=3.0)
        if goal_handle is None:
            self.node.get_logger().error('send_goal timeout')
            return False
        if not goal_handle.accepted:
            self.node.get_logger().error('goal rejected by controller')
            return False

        result_future = goal_handle.get_result_async()
        wrapped = self._wait_future(result_future, timeout_s=timeout_s)
        if wrapped is None:
            self.node.get_logger().error('execute timeout')
            goal_handle.cancel_goal_async()
            return False

        err = wrapped.result.error_code
        if err != 0:
            self.node.get_logger().error(
                f'controller execution failed: error_code={err}, '
                f'msg="{wrapped.result.error_string}"')
            return False
        return True
```

**替换现有** `plan_to_joint_positions` 方法:

```python
    def plan_to_joint_positions(self, positions: List[float]) -> bool:
        if len(positions) != len(self.joint_names):
            self.node.get_logger().error(
                f'joint length mismatch: {len(positions)} vs {len(self.joint_names)}')
            return False

        # 无 controller action: fallback 到 pymoveit2 (单臂兼容路径)
        if self._exec_client is None:
            self.moveit2.move_to_configuration(positions)
            return self.moveit2.wait_until_executed()

        current = self.get_current_joint_positions()
        if current is None:
            self.node.get_logger().warn('no joint state; cannot plan')
            return False

        t0 = time.time()
        traj = self._plan_joint_target(positions, current)
        t_plan = time.time() - t0
        if traj is None:
            return False
        n_pts = len(traj.joint_trajectory.points)
        dur = traj.joint_trajectory.points[-1].time_from_start
        dur_s = dur.sec + dur.nanosec / 1e9
        self.node.get_logger().info(
            f'plan OK: {n_pts} pts, {dur_s:.2f}s traj, {t_plan*1000:.0f}ms plan')

        return self._execute_via_controller(traj, timeout_s=dur_s * 2 + 5.0)
```

**`plan_to_pose`(裸 pymoveit2 fallback)保留不动**——只有 IK 完全失败时才走它,场景少。M1.7 单臂兼容也需要它。M3+ 阶段可以再统一。

---

### 2. `move_to_pose_server.py` 修改

在 `declare_parameter` 段加:

```python
        self.declare_parameter('arm_controller_action', '')   # ← 新增
```

读参 + 传给 planner:

```python
        joint_names = list(self.get_parameter('joint_names').value)
        base_link = self.get_parameter('base_link').value
        end_effector = self.get_parameter('end_effector').value
        group_name = self.get_parameter('group_name').value
        self._default_v = self.get_parameter('default_velocity_scale').value
        self._default_a = self.get_parameter('default_acceleration_scale').value
        servo_ns = self.get_parameter('servo_ns').value
        arm_controller_action = self.get_parameter(
            'arm_controller_action').value      # ← 新增
        ...
        cb = ReentrantCallbackGroup()
        self.servo_lc = ServoLifecycleManager(
            self, callback_group=cb, servo_ns=servo_ns)
        self.planner = MoveItPlanner(
            node=self,
            joint_names=joint_names,
            base_link=base_link,
            end_effector=end_effector,
            group_name=group_name,
            callback_group=cb,
            max_vel=self._default_v,
            max_acc=self._default_a,
            arm_controller_action=arm_controller_action,   # ← 传给 planner
        )
```

---

### 3. `left_arm_config.yaml` 追加

在 `/left/move_to_pose_server: ros__parameters:` 段末尾加一行:

```yaml
/left/move_to_pose_server:
  ros__parameters:
    ...
    servo_ns: 'left'
    arm_controller_action: '/left_arm_controller/follow_joint_trajectory'   # ← 新增
    named_pose_names: ['home', 'safe', 'grasp_cube']
    ...
```

### 4. `right_arm_config.yaml` 追加

```yaml
/right/move_to_pose_server:
  ros__parameters:
    ...
    servo_ns: 'right'
    arm_controller_action: '/right_arm_controller/follow_joint_trajectory'   # ← 新增
    named_pose_names: ['home', 'safe', 'grasp_cube']
    ...
```

---

## 编译

```bash
cd ~/my_S622
colcon build --packages-select s622_arm_actions --merge-install --symlink-install
source install/setup.bash

pkill -f "ros2 launch"
pkill -f move_to_pose_server

ros2 launch gz_launch s622_dual_arm.launch.py
```

## 验证

### V4 重跑

```bash
ros2 action send_goal /left/move_to_pose s622_bt_manager/action/MoveToPose \
  "{named_pose: 'home', velocity_scale: 0.2}" --feedback
```

日志应该看到:

```
[left.move_to_pose_server]: going to named pose: home
[MoveItPlanner]: plan OK: 42 pts, 3.20s traj, 145ms plan
```

**关键区别**:M2.6 之前是 `Received new planning service request` 出现在 `move_group` 日志里 + `execute_trajectory` action(串行队列)。现在 `plan_kinematic_path` service 直接返回 traj,然后走 `/left_arm_controller/follow_joint_trajectory` action(独立并发)。

### V7 重跑(方案 A 判定点)

```bash
ros2 action send_goal /right/move_to_pose s622_bt_manager/action/MoveToPose \
  "{named_pose: 'home'}" &
ros2 action send_goal /left/move_to_pose s622_bt_manager/action/MoveToPose \
  "{named_pose: 'safe'}" &
wait
```

**期望**:两个 goal 几乎同时 accepted,双臂**同时**运动。总耗时 ≈ 单臂运动时间(而不是两倍)。

如果规划耗时不同(比如 left 规划 100ms,right 规划 150ms),起动会有几十毫秒错位,但执行阶段完全并发。

### 常见坑

1. **`FollowJointTrajectory` 结果 `error_code=0` 是 SUCCESSFUL**
   `control_msgs/FollowJointTrajectory` 的 SUCCESSFUL 是 0(不是像 MoveIt 的 SUCCESS=1)。上面代码里 `err != 0` 判定正确。

2. **plan_kinematic_path 单例 vs 并发**
   `plan_kinematic_path` 是 service。ROS 2 service 默认单线程处理,并发两个 request 会**在 move_group 内部串行处理**(不是拒绝)。但每次 plan 只 100-200ms,串行也够快。执行阶段并发才是关键。想让 plan 也真并发,需要 move_group 用 MultiThreadedExecutor——已经是 MoveIt 默认。

3. **start_state 用 current(不用 diff)**
   我设 `is_diff=False` + `position=current`,让 planner 明确知道起点是当前 joint state。防止 planning_scene 里状态不一致引起 planning failure。

4. **`arm_controller_action` 空串 fallback**
   如果 config 里忘配这个参数,走 pymoveit2 的老路径(单例执行,不能并发)。日志有 warn。M1.7 单臂 launch 里空着就自动兼容。

5. **规划时长的 timeout 计算**
   `timeout_s = dur_s * 2 + 5.0`——规划出的 traj 时长的 2 倍 + 5s 兜底。若机械臂运动比预期慢(比如受碰撞减速),可以避免误报 timeout。

## M2.6b 预告

V7 并发通过后,推 M2.6b(方案 D 骨架):

* SRDF 加 `<group name="dual_arm">`
* 新建 `dual_move_server.py`(在 `/dual` namespace 起)
* 提供 `/dual/move_to_joint_state` action,接受 12 维 joint goal
* 内部用 `_plan_joint_target` + 通过 `/execute_trajectory` 走 MoveIt 单例执行(单条 traj 双 controller 自动拆分)

M2.7 pick-place 主用 A(独立),M2.8 handover 用 D(联合)。

先跑 M2.6a 验证 V4/V7,过了贴结果我推 M2.6b。

