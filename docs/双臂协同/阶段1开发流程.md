# 创建包

# 先跑通流程， 验证环境(M1.0, M1.1)

先别急着把所有节点都写出来。按 Milestone 1.1 的节奏，下一步应该是**先把骨架跑通，再往里填节点**。这样能尽早发现环境/CMake/XML 加载问题，避免到了写第十个节点才发现 BT.CPP v4 都没装对。

## 推荐的下一步顺序

1. **确认环境**（5 分钟）

   * `apt list --installed 2>/dev/null | grep behaviortree` 看是 v3 还是 v4
   * 如果 Humble 默认是 `behaviortree_cpp_v3`，需要装 `behaviortree_cpp`（v4 包名）
   * 顺便确认 `behaviortree_cpp` 的 cmake target 名（v4 是 `BT::behaviortree_cpp`）

2. **写 `bt_executor_node.cpp` 骨架**（先什么节点都不注册）

   * 加载 XML、创建 tree、循环 tickRoot
   * 加个 `/bt_trigger` topic 触发执行（修订版计划里写的）
   * 暂时只 log，不连任何业务

3. **写 3 个 Dummy 节点**

   * `CheckSystemReady`：永远返回 SUCCESS
   * `PrintMessage`：打印一行 log
   * `Wait`：sleep N 秒

4. **改一个 minimal XML 单独验证**（不动你已经写好的完整版）

   ```xml
   <root BTCPP_format="4">
     <BehaviorTree ID="DummyTree">
       <Sequence>
         <CheckSystemReady/>
         <PrintMessage msg="hello"/>
         <Wait sec="1.0"/>
       </Sequence>
     </BehaviorTree>
   </root>
   ```

   先用这棵小树跑通，**完整版 XML 留到对应节点实现后再切回去**。

5. **CMakeLists.txt + package.xml + launch 文件配通**，`colcon build` 过

6. **跑一次，看到 tick 输出**，再装 Groot2 连上看可视化

## 好。现在 Milestone 1.1 的目标是：**先用 dummy 树跑通骨架**，确认 CMake、XML 加载、Groot2 都正常，再回头填业务节点。

## 1. `package.xml`

```xml
<?xml version="1.0"?>
<package format="3">
  <name>s622_bt_manager</name>
  <version>0.1.0</version>
  <description>BT.CPP-based task manager for S622 single-arm pick-and-place</description>
  <maintainer email="you@example.com">you</maintainer>
  <license>MIT</license>

  <buildtool_depend>ament_cmake</buildtool_depend>

  <depend>rclcpp</depend>
  <depend>std_msgs</depend>
  <depend>ament_index_cpp</depend>
  <depend>behaviortree_cpp</depend>

  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
```

## 2. `CMakeLists.txt`

```cmake
cmake_minimum_required(VERSION 3.8)
project(s622_bt_manager)

if(NOT CMAKE_CXX_STANDARD)
  set(CMAKE_CXX_STANDARD 17)
endif()

if(CMAKE_COMPILER_IS_GNUCXX OR CMAKE_CXX_COMPILER_ID MATCHES "Clang")
  add_compile_options(-Wall -Wextra -Wpedantic)
endif()

find_package(ament_cmake REQUIRED)
find_package(rclcpp REQUIRED)
find_package(std_msgs REQUIRED)
find_package(ament_index_cpp REQUIRED)
find_package(behaviortree_cpp REQUIRED)

add_executable(bt_executor_node src/bt_executor_node.cpp)
target_include_directories(bt_executor_node PRIVATE include)
ament_target_dependencies(bt_executor_node
  rclcpp
  std_msgs
  ament_index_cpp
  behaviortree_cpp
)

install(TARGETS bt_executor_node
  DESTINATION lib/${PROJECT_NAME})

install(DIRECTORY behavior_trees launch
  DESTINATION share/${PROJECT_NAME})

ament_package()
```

## 3. `src/bt_executor_node.cpp`

骨架 + 3 个 dummy 节点，全部写在一个文件里（后面再拆）：

```cpp
#include <chrono>
#include <memory>
#include <string>
#include <thread>

#include <rclcpp/rclcpp.hpp>
#include <std_msgs/msg/bool.hpp>
#include <ament_index_cpp/get_package_share_directory.hpp>

#include <behaviortree_cpp/bt_factory.h>
#include <behaviortree_cpp/loggers/groot2_publisher.h>
#include <behaviortree_cpp/loggers/bt_cout_logger.h>

using namespace std::chrono_literals;

// ===================== Dummy 节点 =====================

class CheckSystemReady : public BT::SyncActionNode {
public:
  CheckSystemReady(const std::string& name, const BT::NodeConfig& config)
    : BT::SyncActionNode(name, config) {}

  static BT::PortsList providedPorts() { return {}; }

  BT::NodeStatus tick() override {
    RCLCPP_INFO(rclcpp::get_logger("CheckSystemReady"), "System ready (dummy)");
    return BT::NodeStatus::SUCCESS;
  }
};

class PrintMessage : public BT::SyncActionNode {
public:
  PrintMessage(const std::string& name, const BT::NodeConfig& config)
    : BT::SyncActionNode(name, config) {}

  static BT::PortsList providedPorts() {
    return { BT::InputPort<std::string>("msg") };
  }

  BT::NodeStatus tick() override {
    auto msg = getInput<std::string>("msg");
    RCLCPP_INFO(rclcpp::get_logger("PrintMessage"), "%s",
                msg.value_or(std::string{"(no msg)"}).c_str());
    return BT::NodeStatus::SUCCESS;
  }
};

class Wait : public BT::SyncActionNode {
public:
  Wait(const std::string& name, const BT::NodeConfig& config)
    : BT::SyncActionNode(name, config) {}

  static BT::PortsList providedPorts() {
    return { BT::InputPort<double>("sec") };
  }

  BT::NodeStatus tick() override {
    double sec = 1.0;
    getInput("sec", sec);
    RCLCPP_INFO(rclcpp::get_logger("Wait"), "Waiting %.2f sec", sec);
    std::this_thread::sleep_for(std::chrono::duration<double>(sec));
    return BT::NodeStatus::SUCCESS;
  }
};

// ===================== Executor =====================

class BTExecutor : public rclcpp::Node {
public:
  BTExecutor() : rclcpp::Node("bt_executor") {
    this->declare_parameter<std::string>("tree_file", "dummy_tree.xml");
    this->declare_parameter<std::string>("tree_id", "DummyTree");
    this->declare_parameter<int>("tick_rate_hz", 10);
    this->declare_parameter<bool>("auto_start", false);
    this->declare_parameter<int>("groot2_port", 1667);

    tick_rate_hz_ = this->get_parameter("tick_rate_hz").as_int();

    // 注册节点
    factory_.registerNodeType<CheckSystemReady>("CheckSystemReady");
    factory_.registerNodeType<PrintMessage>("PrintMessage");
    factory_.registerNodeType<Wait>("Wait");

    // 加载行为树
    auto pkg_share = ament_index_cpp::get_package_share_directory("s622_bt_manager");
    auto tree_file = this->get_parameter("tree_file").as_string();
    auto tree_path = pkg_share + "/behavior_trees/" + tree_file;
    RCLCPP_INFO(this->get_logger(), "Loading tree: %s", tree_path.c_str());
    factory_.registerBehaviorTreeFromFile(tree_path);

    trigger_sub_ = this->create_subscription<std_msgs::msg::Bool>(
      "/bt_trigger", 10,
      std::bind(&BTExecutor::onTrigger, this, std::placeholders::_1));

    if (this->get_parameter("auto_start").as_bool()) {
      // 延迟一点点执行，让 spin 跑起来
      auto timer = this->create_wall_timer(
        500ms, [this]() { this->runTree(); });
      one_shot_timer_ = timer;
    } else {
      RCLCPP_INFO(this->get_logger(), "Waiting for trigger on /bt_trigger ...");
    }
  }

private:
  void onTrigger(const std_msgs::msg::Bool::SharedPtr msg) {
    if (!msg->data) return;
    if (running_) {
      RCLCPP_WARN(this->get_logger(), "Tree already running, ignoring trigger");
      return;
    }
    runTree();
  }

  void runTree() {
    running_ = true;
    auto tree_id = this->get_parameter("tree_id").as_string();
    auto groot_port = this->get_parameter("groot2_port").as_int();

    RCLCPP_INFO(this->get_logger(), "=== Starting BT [%s] ===", tree_id.c_str());

    auto tree = factory_.createTree(tree_id);
    BT::Groot2Publisher groot_pub(tree, groot_port);
    BT::StdCoutLogger cout_logger(tree);

    auto status = BT::NodeStatus::RUNNING;
    rclcpp::Rate rate(tick_rate_hz_);
    while (rclcpp::ok() && status == BT::NodeStatus::RUNNING) {
      status = tree.tickOnce();
      rate.sleep();
    }

    RCLCPP_INFO(this->get_logger(), "=== BT finished: %s ===",
                BT::toStr(status).c_str());
    running_ = false;
    if (one_shot_timer_) one_shot_timer_->cancel();
  }

  BT::BehaviorTreeFactory factory_;
  rclcpp::Subscription<std_msgs::msg::Bool>::SharedPtr trigger_sub_;
  rclcpp::TimerBase::SharedPtr one_shot_timer_;
  int tick_rate_hz_;
  bool running_ = false;
};

int main(int argc, char** argv) {
  rclcpp::init(argc, argv);
  auto node = std::make_shared<BTExecutor>();
  rclcpp::spin(node);
  rclcpp::shutdown();
  return 0;
}
```

## 4. `behavior_trees/dummy_tree.xml`

**新建一个**，不要动你已经写好的 `single_arm_pick_place.xml`（那个等节点齐了再切换）：

```xml
<root BTCPP_format="4">
  <BehaviorTree ID="DummyTree">
    <Sequence>
      <CheckSystemReady/>
      <PrintMessage msg="Hello from BT.CPP v4"/>
      <Wait sec="1.0"/>
      <PrintMessage msg="Done"/>
    </Sequence>
  </BehaviorTree>
</root>
```

## 5. `launch/single_arm_pick_place_bt.launch.py`

```python
from launch import LaunchDescription
from launch.actions import DeclareLaunchArgument
from launch.substitutions import LaunchConfiguration
from launch_ros.actions import Node


def generate_launch_description():
    return LaunchDescription([
        DeclareLaunchArgument('tree_file', default_value='dummy_tree.xml'),
        DeclareLaunchArgument('tree_id', default_value='DummyTree'),
        DeclareLaunchArgument('auto_start', default_value='false'),

        Node(
            package='s622_bt_manager',
            executable='bt_executor_node',
            name='bt_executor',
            output='screen',
            parameters=[{
                'tree_file': LaunchConfiguration('tree_file'),
                'tree_id': LaunchConfiguration('tree_id'),
                'auto_start': LaunchConfiguration('auto_start'),
                'tick_rate_hz': 10,
                'groot2_port': 1667,
            }]
        ),
    ])
```

## 6. 验证步骤

```bash
cd ~/my_S622
colcon build --merge-install --symlink-install --packages-select s622_bt_manager
source install/setup.bash

# 终端 1：启动 BT
ros2 launch s622_bt_manager single_arm_pick_place_bt.launch.py

# 终端 2：触发
ros2 topic pub /bt_trigger std_msgs/msg/Bool "data: true" --once
```

预期输出：

```
[bt_executor]: Loading tree: .../share/s622_bt_manager/behavior_trees/dummy_tree.xml
[bt_executor]: Waiting for trigger on /bt_trigger ...
# 收到 trigger 后
[bt_executor]: === Starting BT [DummyTree] ===
[CheckSystemReady]: System ready (dummy)
[PrintMessage]: Hello from BT.CPP v4
[Wait]: Waiting 1.00 sec
[PrintMessage]: Done
[bt_executor]: === BT finished: SUCCESS ===
```

## 7. 可能踩的坑

| 现象                                       | 原因                          | 解决                                                                     |
| ------------------------------------------ | ----------------------------- | ------------------------------------------------------------------------ |
| `find_package(behaviortree_cpp)` 失败      | 装的是 v3                     | 改成 `behaviortree_cpp_v3` + 头文件路径去掉斜杠后的目录                  |
| `Groot2Publisher` 头文件找不到             | v3 没这个类，是 ZMQ publisher | 用 `<behaviortree_cpp_v3/loggers/bt_zmq_publisher.h>` 替代，或直接注释掉 |
| `tickOnce` 找不到                          | v3 API 叫 `tickRoot`          | 改名                                                                     |
| XML 报 unknown node "RetryUntilSuccessful" | 你完整版 XML 提前加载了       | 这阶段先用 dummy_tree.xml，别管完整版                                    |
| Groot2 连不上                              | 端口冲突或 Groot2 还没装      | 这一步可以先跳过，能 tick 出 SUCCESS 就算过                              |

## 下一步

跑通后告诉我输出长什么样，然后进入 **Milestone 1.2：接入 YOLO 检测**。那一步开始写 `DetectObject` 这种真实业务节点，并且会建议你做一些目录拆分（不要所有节点都堆在 `bt_executor_node.cpp` 里）。



# MileStone 1.2 接入 yolo 检测
进入 **Milestone 1.2**。这一步要做三件事：

1. **目录拆分**：把 dummy 节点搬出去
2. **加共享 ROS 上下文**：让 BT 节点能用 ROS（订阅 + 缓存最新一帧）
3. **加 `DetectObject` 节点 + 测试树**

⚠️ 一个关键点要先说：现在的 executor 在 `onTrigger` 里跑 BT 循环会**阻塞 spin**，订阅回调进不来。所以这一步必须切换到 **MultiThreadedExecutor + 独立 callback group**，否则 DetectObject 永远拿不到 YOLO 消息。

---

## 1. 拆分 dummy 节点

### `include/s622_bt_manager/dummy_nodes.hpp`

```cpp
#pragma once
#include <behaviortree_cpp/bt_factory.h>

namespace s622_bt {

class CheckSystemReady : public BT::SyncActionNode {
public:
  CheckSystemReady(const std::string& name, const BT::NodeConfig& config)
    : BT::SyncActionNode(name, config) {}
  static BT::PortsList providedPorts() { return {}; }
  BT::NodeStatus tick() override;
};

class PrintMessage : public BT::SyncActionNode {
public:
  PrintMessage(const std::string& name, const BT::NodeConfig& config)
    : BT::SyncActionNode(name, config) {}
  static BT::PortsList providedPorts() {
    return { BT::InputPort<std::string>("msg") };
  }
  BT::NodeStatus tick() override;
};

class Wait : public BT::SyncActionNode {
public:
  Wait(const std::string& name, const BT::NodeConfig& config)
    : BT::SyncActionNode(name, config) {}
  static BT::PortsList providedPorts() {
    return { BT::InputPort<double>("sec") };
  }
  BT::NodeStatus tick() override;
};

void registerDummyNodes(BT::BehaviorTreeFactory& factory);

}  // namespace s622_bt
```

### `src/dummy_nodes.cpp`

```cpp
#include "s622_bt_manager/dummy_nodes.hpp"

#include <chrono>
#include <thread>
#include <rclcpp/rclcpp.hpp>

namespace s622_bt {

BT::NodeStatus CheckSystemReady::tick() {
  RCLCPP_INFO(rclcpp::get_logger("CheckSystemReady"), "System ready (dummy)");
  return BT::NodeStatus::SUCCESS;
}

BT::NodeStatus PrintMessage::tick() {
  auto msg = getInput<std::string>("msg");
  RCLCPP_INFO(rclcpp::get_logger("PrintMessage"), "%s",
              msg.value_or(std::string{"(no msg)"}).c_str());
  return BT::NodeStatus::SUCCESS;
}

BT::NodeStatus Wait::tick() {
  double sec = 1.0;
  getInput("sec", sec);
  RCLCPP_INFO(rclcpp::get_logger("Wait"), "Waiting %.2f sec", sec);
  std::this_thread::sleep_for(std::chrono::duration<double>(sec));
  return BT::NodeStatus::SUCCESS;
}

void registerDummyNodes(BT::BehaviorTreeFactory& factory) {
  factory.registerNodeType<CheckSystemReady>("CheckSystemReady");
  factory.registerNodeType<PrintMessage>("PrintMessage");
  factory.registerNodeType<Wait>("Wait");
}

}  // namespace s622_bt
```

## 2. 共享 ROS 上下文 + DetectObject

### `include/s622_bt_manager/perception_nodes.hpp`

```cpp
#pragma once

#include <memory>
#include <mutex>
#include <string>

#include <behaviortree_cpp/bt_factory.h>
#include <rclcpp/rclcpp.hpp>
#include <yolov8_obb_msgs/msg/yolov8_inference.hpp>

namespace s622_bt {

// 注入给需要 ROS 能力的 BT 节点
struct RosContext {
  rclcpp::Node::SharedPtr node;
  rclcpp::CallbackGroup::SharedPtr perception_cb_group;

  std::mutex yolo_mutex;
  yolov8_obb_msgs::msg::Yolov8Inference::SharedPtr latest_yolo;
  rclcpp::Subscription<yolov8_obb_msgs::msg::Yolov8Inference>::SharedPtr yolo_sub;

  void initYoloSubscription(const std::string& topic);
};

using RosContextPtr = std::shared_ptr<RosContext>;

class DetectObject : public BT::SyncActionNode {
public:
  DetectObject(const std::string& name,
               const BT::NodeConfig& config,
               RosContextPtr ros);

  static BT::PortsList providedPorts();
  BT::NodeStatus tick() override;

private:
  RosContextPtr ros_;
};

void registerPerceptionNodes(BT::BehaviorTreeFactory& factory, RosContextPtr ros);

}  // namespace s622_bt
```

### `src/perception_nodes.cpp`

```cpp
#include "s622_bt_manager/perception_nodes.hpp"

namespace s622_bt {

void RosContext::initYoloSubscription(const std::string& topic) {
  rclcpp::SubscriptionOptions opts;
  opts.callback_group = perception_cb_group;

  yolo_sub = node->create_subscription<yolov8_obb_msgs::msg::Yolov8Inference>(
    topic, rclcpp::SensorDataQoS(),
    [this](yolov8_obb_msgs::msg::Yolov8Inference::SharedPtr msg) {
      std::lock_guard<std::mutex> lock(yolo_mutex);
      latest_yolo = msg;
    },
    opts);
}

DetectObject::DetectObject(const std::string& name,
                           const BT::NodeConfig& config,
                           RosContextPtr ros)
  : BT::SyncActionNode(name, config), ros_(std::move(ros)) {}

BT::PortsList DetectObject::providedPorts() {
  return {
    BT::InputPort<std::string>("object_name", "cube", "target class name"),
    BT::InputPort<double>("min_confidence", 0.05, "minimum confidence"),
    BT::InputPort<double>("max_age_sec", 1.0, "max age of latest detection"),
    BT::OutputPort<double>("output_u"),
    BT::OutputPort<double>("output_v"),
    BT::OutputPort<double>("output_yaw"),
    BT::OutputPort<double>("output_confidence"),
  };
}

BT::NodeStatus DetectObject::tick() {
  std::string object_name = "cube";
  double min_conf = 0.05;
  double max_age = 1.0;
  getInput("object_name", object_name);
  getInput("min_confidence", min_conf);
  getInput("max_age_sec", max_age);

  yolov8_obb_msgs::msg::Yolov8Inference::SharedPtr latest;
  {
    std::lock_guard<std::mutex> lock(ros_->yolo_mutex);
    latest = ros_->latest_yolo;
  }

  if (!latest) {
    RCLCPP_WARN_THROTTLE(ros_->node->get_logger(),
                         *ros_->node->get_clock(), 2000,
                         "DetectObject: no YOLO message received yet");
    return BT::NodeStatus::FAILURE;
  }

  auto now = ros_->node->now();
  auto msg_time = rclcpp::Time(latest->header.stamp);
  double age = (now - msg_time).seconds();
  if (age > max_age) {
    RCLCPP_WARN(ros_->node->get_logger(),
                "DetectObject: latest YOLO is %.2fs old (> %.2f)", age, max_age);
    return BT::NodeStatus::FAILURE;
  }

  const yolov8_obb_msgs::msg::InferenceResult* best = nullptr;
  for (const auto& r : latest->results) {
    if (r.class_name != object_name) continue;
    if (r.confidence < min_conf) continue;
    if (!best || r.confidence > best->confidence) best = &r;
  }

  if (!best) {
    RCLCPP_WARN(ros_->node->get_logger(),
                "DetectObject: no '%s' with conf>=%.2f in %zu results",
                object_name.c_str(), min_conf, latest->results.size());
    return BT::NodeStatus::FAILURE;
  }

  setOutput("output_u",          static_cast<double>(best->center_x));
  setOutput("output_v",          static_cast<double>(best->center_y));
  setOutput("output_yaw",        static_cast<double>(best->angle));
  setOutput("output_confidence", static_cast<double>(best->confidence));

  RCLCPP_INFO(ros_->node->get_logger(),
              "DetectObject [%s]: uv=(%.1f,%.1f) yaw=%.3f conf=%.2f age=%.2fs",
              object_name.c_str(),
              best->center_x, best->center_y, best->angle, best->confidence, age);

  return BT::NodeStatus::SUCCESS;
}

void registerPerceptionNodes(BT::BehaviorTreeFactory& factory, RosContextPtr ros) {
  BT::NodeBuilder builder =
    [ros](const std::string& name, const BT::NodeConfig& config) {
      return std::make_unique<DetectObject>(name, config, ros);
    };
  factory.registerBuilder<DetectObject>("DetectObject", builder);
}

}  // namespace s622_bt
```

## 3. 重写 `bt_executor_node.cpp`

```cpp
#include <chrono>
#include <memory>
#include <string>

#include <rclcpp/rclcpp.hpp>
#include <rclcpp/executors/multi_threaded_executor.hpp>
#include <std_msgs/msg/bool.hpp>
#include <ament_index_cpp/get_package_share_directory.hpp>

#include <behaviortree_cpp/bt_factory.h>
#include <behaviortree_cpp/loggers/groot2_publisher.h>
#include <behaviortree_cpp/loggers/bt_cout_logger.h>

#include "s622_bt_manager/dummy_nodes.hpp"
#include "s622_bt_manager/perception_nodes.hpp"

using namespace std::chrono_literals;

class BTExecutor : public rclcpp::Node {
public:
  BTExecutor() : rclcpp::Node("bt_executor") {
    declare_parameter<std::string>("tree_file", "dummy_tree.xml");
    declare_parameter<std::string>("tree_id", "DummyTree");
    declare_parameter<int>("tick_rate_hz", 10);
    declare_parameter<bool>("auto_start", false);
    declare_parameter<int>("groot2_port", 1667);
    declare_parameter<std::string>("yolo_topic", "/yolov8/obb_detections");

    tick_rate_hz_ = get_parameter("tick_rate_hz").as_int();

    trigger_sub_ = create_subscription<std_msgs::msg::Bool>(
      "/bt_trigger", 10,
      std::bind(&BTExecutor::onTrigger, this, std::placeholders::_1));
  }

  void init() {
    ros_ctx_ = std::make_shared<s622_bt::RosContext>();
    ros_ctx_->node = shared_from_this();
    ros_ctx_->perception_cb_group =
      create_callback_group(rclcpp::CallbackGroupType::Reentrant);

    auto yolo_topic = get_parameter("yolo_topic").as_string();
    ros_ctx_->initYoloSubscription(yolo_topic);
    RCLCPP_INFO(get_logger(), "Subscribed to YOLO topic: %s", yolo_topic.c_str());

    s622_bt::registerDummyNodes(factory_);
    s622_bt::registerPerceptionNodes(factory_, ros_ctx_);

    auto pkg_share = ament_index_cpp::get_package_share_directory("s622_bt_manager");
    auto tree_file = get_parameter("tree_file").as_string();
    auto tree_path = pkg_share + "/behavior_trees/" + tree_file;
    RCLCPP_INFO(get_logger(), "Loading tree: %s", tree_path.c_str());
    factory_.registerBehaviorTreeFromFile(tree_path);

    if (get_parameter("auto_start").as_bool()) {
      auto_start_timer_ = create_wall_timer(500ms, [this]() {
        auto_start_timer_->cancel();
        runTree();
      });
    } else {
      RCLCPP_INFO(get_logger(), "Waiting for trigger on /bt_trigger ...");
    }
  }

private:
  void onTrigger(const std_msgs::msg::Bool::SharedPtr msg) {
    if (!msg->data) return;
    if (running_) {
      RCLCPP_WARN(get_logger(), "Tree already running, ignoring trigger");
      return;
    }
    runTree();
  }

  void runTree() {
    running_ = true;
    auto tree_id = get_parameter("tree_id").as_string();
    auto groot_port = get_parameter("groot2_port").as_int();

    RCLCPP_INFO(get_logger(), "=== Starting BT [%s] ===", tree_id.c_str());

    auto tree = factory_.createTree(tree_id);
    BT::Groot2Publisher groot_pub(tree, groot_port);
    BT::StdCoutLogger cout_logger(tree);

    auto status = BT::NodeStatus::RUNNING;
    rclcpp::Rate rate(tick_rate_hz_);
    while (rclcpp::ok() && status == BT::NodeStatus::RUNNING) {
      status = tree.tickOnce();
      rate.sleep();
    }

    RCLCPP_INFO(get_logger(), "=== BT finished: %s ===", BT::toStr(status).c_str());
    running_ = false;
  }

  BT::BehaviorTreeFactory factory_;
  rclcpp::Subscription<std_msgs::msg::Bool>::SharedPtr trigger_sub_;
  rclcpp::TimerBase::SharedPtr auto_start_timer_;
  s622_bt::RosContextPtr ros_ctx_;
  int tick_rate_hz_;
  bool running_ = false;
};

int main(int argc, char** argv) {
  rclcpp::init(argc, argv);
  auto node = std::make_shared<BTExecutor>();
  node->init();

  // 至少 2 个线程：一个跑 BT 循环，一个跑感知回调
  rclcpp::executors::MultiThreadedExecutor executor(rclcpp::ExecutorOptions(), 2);
  executor.add_node(node);
  executor.spin();
  rclcpp::shutdown();
  return 0;
}
```

## 4. 更新 `CMakeLists.txt`

```cmake
cmake_minimum_required(VERSION 3.8)
project(s622_bt_manager)

if(NOT CMAKE_CXX_STANDARD)
  set(CMAKE_CXX_STANDARD 17)
endif()

if(CMAKE_COMPILER_IS_GNUCXX OR CMAKE_CXX_COMPILER_ID MATCHES "Clang")
  add_compile_options(-Wall -Wextra -Wpedantic)
endif()

find_package(ament_cmake REQUIRED)
find_package(rclcpp REQUIRED)
find_package(std_msgs REQUIRED)
find_package(ament_index_cpp REQUIRED)
find_package(behaviortree_cpp REQUIRED)
find_package(yolov8_obb_msgs REQUIRED)

add_executable(bt_executor_node
  src/bt_executor_node.cpp
  src/dummy_nodes.cpp
  src/perception_nodes.cpp
)
target_include_directories(bt_executor_node PRIVATE include)
ament_target_dependencies(bt_executor_node
  rclcpp
  std_msgs
  ament_index_cpp
  behaviortree_cpp
  yolov8_obb_msgs
)

install(TARGETS bt_executor_node DESTINATION lib/${PROJECT_NAME})
install(DIRECTORY behavior_trees launch DESTINATION share/${PROJECT_NAME})

ament_package()
```

## 5. 更新 `package.xml`

加一行：

```xml
<depend>yolov8_obb_msgs</depend>
```

## 6. 新建 `behavior_trees/detect_only_tree.xml`

```xml
<root BTCPP_format="4">
  <BehaviorTree ID="DetectOnlyTree">
    <Sequence>
      <CheckSystemReady/>
      <!-- 给订阅一点 warmup 时间，避免触发瞬间 cache 还是空 -->
      <Wait sec="0.5"/>
      <RetryUntilSuccessful num_attempts="10">
        <DetectObject object_name="cube"
                      min_confidence="0.05"
                      max_age_sec="1.0"
                      output_u="{u}"
                      output_v="{v}"
                      output_yaw="{yaw}"
                      output_confidence="{conf}"/>
      </RetryUntilSuccessful>
      <PrintMessage msg="Detection success"/>
    </Sequence>
  </BehaviorTree>
</root>
```

## 7. 验证步骤

```bash
cd ~/my_S622
colcon build --merge-install --symlink-install --packages-select s622_bt_manager
source install/setup.bash
```

### 测试 A：YOLO 不跑 → 应该失败

```bash
# 终端 1
ros2 launch s622_bt_manager single_arm_pick_place_bt.launch.py \
  tree_file:=detect_only_tree.xml tree_id:=DetectOnlyTree

# 终端 2
ros2 topic pub /bt_trigger std_msgs/msg/Bool "data: true" --once
```

预期：DetectObject 重试 10 次都失败，整棵树 FAILURE。

### 测试 B：YOLO 在跑 → 应该立刻成功

```bash
# 起仿真 + YOLO（按现有流程，YOLO 在 conda yolov8 终端）
# 等 cube 进画面，确认 ros2 topic echo /yolov8/obb_detections 有 cube
# 然后启动 BT 并触发
```

预期：看到类似

```
[DetectObject [cube]: uv=(336.2, 254.1) yaw=0.123 conf=0.78 age=0.05s]
[PrintMessage]: Detection success
=== BT finished: SUCCESS ===
```

## 8. 可能踩的坑

| 现象                                        | 原因                               | 解决                                                                                   |
| ------------------------------------------- | ---------------------------------- | -------------------------------------------------------------------------------------- |
| 编译时找不到 `yolov8_obb_msgs`              | 包没构建或没 source                | `colcon build --packages-up-to s622_bt_manager` 让依赖一起编                           |
| 头文件名报错                                | 路径不对                           | ROS 2 是 `<yolov8_obb_msgs/msg/yolov8_inference.hpp>`（snake_case）                    |
| 永远 FAILURE 即使 YOLO 在跑                 | 时间戳问题（仿真时间 vs 系统时间） | 把 `max_age_sec` 调到 5.0 验证；如确是 sim time，给 BTExecutor 加 `use_sim_time:=true` |
| `shared_from_this()` 抛 bad_weak_ptr        | 在构造函数里调了                   | 必须用 `make_shared` 后调 `init()`，已经这么做了                                       |
| `RetryUntilSuccessful` 内秒重试，看不到日志 | 失败太快                           | 把 `num_attempts` 调高 + 在 RetryUntilSuccessful 内套一个 `Wait sec="0.2"`             |

## 9. 验收清单

* [ ] colcon 通过
* [ ] YOLO 不跑时整棵树 FAILURE，DetectObject 警告"no YOLO message"
* [ ] YOLO 跑且 cube 在画面时整棵树 SUCCESS，日志看到正确的 uv/yaw/conf
* [ ] cube 移出画面后再触发，DetectObject 警告"no 'cube' with conf>=..."后失败
* [ ] 修订计划里的 `dummy_tree.xml` 仍能跑通（回归测试）

---

跑通后告诉我 DetectObject 输出的 `uv/yaw/conf` 实际值大概什么样，特别是 `yaw`——YOLO OBB 输出的 angle 是相对图像 x 轴还是别的约定，这个值会直接影响 1.3 里 `GenerateGraspCandidate` 怎么把它转成 base frame 的 yaw。

# MileStone 1.3 生成 GraspCandidate 并可视化
进入 **Milestone 1.3**。这一步代码量比 1.2 大不少，因为要引入：

* 深度图 + camera_info 订阅
* TF buffer + listener
* 像素→base 反投影
* yaw 4 等价解择优
* PoseArray 可视化发布

⚠️ **架构上有一处必须改**：现在 BT 循环跑在 `onTrigger` 回调里，会阻塞 executor 的一个线程。本里程碑 BT 节点要在 tick 内同步 `lookupTransform`，
需要 TF listener 在另一个线程持续更新。所以把 BT 循环挪到独立 std::thread。

---

## 1. 扩展 `RosContext`

### `include/s622_bt_manager/perception_nodes.hpp`

```cpp
#pragma once

#include <memory>
#include <mutex>
#include <string>

#include <behaviortree_cpp/bt_factory.h>
#include <rclcpp/rclcpp.hpp>
#include <yolov8_obb_msgs/msg/yolov8_inference.hpp>
#include <sensor_msgs/msg/image.hpp>
#include <sensor_msgs/msg/camera_info.hpp>
#include <geometry_msgs/msg/pose_array.hpp>
#include <tf2_ros/buffer.h>
#include <tf2_ros/transform_listener.h>

namespace s622_bt {

struct RosContext {
  rclcpp::Node::SharedPtr node;
  rclcpp::CallbackGroup::SharedPtr perception_cb_group;

  // YOLO
  std::mutex yolo_mutex;
  yolov8_obb_msgs::msg::Yolov8Inference::SharedPtr latest_yolo;
  rclcpp::Time latest_yolo_recv_time;
  rclcpp::Subscription<yolov8_obb_msgs::msg::Yolov8Inference>::SharedPtr yolo_sub;

  // Depth
  std::mutex depth_mutex;
  sensor_msgs::msg::Image::SharedPtr latest_depth;
  rclcpp::Subscription<sensor_msgs::msg::Image>::SharedPtr depth_sub;

  // CameraInfo
  std::mutex caminfo_mutex;
  sensor_msgs::msg::CameraInfo::SharedPtr latest_caminfo;
  rclcpp::Subscription<sensor_msgs::msg::CameraInfo>::SharedPtr caminfo_sub;

  // TF
  std::shared_ptr<tf2_ros::Buffer> tf_buffer;
  std::shared_ptr<tf2_ros::TransformListener> tf_listener;

  // 可视化
  rclcpp::Publisher<geometry_msgs::msg::PoseArray>::SharedPtr grasp_viz_pub;

  void initYoloSubscription(const std::string& topic);
  void initDepthSubscription(const std::string& topic);
  void initCameraInfoSubscription(const std::string& topic);
  void initTf();
  void initGraspVizPublisher(const std::string& topic);
};

using RosContextPtr = std::shared_ptr<RosContext>;

class DetectObject : public BT::SyncActionNode {
public:
  DetectObject(const std::string& name,
               const BT::NodeConfig& config,
               RosContextPtr ros);
  static BT::PortsList providedPorts();
  BT::NodeStatus tick() override;
private:
  RosContextPtr ros_;
};

void registerPerceptionNodes(BT::BehaviorTreeFactory& factory, RosContextPtr ros);

}  // namespace s622_bt
```

### `src/perception_nodes.cpp` 增量

在你现有的文件里加这几个 init 方法（保持原 `initYoloSubscription` 和 `DetectObject::tick()` 不变）：

```cpp
void RosContext::initDepthSubscription(const std::string& topic) {
  rclcpp::SubscriptionOptions opts;
  opts.callback_group = perception_cb_group;
  depth_sub = node->create_subscription<sensor_msgs::msg::Image>(
    topic, rclcpp::SensorDataQoS(),
    [this](sensor_msgs::msg::Image::SharedPtr msg) {
      std::lock_guard<std::mutex> lock(depth_mutex);
      latest_depth = msg;
    },
    opts);
}

void RosContext::initCameraInfoSubscription(const std::string& topic) {
  rclcpp::SubscriptionOptions opts;
  opts.callback_group = perception_cb_group;
  caminfo_sub = node->create_subscription<sensor_msgs::msg::CameraInfo>(
    topic, rclcpp::SensorDataQoS(),
    [this](sensor_msgs::msg::CameraInfo::SharedPtr msg) {
      std::lock_guard<std::mutex> lock(caminfo_mutex);
      latest_caminfo = msg;
    },
    opts);
}

void RosContext::initTf() {
  tf_buffer = std::make_shared<tf2_ros::Buffer>(node->get_clock());
  tf_listener = std::make_shared<tf2_ros::TransformListener>(*tf_buffer);
}

void RosContext::initGraspVizPublisher(const std::string& topic) {
  grasp_viz_pub = node->create_publisher<geometry_msgs::msg::PoseArray>(topic, 10);
}
```

## 2. 新文件 `include/s622_bt_manager/grasp_nodes.hpp`

```cpp
#pragma once

#include <behaviortree_cpp/bt_factory.h>
#include <geometry_msgs/msg/pose_stamped.hpp>
#include "s622_bt_manager/perception_nodes.hpp"

namespace s622_bt {

class GenerateGraspCandidate : public BT::SyncActionNode {
public:
  GenerateGraspCandidate(const std::string& name,
                         const BT::NodeConfig& config,
                         RosContextPtr ros);
  static BT::PortsList providedPorts();
  BT::NodeStatus tick() override;
private:
  RosContextPtr ros_;
};

void registerGraspNodes(BT::BehaviorTreeFactory& factory, RosContextPtr ros);

}  // namespace s622_bt
```

## 3. 新文件 `src/grasp_nodes.cpp`

```cpp
#include "s622_bt_manager/grasp_nodes.hpp"

#include <algorithm>
#include <cmath>
#include <vector>

#include <tf2/utils.h>
#include <tf2_geometry_msgs/tf2_geometry_msgs.hpp>
#include <geometry_msgs/msg/pose_array.hpp>
#include <geometry_msgs/msg/point_stamped.hpp>

namespace s622_bt {

namespace {

// 5x5 中值采样深度，单位米
double samplePixelDepth(const sensor_msgs::msg::Image& depth_img,
                        int u, int v, int kernel = 5) {
  if (depth_img.encoding != "32FC1") return std::nan("");
  std::vector<float> samples;
  int half = kernel / 2;
  int w = depth_img.width;
  int h = depth_img.height;
  const float* data = reinterpret_cast<const float*>(depth_img.data.data());
  for (int dy = -half; dy <= half; ++dy) {
    for (int dx = -half; dx <= half; ++dx) {
      int x = u + dx, y = v + dy;
      if (x < 0 || x >= w || y < 0 || y >= h) continue;
      float z = data[y * w + x];
      if (std::isfinite(z) && z > 0.01f && z < 5.0f) samples.push_back(z);
    }
  }
  if (samples.empty()) return std::nan("");
  std::nth_element(samples.begin(),
                   samples.begin() + samples.size() / 2,
                   samples.end());
  return samples[samples.size() / 2];
}

double normalizeAngle(double a) {
  while (a > M_PI) a -= 2 * M_PI;
  while (a < -M_PI) a += 2 * M_PI;
  return a;
}

// cube 4 个等价 yaw 中选离 current_yaw 最近的
double selectBestYaw(double obj_yaw, double current_yaw) {
  double best = normalizeAngle(obj_yaw);
  double best_err = std::abs(normalizeAngle(best - current_yaw));
  for (int k = 1; k < 4; ++k) {
    double cand = normalizeAngle(obj_yaw + k * M_PI / 2.0);
    double err = std::abs(normalizeAngle(cand - current_yaw));
    if (err < best_err) { best_err = err; best = cand; }
  }
  return best;
}

geometry_msgs::msg::Pose makePose(double x, double y, double z,
                                  double roll, double pitch, double yaw) {
  geometry_msgs::msg::Pose p;
  p.position.x = x; p.position.y = y; p.position.z = z;
  tf2::Quaternion q;
  q.setRPY(roll, pitch, yaw);
  p.orientation = tf2::toMsg(q);
  return p;
}

}  // namespace

GenerateGraspCandidate::GenerateGraspCandidate(const std::string& name,
                                               const BT::NodeConfig& config,
                                               RosContextPtr ros)
  : BT::SyncActionNode(name, config), ros_(std::move(ros)) {}

BT::PortsList GenerateGraspCandidate::providedPorts() {
  return {
    BT::InputPort<double>("u"),
    BT::InputPort<double>("v"),
    BT::InputPort<double>("object_yaw"),
    BT::InputPort<double>("table_z", 0.0, "table z in base frame"),
    BT::InputPort<double>("grasp_height_above_table", 0.030, ""),
    BT::InputPort<double>("pregrasp_height_above_table", 0.16, ""),
    BT::InputPort<double>("pregrasp_camera_offset", 0.03, ""),
    BT::InputPort<std::string>("base_frame", "base_link", ""),
    BT::InputPort<std::string>("ee_frame", "grasp_frame", ""),
    BT::InputPort<double>("camera_x_in_base", 0.3825, ""),
    BT::InputPort<double>("camera_y_in_base", 0.4838, ""),
    BT::OutputPort<geometry_msgs::msg::PoseStamped>("tcp_grasp_pose"),
    BT::OutputPort<geometry_msgs::msg::PoseStamped>("tcp_pregrasp_pose"),
    BT::OutputPort<double>("grasp_yaw"),
  };
}

BT::NodeStatus GenerateGraspCandidate::tick() {
  double u, v, obj_yaw;
  if (!getInput("u", u) || !getInput("v", v) || !getInput("object_yaw", obj_yaw)) {
    RCLCPP_ERROR(ros_->node->get_logger(), "GenerateGraspCandidate: missing inputs");
    return BT::NodeStatus::FAILURE;
  }
  double table_z, grasp_dz, pregrasp_dz, cam_offset;
  double cam_x_base, cam_y_base;
  std::string base_frame, ee_frame;
  getInput("table_z", table_z);
  getInput("grasp_height_above_table", grasp_dz);
  getInput("pregrasp_height_above_table", pregrasp_dz);
  getInput("pregrasp_camera_offset", cam_offset);
  getInput("base_frame", base_frame);
  getInput("ee_frame", ee_frame);
  getInput("camera_x_in_base", cam_x_base);
  getInput("camera_y_in_base", cam_y_base);

  // 1) camera_info
  sensor_msgs::msg::CameraInfo::SharedPtr caminfo;
  { std::lock_guard<std::mutex> lk(ros_->caminfo_mutex); caminfo = ros_->latest_caminfo; }
  if (!caminfo) {
    RCLCPP_WARN(ros_->node->get_logger(), "no camera_info yet");
    return BT::NodeStatus::FAILURE;
  }
  double fx = caminfo->k[0], fy = caminfo->k[4];
  double cx = caminfo->k[2], cy = caminfo->k[5];
  std::string camera_frame = caminfo->header.frame_id;
  if (camera_frame.empty()) camera_frame = "camera_color_optical_frame";

  // 2) depth
  sensor_msgs::msg::Image::SharedPtr depth;
  { std::lock_guard<std::mutex> lk(ros_->depth_mutex); depth = ros_->latest_depth; }
  if (!depth) {
    RCLCPP_WARN(ros_->node->get_logger(), "no depth yet");
    return BT::NodeStatus::FAILURE;
  }
  double z_cam = samplePixelDepth(*depth, int(std::round(u)), int(std::round(v)));
  if (!std::isfinite(z_cam) || z_cam <= 0) {
    RCLCPP_WARN(ros_->node->get_logger(), "invalid depth at (%.1f,%.1f)", u, v);
    return BT::NodeStatus::FAILURE;
  }

  // 3) pixel → camera frame
  double x_cam = (u - cx) * z_cam / fx;
  double y_cam = (v - cy) * z_cam / fy;

  // 4) camera → base
  geometry_msgs::msg::PointStamped p_cam, p_base;
  p_cam.header.frame_id = camera_frame;
  p_cam.header.stamp = depth->header.stamp;
  p_cam.point.x = x_cam; p_cam.point.y = y_cam; p_cam.point.z = z_cam;
  try {
    p_base = ros_->tf_buffer->transform(p_cam, base_frame,
                                         tf2::durationFromSec(0.2));
  } catch (const tf2::TransformException& e) {
    RCLCPP_WARN(ros_->node->get_logger(), "TF %s->%s failed: %s",
                camera_frame.c_str(), base_frame.c_str(), e.what());
    return BT::NodeStatus::FAILURE;
  }
  double obj_x = p_base.point.x, obj_y = p_base.point.y;

  // 5) 当前 EE yaw
  double current_ee_yaw = 0.0;
  try {
    auto tf = ros_->tf_buffer->lookupTransform(base_frame, ee_frame,
                                                tf2::TimePointZero,
                                                tf2::durationFromSec(0.2));
    tf2::Quaternion q;
    tf2::fromMsg(tf.transform.rotation, q);
    double r, p, y; tf2::Matrix3x3(q).getRPY(r, p, y);
    current_ee_yaw = y;
  } catch (const tf2::TransformException& e) {
    RCLCPP_WARN(ros_->node->get_logger(),
                "TF %s->%s failed, default ee_yaw=0: %s",
                base_frame.c_str(), ee_frame.c_str(), e.what());
  }

  double best_yaw = selectBestYaw(obj_yaw, current_ee_yaw);

  // 6) grasp pose（z 用配置，不用反投影）
  geometry_msgs::msg::PoseStamped grasp;
  grasp.header.frame_id = base_frame;
  grasp.header.stamp = ros_->node->now();
  grasp.pose = makePose(obj_x, obj_y, table_z + grasp_dz, M_PI, 0.0, best_yaw);

  // 7) pregrasp：XY 沿"远离相机"方向偏移
  double dx = obj_x - cam_x_base, dy = obj_y - cam_y_base;
  double norm = std::hypot(dx, dy);
  double off_x = norm > 1e-6 ? dx / norm * cam_offset : 0.0;
  double off_y = norm > 1e-6 ? dy / norm * cam_offset : 0.0;

  geometry_msgs::msg::PoseStamped pregrasp;
  pregrasp.header.frame_id = base_frame;
  pregrasp.header.stamp = grasp.header.stamp;
  pregrasp.pose = makePose(obj_x + off_x, obj_y + off_y,
                            table_z + pregrasp_dz, M_PI, 0.0, best_yaw);

  setOutput("tcp_grasp_pose", grasp);
  setOutput("tcp_pregrasp_pose", pregrasp);
  setOutput("grasp_yaw", best_yaw);

  RCLCPP_INFO(ros_->node->get_logger(),
              "Grasp: uv=(%.1f,%.1f) z_cam=%.3f -> base=(%.3f,%.3f,%.3f) "
              "yaw=%.3f (obj=%.3f ee=%.3f) pregrasp=(%.3f,%.3f,%.3f)",
              u, v, z_cam, obj_x, obj_y, table_z + grasp_dz,
              best_yaw, obj_yaw, current_ee_yaw,
              pregrasp.pose.position.x, pregrasp.pose.position.y, pregrasp.pose.position.z);

  // 8) 可视化
  if (ros_->grasp_viz_pub) {
    geometry_msgs::msg::PoseArray arr;
    arr.header = pregrasp.header;
    arr.poses.push_back(pregrasp.pose);
    arr.poses.push_back(grasp.pose);
    ros_->grasp_viz_pub->publish(arr);
  }
  return BT::NodeStatus::SUCCESS;
}

void registerGraspNodes(BT::BehaviorTreeFactory& factory, RosContextPtr ros) {
  factory.registerBuilder<GenerateGraspCandidate>(
    "GenerateGraspCandidate",
    [ros](const std::string& name, const BT::NodeConfig& config) {
      return std::make_unique<GenerateGraspCandidate>(name, config, ros);
    });
}

}  // namespace s622_bt
```

## 4. 改 `src/bt_executor_node.cpp`

**关键变更**：BT 循环挪到独立线程。完整版：

```cpp
#include <atomic>
#include <chrono>
#include <memory>
#include <string>
#include <thread>

#include <rclcpp/rclcpp.hpp>
#include <rclcpp/executors/multi_threaded_executor.hpp>
#include <std_msgs/msg/bool.hpp>
#include <ament_index_cpp/get_package_share_directory.hpp>

#include <behaviortree_cpp/bt_factory.h>
#include <behaviortree_cpp/loggers/groot2_publisher.h>
#include <behaviortree_cpp/loggers/bt_cout_logger.h>

#include "s622_bt_manager/dummy_nodes.hpp"
#include "s622_bt_manager/perception_nodes.hpp"
#include "s622_bt_manager/grasp_nodes.hpp"

using namespace std::chrono_literals;

class BTExecutor : public rclcpp::Node {
public:
  BTExecutor() : rclcpp::Node("bt_executor") {
    declare_parameter<std::string>("tree_file", "dummy_tree.xml");
    declare_parameter<std::string>("tree_id", "DummyTree");
    declare_parameter<int>("tick_rate_hz", 10);
    declare_parameter<bool>("auto_start", false);
    declare_parameter<int>("groot2_port", 1667);
    declare_parameter<std::string>("yolo_topic", "/yolov8/obb_detections");
    declare_parameter<std::string>("depth_topic", "/camera/depth/image_rect_raw");
    declare_parameter<std::string>("caminfo_topic", "/camera/color/camera_info");
    declare_parameter<std::string>("grasp_viz_topic", "/grasp_visualization");

    tick_rate_hz_ = get_parameter("tick_rate_hz").as_int();

    trigger_sub_ = create_subscription<std_msgs::msg::Bool>(
      "/bt_trigger", 10,
      std::bind(&BTExecutor::onTrigger, this, std::placeholders::_1));
  }

  ~BTExecutor() {
    stop_request_ = true;
    if (bt_thread_.joinable()) bt_thread_.join();
  }

  void init() {
    ros_ctx_ = std::make_shared<s622_bt::RosContext>();
    ros_ctx_->node = shared_from_this();
    ros_ctx_->perception_cb_group =
      create_callback_group(rclcpp::CallbackGroupType::Reentrant);

    ros_ctx_->initYoloSubscription(get_parameter("yolo_topic").as_string());
    ros_ctx_->initDepthSubscription(get_parameter("depth_topic").as_string());
    ros_ctx_->initCameraInfoSubscription(get_parameter("caminfo_topic").as_string());
    ros_ctx_->initTf();
    ros_ctx_->initGraspVizPublisher(get_parameter("grasp_viz_topic").as_string());

    s622_bt::registerDummyNodes(factory_);
    s622_bt::registerPerceptionNodes(factory_, ros_ctx_);
    s622_bt::registerGraspNodes(factory_, ros_ctx_);

    auto pkg_share = ament_index_cpp::get_package_share_directory("s622_bt_manager");
    auto tree_path = pkg_share + "/behavior_trees/" + get_parameter("tree_file").as_string();
    RCLCPP_INFO(get_logger(), "Loading tree: %s", tree_path.c_str());
    factory_.registerBehaviorTreeFromFile(tree_path);

    if (get_parameter("auto_start").as_bool()) {
      startTreeAsync();
    } else {
      RCLCPP_INFO(get_logger(), "Waiting for trigger on /bt_trigger ...");
    }
  }

private:
  void onTrigger(const std_msgs::msg::Bool::SharedPtr msg) {
    if (!msg->data) return;
    if (running_.load()) {
      RCLCPP_WARN(get_logger(), "Tree already running, ignoring trigger");
      return;
    }
    startTreeAsync();
  }

  void startTreeAsync() {
    if (bt_thread_.joinable()) bt_thread_.join();
    running_ = true;
    bt_thread_ = std::thread(&BTExecutor::runTree, this);
  }

  void runTree() {
    auto tree_id = get_parameter("tree_id").as_string();
    auto groot_port = get_parameter("groot2_port").as_int();

    RCLCPP_INFO(get_logger(), "=== Starting BT [%s] ===", tree_id.c_str());
    auto tree = factory_.createTree(tree_id);
    BT::Groot2Publisher groot_pub(tree, groot_port);
    BT::StdCoutLogger cout_logger(tree);

    auto status = BT::NodeStatus::RUNNING;
    rclcpp::Rate rate(tick_rate_hz_);
    while (rclcpp::ok() && !stop_request_.load() && status == BT::NodeStatus::RUNNING) {
      status = tree.tickOnce();
      rate.sleep();
    }
    RCLCPP_INFO(get_logger(), "=== BT finished: %s ===", BT::toStr(status).c_str());
    running_ = false;
  }

  BT::BehaviorTreeFactory factory_;
  rclcpp::Subscription<std_msgs::msg::Bool>::SharedPtr trigger_sub_;
  s622_bt::RosContextPtr ros_ctx_;
  std::thread bt_thread_;
  std::atomic<bool> running_{false};
  std::atomic<bool> stop_request_{false};
  int tick_rate_hz_;
};

int main(int argc, char** argv) {
  rclcpp::init(argc, argv);
  auto node = std::make_shared<BTExecutor>();
  node->init();
  rclcpp::executors::MultiThreadedExecutor executor(rclcpp::ExecutorOptions(), 3);
  executor.add_node(node);
  executor.spin();
  rclcpp::shutdown();
  return 0;
}
```

## 5. `CMakeLists.txt`

```cmake
cmake_minimum_required(VERSION 3.8)
project(s622_bt_manager)

if(NOT CMAKE_CXX_STANDARD)
  set(CMAKE_CXX_STANDARD 17)
endif()

find_package(ament_cmake REQUIRED)
find_package(rclcpp REQUIRED)
find_package(std_msgs REQUIRED)
find_package(sensor_msgs REQUIRED)
find_package(geometry_msgs REQUIRED)
find_package(ament_index_cpp REQUIRED)
find_package(behaviortree_cpp REQUIRED)
find_package(yolov8_obb_msgs REQUIRED)
find_package(tf2 REQUIRED)
find_package(tf2_ros REQUIRED)
find_package(tf2_geometry_msgs REQUIRED)

add_executable(bt_executor_node
  src/bt_executor_node.cpp
  src/dummy_nodes.cpp
  src/perception_nodes.cpp
  src/grasp_nodes.cpp
)
target_include_directories(bt_executor_node PRIVATE include)
ament_target_dependencies(bt_executor_node
  rclcpp
  std_msgs
  sensor_msgs
  geometry_msgs
  ament_index_cpp
  behaviortree_cpp
  yolov8_obb_msgs
  tf2
  tf2_ros
  tf2_geometry_msgs
)

install(TARGETS bt_executor_node DESTINATION lib/${PROJECT_NAME})
install(DIRECTORY behavior_trees launch DESTINATION share/${PROJECT_NAME})

ament_package()
```

## 6. `package.xml` 加依赖

```xml
<depend>sensor_msgs</depend>
<depend>geometry_msgs</depend>
<depend>tf2</depend>
<depend>tf2_ros</depend>
<depend>tf2_geometry_msgs</depend>
```

## 7. 新行为树 `behavior_trees/detect_and_grasp_tree.xml`

```xml
<root BTCPP_format="4">
  <BehaviorTree ID="DetectAndGraspTree">
    <Sequence>
      <CheckSystemReady/>
      <Wait sec="1.0"/>
      <RetryUntilSuccessful num_attempts="20">
        <DetectObject object_name="cube"
                      min_confidence="0.05"
                      max_age_sec="1.0"
                      output_u="{u}"
                      output_v="{v}"
                      output_yaw="{obj_yaw}"
                      output_confidence="{conf}"/>
      </RetryUntilSuccessful>
      <GenerateGraspCandidate u="{u}"
                              v="{v}"
                              object_yaw="{obj_yaw}"
                              tcp_grasp_pose="{grasp_pose}"
                              tcp_pregrasp_pose="{pregrasp_pose}"
                              grasp_yaw="{grasp_yaw}"/>
      <PrintMessage msg="Grasp candidate generated"/>
    </Sequence>
  </BehaviorTree>
</root>
```

## 8. 验证步骤

### 先确认 topic 名

```bash
ros2 topic list | grep -E "depth|camera_info"
```

预期看到类似：

* `/camera/depth/image_rect_raw`
* `/camera/color/camera_info`

如果名字不一样，启动 BT 时用参数覆盖：

```bash
ros2 launch s622_bt_manager single_arm_pick_place_bt.launch.py \
  tree_file:=detect_and_grasp_tree.xml \
  tree_id:=DetectAndGraspTree \
  --ros-args -p depth_topic:=/your/depth -p caminfo_topic:=/your/info


ros2 launch s622_bt_manager single_arm_pick_place_bt.launch.py \
  tree_file:=detect_and_grasp_tree.xml \
  tree_id:=DetectAndGraspTree \
  --ros-args -p depth_topic:=/camera/color/camera_info
```

### 跑测试

```bash
# 编译
colcon build --merge-install --symlink-install --packages-select s622_bt_manager
source install/setup.bash

# 终端 1：仿真
ros2 launch gz_launch s622_gazebo.launch.py

# 终端 2：YOLO（conda）
# ... 按现有流程

# 终端 3：BT
ros2 launch s622_bt_manager single_arm_pick_place_bt.launch.py \
  tree_file:=detect_and_grasp_tree.xml \
  tree_id:=DetectAndGraspTree

# 终端 4：触发
ros2 topic pub /bt_trigger std_msgs/msg/Bool "data: true" --once
```

### RViz 加可视化

在 RViz2 里：

* Add → By topic → `/grasp_visualization` → PoseArray
* 设置 Fixed Frame = `base_link`

预期看到两个箭头：

* pregrasp（高位，沿"远离相机"方向偏移 3cm）
* grasp（低位，在方块上方 3cm 处）

## 9. 验收清单

* [ ] colcon 通过
* [ ] BT 不再阻塞，触发后能正确启动子线程
* [ ] DetectObject 输出 uv/yaw/conf 正常
* [ ] GenerateGraspCandidate 日志显示合理的 base 坐标（cube 大概在 `(0.35, 0)` 附近，z = 0.030）
* [ ] RViz 中看到两个 pose 箭头：grasp 在低位 z≈0.03，pregrasp 高约 13cm
* [ ] grasp 箭头指向下（roll=π）
* [ ] 手动改 EE 初始姿态后再触发，yaw 等价解选取不同（看日志 obj_yaw 和 best_yaw 的关系）
* [ ] cube 移到 `(0.4, 0.1)` 等不同位置时，pregrasp 沿远离 `(0.3825, 0.4838)` 的方向偏移可见

## 10. 可能踩的坑

| 现象                                                    | 原因                               | 解决                                                                           |
| ------------------------------------------------------- | ---------------------------------- | ------------------------------------------------------------------------------ |
| `no camera_info yet`                                    | topic 名错或没启动                 | `ros2 topic hz /camera/color/camera_info` 确认                                 |
| `no depth yet`                                          | 同上                               | 同上                                                                           |
| `invalid depth at (...)`                                | 仿真深度有空洞或单位非米           | `ros2 topic echo /camera/depth/image_rect_raw --field encoding` 应该是 `32FC1` |
| `TF camera->base failed`                                | TF 还没建立完                      | `Wait sec="1.0"` 已加 buffer，不行就拉长                                       |
| 编译报 `tf2_geometry_msgs/tf2_geometry_msgs.hpp` 找不到 | Humble 上需要 `.hpp`，Foxy 是 `.h` | Humble 没问题，路径写对就行                                                    |
| 编译报 BT.CPP 找不到 `PoseStamped` 作为 BB 类型         | v4 默认支持，注册时不需要做啥      | 如果真出问题，给 `BT::convertFromString` 加特化（一般不需要）                  |
| Groot2 端口冲突                                         | 上一次没退干净                     | `--ros-args -p groot2_port:=1668` 换个端口                                     |

---

跑通后告诉我日志里的 `obj_yaw`、`best_yaw`、`base=(...)` 几个值。**特别是 obj_yaw 这个数值**，0 还是 π/4 还是别的，会决定下一步 yaw 处理需不需要做坐标系转换（YOLO OBB 的 angle 一般是图像坐标系下的，可能需要换成 base frame）。

# 1.3 的 yaw 选择 和 pregrasp XY 偏移


| 步骤                 | visual_servo (Python)                                             | grasp_nodes.cpp (C++)                                                              | 一致？   |
| -------------------- | ----------------------------------------------------------------- | ---------------------------------------------------------------------------------- |
| 像素→3D              | 5x5 中值深度采样 error_estimator.pixel_to_camera()                | 5x5 中值深度采样 samplePixelDepth()                                                | ✅ 相同   |
| camera→base          | TF	tf_buffer.transform()                                          | tf_buffer->transform()                                                             | ✅ 相同   |
| grasp Z              | table_z + grasp_height_above_table                                | table_z + grasp_dz                                                                 | ✅ 相同   |
| grasp XY             | 深度反投影的 xy                                                   | 深度反投影的 xy                                                                    | ✅ 相同   |
| grasp orientation    | roll=π, pitch=0                                                   | roll=π, pitch=0                                                                    | ✅ 相同   |
| pregrasp Z           | table_z + pregrasp_height_above_table                             | table_z + pregrasp_dz                                                              | ✅ 相同   |
| yaw 选择             | _target_yaw_from_obb()：读 OBB 的 w/h 判断长轴/短轴，决定夹持方向 | selectBestYaw()：无视 w/h，从 4 个等价旋转（0°/90°/180°/270°）中选离当前 EE yaw 最 | 近的     | ❌ 不同 |
| pregrasp XY 偏移方向 | 16 个候选方向全部投影到像素，选 image-space 分离度最大的          | 仅沿"远离相机"方向（target→camera 方向），不做像素空间验证                         | ❌ 简化了 |

两个差异：
Yaw：Python 版利用 OBB 的 width/height 判断哪些边对应长轴、夹爪该夹长边还是短边。C++ 版假设方块对称，直接用 4 等价旋转挑最小转角——逻辑更简单但丢失了 OBB 形状信息。

Pregrasp 偏移：Python 版在 image space 里验证 16 个方向，选 pixel separation 最大的。C++ 版直接用远离相机的射线方向（退化为 Python 的 fallback 逻辑）。


不用现在改。这两处都是**有意保留的简化**，原因和判据如下。

## 为什么现在不改

### Yaw 选择

cube 是 4cm 立方体，**4 边几何对称**。理论上 w == h，OBB 输出的 angle 物理意义只剩下 yaw mod 90°。

* visual_servo 的 w/h 长轴判断对 cube 没意义。它那段逻辑是为非正方形物体（cuboid、长条）设计的，cube 上跑结果只会让代码看起来复杂，实际等效于"选 4 等价之一"。
* C++ 简化版做的是同一件事，只是显式声明"4 等价择优"，反而更清晰。

只有当你要抓**非对称物体**时，才需要把 OBB 长边方向真正用起来。那是阶段 2 的事。

### Pregrasp 偏移

你写的几何图是对的。本项目相机几何条件下，"远离相机水平投影"方向**就是**像素空间里大概率不遮挡的方向，原因是相机光轴主要在水平面上有方向性偏移（相机在 Y=0.48 看 Y=0 的目标，主向是 -Y）。

visual_servo 的"16 方向投像素+图像分离度最大"是更**通用**的算法——它在相机几乎正上方俯视、或者目标偏出画面边缘等极端情况下更鲁棒。但当前几何：

```
相机水平偏移 ≈ 0.48m（Y 方向）
目标深度    ≈ 0.93m
俯角        ≈ 58°
```

这种"明显的侧后方俯视"几何下，两种算法的输出方向**几乎一致**。我估计 base 系下偏移向量的差异 < 5°。

## 什么时候要改

给你两个**触发条件**，到那个时候再升级，否则不动：

| 触发条件                                                                   | 升级哪个                                           |
| -------------------------------------------------------------------------- | -------------------------------------------------- |
| 1.5 阶段测试时，VISUAL_ALIGN_XY 启动后 YOLO 检测频繁丢失或 confidence 暴跌 | 把 pregrasp 偏移升级成 visual_servo 的 16 方向择优 |
| 1.5 阶段测试时，EE yaw 频繁出现 > 60° 大转角                               | 检查 obj_yaw 的值域，可能要加入对称性归一化        |
| 阶段 2 抓非正方形物体（长条、cuboid）                                      | yaw 改成 OBB 长边方向（w<h 时 yaw+=π/2）           |
| 相机改装为正上方俯视（俯角 90°）                                           | pregrasp 偏移必须升级，否则方向不定                |

## 现在该做的一件小事

确认一下 `obj_yaw` 的值域。跑一次 1.3 的测试树，看日志里 `obj_yaw=...` 实际取值范围：

* 如果是 **[-π/2, π/2]** 或 **[-π/4, π/4]** —— YOLO 输出已归一化到 cube 等价范围内，`selectBestYaw` 工作正常
* 如果是 **[0, 2π]** —— `normalizeAngle` 会把它拉回 [-π, π]，4 等价仍然能覆盖
* 如果是 **图像坐标系**（y 向下）而不是 base 系 —— 这是个潜在坑，需要做坐标系转换（图像 y 向下导致 yaw 翻号）

把 cube 在 Gazebo 里手动转 30° 和 60°，看 `obj_yaw` 和 `best_yaw` 的变化趋势是不是一致。如果发现 cube 转 30° 但 obj_yaw 跳了一个看似无关的值，那就是 YOLO 输出约定和 base 系不一致，**到 1.5 之前必须修**。

---

**结论：1.3 不改，进入 1.4**。你可以现在跑一次确认 obj_yaw 值域，然后我们开始 Milestone 1.4 (MoveToPose Action)。

# MileStone 1.4 MoveToPose Action
进入 1.4。这一步代码量大，先说一下范围和关键决策：

## 范围

* 在 `s622_bt_manager` 加 `MoveToPose.action` 接口（用 rosidl 生成）
* 新建 `s622_arm_actions` Python 包
  * `ServoLifecycleManager`（单点管理 servo 状态）
  * `MoveItPlanner`（从 visual_servo 复制并去掉 servo 调用）
  * `MoveToPoseServer`（Action server）
* 在 `s622_bt_manager` 加 `MoveToPoseAction` BT 节点（C++ Action client）
* 测试树：检测 → 生成 grasp → 移动到 pregrasp

## 关键决策

1. **接口先放 `s622_bt_manager`**（按修订计划，双臂阶段再抽出来）
2. **`named_pose` 字段在 Action 里**，server 端用 yaml 查表 joint positions（不依赖 SRDF 的 named target）
3. **BT Action client 用 polling 模式**（每次 tick 检查 future 状态），避免阻塞 BT 线程
4. **server 用 MultiThreadedExecutor + ReentrantCallbackGroup**（MoveIt 是阻塞调用）

---

## Step 1：在 `s622_bt_manager` 加 Action 接口

### 1.1 新建 `action/MoveToPose.action`

```
# Goal
geometry_msgs/PoseStamped target_pose
string named_pose
string planner_id
float32 velocity_scale
float32 acceleration_scale
float32 timeout_sec
bool ensure_servo_stopped
---
# Result
bool success
int32 error_code
string error_msg
---
# Feedback
float32 progress
```

### 1.2 更新 `package.xml`

```xml
<?xml version="1.0"?>
<package format="3">
  <name>s622_bt_manager</name>
  <version>0.1.0</version>
  <description>BT.CPP-based task manager for S622 single-arm pick-and-place</description>
  <maintainer email="you@example.com">you</maintainer>
  <license>MIT</license>

  <buildtool_depend>ament_cmake</buildtool_depend>
  <buildtool_depend>rosidl_default_generators</buildtool_depend>

  <depend>rclcpp</depend>
  <depend>rclcpp_action</depend>
  <depend>std_msgs</depend>
  <depend>sensor_msgs</depend>
  <depend>geometry_msgs</depend>
  <depend>action_msgs</depend>
  <depend>ament_index_cpp</depend>
  <depend>behaviortree_cpp</depend>
  <depend>yolov8_obb_msgs</depend>
  <depend>tf2</depend>
  <depend>tf2_ros</depend>
  <depend>tf2_geometry_msgs</depend>

  <exec_depend>rosidl_default_runtime</exec_depend>
  <member_of_group>rosidl_interface_packages</member_of_group>

  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
```

### 1.3 更新 `CMakeLists.txt`

```cmake
cmake_minimum_required(VERSION 3.8)
project(s622_bt_manager)

if(NOT CMAKE_CXX_STANDARD)
  set(CMAKE_CXX_STANDARD 17)
endif()

find_package(ament_cmake REQUIRED)
find_package(rclcpp REQUIRED)
find_package(rclcpp_action REQUIRED)
find_package(std_msgs REQUIRED)
find_package(sensor_msgs REQUIRED)
find_package(geometry_msgs REQUIRED)
find_package(action_msgs REQUIRED)
find_package(ament_index_cpp REQUIRED)
find_package(behaviortree_cpp REQUIRED)
find_package(yolov8_obb_msgs REQUIRED)
find_package(tf2 REQUIRED)
find_package(tf2_ros REQUIRED)
find_package(tf2_geometry_msgs REQUIRED)

find_package(rosidl_default_generators REQUIRED)

rosidl_generate_interfaces(${PROJECT_NAME}
  "action/MoveToPose.action"
  DEPENDENCIES geometry_msgs action_msgs std_msgs
)

add_executable(bt_executor_node
  src/bt_executor_node.cpp
  src/dummy_nodes.cpp
  src/perception_nodes.cpp
  src/grasp_nodes.cpp
  src/motion_nodes.cpp
)
target_include_directories(bt_executor_node PRIVATE include)
ament_target_dependencies(bt_executor_node
  rclcpp rclcpp_action std_msgs sensor_msgs geometry_msgs
  ament_index_cpp behaviortree_cpp yolov8_obb_msgs
  tf2 tf2_ros tf2_geometry_msgs
)

# Link executable against generated interfaces
rosidl_get_typesupport_target(cpp_typesupport_target
  ${PROJECT_NAME} "rosidl_typesupport_cpp")
target_link_libraries(bt_executor_node "${cpp_typesupport_target}")

install(TARGETS bt_executor_node DESTINATION lib/${PROJECT_NAME})
install(DIRECTORY behavior_trees launch DESTINATION share/${PROJECT_NAME})

ament_export_dependencies(rosidl_default_runtime)
ament_package()
```

### 1.4 先验证接口编译

```bash
colcon build --merge-install --symlink-install --packages-select s622_bt_manager
```

之后还没有 `motion_nodes.cpp`，所以会报缺文件——下一步就建它。

---

## Step 2：新建 `s622_arm_actions` Python 包

### 2.1 目录结构

```bash
cd ~/my_S622/src
ros2 pkg create s622_arm_actions --build-type ament_python
```

然后：

```
src/s622_arm_actions/
├── s622_arm_actions/
│   ├── __init__.py
│   ├── servo_lifecycle.py
│   ├── moveit_planner.py
│   └── move_to_pose_server.py
├── launch/
│   └── arm_actions.launch.py
├── config/
│   └── arm_config.yaml
├── resource/
│   └── s622_arm_actions
├── package.xml
├── setup.py
└── setup.cfg
```

### 2.2 `package.xml`

```xml
<?xml version="1.0"?>
<package format="3">
  <name>s622_arm_actions</name>
  <version>0.1.0</version>
  <description>Arm capability action servers (MoveToPose, VisualAlign, Gripper)</description>
  <maintainer email="you@example.com">you</maintainer>
  <license>MIT</license>

  <depend>rclpy</depend>
  <depend>rclpy_action</depend>
  <depend>std_msgs</depend>
  <depend>std_srvs</depend>
  <depend>geometry_msgs</depend>
  <depend>s622_bt_manager</depend>
  <depend>pymoveit2</depend>

  <export>
    <build_type>ament_python</build_type>
  </export>
</package>
```

### 2.3 `setup.py`

```python
from setuptools import setup
import os
from glob import glob

package_name = 's622_arm_actions'

setup(
    name=package_name,
    version='0.1.0',
    packages=[package_name],
    data_files=[
        ('share/ament_index/resource_index/packages',
            ['resource/' + package_name]),
        ('share/' + package_name, ['package.xml']),
        (os.path.join('share', package_name, 'launch'),
            glob('launch/*.launch.py')),
        (os.path.join('share', package_name, 'config'),
            glob('config/*.yaml')),
    ],
    install_requires=['setuptools'],
    zip_safe=True,
    maintainer='you',
    maintainer_email='you@example.com',
    description='Arm capability action servers',
    license='MIT',
    entry_points={
        'console_scripts': [
            'move_to_pose_server = s622_arm_actions.move_to_pose_server:main',
        ],
    },
)
```

### 2.4 `s622_arm_actions/servo_lifecycle.py`

```python
"""单点管理 MoveIt Servo 的 start/stop 状态。

避免多个 Action server 重复调用 /servo_node/start_servo 或 /stop_servo。
所有 servo 状态变更必须通过这个类。
"""
import threading
import time
from typing import Optional

from rclpy.node import Node
from rclpy.callback_groups import CallbackGroup, ReentrantCallbackGroup
from std_srvs.srv import Trigger


class ServoLifecycleManager:
    STOPPED = 'STOPPED'
    RUNNING = 'RUNNING'

    def __init__(self, node: Node, callback_group: Optional[CallbackGroup] = None):
        self.node = node
        cb = callback_group or ReentrantCallbackGroup()
        self.state = self.STOPPED
        self._lock = threading.Lock()
        self.start_cli = node.create_client(
            Trigger, '/servo_node/start_servo', callback_group=cb)
        self.stop_cli = node.create_client(
            Trigger, '/servo_node/stop_servo', callback_group=cb)

    def _call(self, client, name: str, timeout: float = 3.0) -> bool:
        if not client.wait_for_service(timeout_sec=2.0):
            self.node.get_logger().warning(f'{name} service unavailable')
            return False
        future = client.call_async(Trigger.Request())
        deadline = time.time() + timeout
        while time.time() < deadline:
            if future.done():
                break
            time.sleep(0.02)
        if not future.done():
            self.node.get_logger().warning(f'{name}: timeout')
            return False
        res = future.result()
        ok = res is not None and res.success
        msg = res.message if res else 'no result'
        self.node.get_logger().info(f'{name}: {"ok" if ok else "failed"} ({msg})')
        return ok

    def start_servo(self) -> bool:
        with self._lock:
            if self.state == self.RUNNING:
                return True
            ok = self._call(self.start_cli, 'start_servo')
            if ok:
                self.state = self.RUNNING
            return ok

    def stop_servo(self) -> bool:
        with self._lock:
            if self.state == self.STOPPED:
                return True
            ok = self._call(self.stop_cli, 'stop_servo')
            if ok:
                self.state = self.STOPPED
            return ok

    def force_stop(self) -> bool:
        """无条件 stop，用于 Recovery 路径。"""
        with self._lock:
            ok = self._call(self.stop_cli, 'force_stop_servo')
            self.state = self.STOPPED
            return ok
```

### 2.5 `s622_arm_actions/moveit_planner.py`

去掉了 servo 调用，只负责 MoveIt 规划执行：

```python
"""MoveIt 规划执行封装（不再管 servo，servo 由 ServoLifecycleManager 管）。"""
from typing import List, Optional

import numpy as np
from rclpy.node import Node
from rclpy.callback_groups import CallbackGroup, ReentrantCallbackGroup

from pymoveit2 import MoveIt2


class MoveItPlanner:
    def __init__(
        self,
        node: Node,
        joint_names: List[str],
        base_link: str,
        end_effector: str,
        group_name: str,
        callback_group: Optional[CallbackGroup] = None,
        max_vel: float = 0.2,
        max_acc: float = 0.2,
    ):
        self.node = node
        self.joint_names = joint_names
        cb = callback_group or ReentrantCallbackGroup()

        self.moveit2 = MoveIt2(
            node=node,
            joint_names=joint_names,
            base_link_name=base_link,
            end_effector_name=end_effector,
            group_name=group_name,
            callback_group=cb,
        )
        self.moveit2.max_velocity = max_vel
        self.moveit2.max_acceleration = max_acc

    def set_speed(self, vel: float, acc: float):
        self.moveit2.max_velocity = float(vel)
        self.moveit2.max_acceleration = float(acc)

    def plan_to_pose(
        self,
        position,
        quat_xyzw,
        cartesian: bool = False,
    ) -> bool:
        """规划执行到指定位姿。"""
        self.node.get_logger().info(
            f'plan_to_pose: pos=({position[0]:.3f},{position[1]:.3f},{position[2]:.3f}) '
            f'quat=({quat_xyzw[0]:.3f},{quat_xyzw[1]:.3f},{quat_xyzw[2]:.3f},{quat_xyzw[3]:.3f})')
        self.moveit2.move_to_pose(
            position=list(position),
            quat_xyzw=list(quat_xyzw),
            cartesian=cartesian,
        )
        return self.moveit2.wait_until_executed()

    def plan_to_joint_positions(self, positions: List[float]) -> bool:
        """规划执行到指定关节位置。"""
        if len(positions) != len(self.joint_names):
            self.node.get_logger().error(
                f'joint_positions length mismatch: {len(positions)} vs {len(self.joint_names)}')
            return False
        self.node.get_logger().info(f'plan_to_joint_positions: {positions}')
        self.moveit2.move_to_configuration(positions)
        return self.moveit2.wait_until_executed()
```

### 2.6 `s622_arm_actions/move_to_pose_server.py`

```python
#!/usr/bin/env python3
"""MoveToPose Action server。

支持两种 goal：
  - target_pose: 位姿目标
  - named_pose:  通过 yaml 配置的命名关节配置（如 "home"）
"""
import rclpy
from rclpy.node import Node
from rclpy.action import ActionServer, GoalResponse, CancelResponse
from rclpy.callback_groups import ReentrantCallbackGroup
from rclpy.executors import MultiThreadedExecutor

from s622_bt_manager.action import MoveToPose

from s622_arm_actions.servo_lifecycle import ServoLifecycleManager
from s622_arm_actions.moveit_planner import MoveItPlanner


class MoveToPoseServer(Node):
    def __init__(self):
        super().__init__('move_to_pose_server')

        # ---- 参数 ----
        self.declare_parameter('joint_names',
            ['j1', 'j2', 'j3', 'j4', 'j5', 'j6'])
        self.declare_parameter('base_link', 'base_link')
        self.declare_parameter('end_effector', 'grasp_frame')
        self.declare_parameter('group_name', 'arm_group')
        self.declare_parameter('default_velocity_scale', 0.2)
        self.declare_parameter('default_acceleration_scale', 0.2)

        # named poses (joint positions)
        self.declare_parameter('named_poses.home',
            [0.0, -1.2, 1.5, 0.0, 1.2, 0.0])
        self.declare_parameter('named_poses.safe',
            [0.0, -1.2, 1.5, 0.0, 1.2, 0.0])

        joint_names = list(self.get_parameter('joint_names').value)
        base_link = self.get_parameter('base_link').value
        end_effector = self.get_parameter('end_effector').value
        group_name = self.get_parameter('group_name').value
        self._default_v = self.get_parameter('default_velocity_scale').value
        self._default_a = self.get_parameter('default_acceleration_scale').value

        self._named_poses = {
            'home': list(self.get_parameter('named_poses.home').value),
            'safe': list(self.get_parameter('named_poses.safe').value),
        }

        cb = ReentrantCallbackGroup()
        self.servo_lc = ServoLifecycleManager(self, callback_group=cb)
        self.planner = MoveItPlanner(
            node=self,
            joint_names=joint_names,
            base_link=base_link,
            end_effector=end_effector,
            group_name=group_name,
            callback_group=cb,
            max_vel=self._default_v,
            max_acc=self._default_a,
        )

        self._action_server = ActionServer(
            self,
            MoveToPose,
            'move_to_pose',
            execute_callback=self._execute,
            goal_callback=lambda goal: GoalResponse.ACCEPT,
            cancel_callback=lambda gh: CancelResponse.ACCEPT,
            callback_group=cb,
        )
        self.get_logger().info(
            f'move_to_pose ready: group={group_name}, ee={end_effector}, '
            f'base={base_link}, joints={joint_names}')

    def _execute(self, goal_handle):
        goal = goal_handle.request
        result = MoveToPose.Result()

        # 1) ensure servo stopped
        if goal.ensure_servo_stopped:
            if not self.servo_lc.stop_servo():
                self.get_logger().warning(
                    'stop_servo failed; continuing (servo may not be running)')

        # 2) set speed
        v = goal.velocity_scale if goal.velocity_scale > 0 else self._default_v
        a = goal.acceleration_scale if goal.acceleration_scale > 0 else self._default_a
        self.planner.set_speed(v, a)

        # 3) plan + execute
        try:
            if goal.named_pose:
                if goal.named_pose not in self._named_poses:
                    msg = f'unknown named_pose: {goal.named_pose}'
                    self.get_logger().error(msg)
                    goal_handle.abort()
                    result.success = False
                    result.error_msg = msg
                    return result
                self.get_logger().info(f'going to named pose: {goal.named_pose}')
                ok = self.planner.plan_to_joint_positions(
                    self._named_poses[goal.named_pose])
            else:
                p = goal.target_pose.pose
                ok = self.planner.plan_to_pose(
                    position=[p.position.x, p.position.y, p.position.z],
                    quat_xyzw=[p.orientation.x, p.orientation.y,
                               p.orientation.z, p.orientation.w],
                    cartesian=False,
                )
        except Exception as e:
            self.get_logger().error(f'plan/execute exception: {e}')
            goal_handle.abort()
            result.success = False
            result.error_msg = str(e)
            return result

        if ok:
            goal_handle.succeed()
            result.success = True
            result.error_msg = ''
        else:
            goal_handle.abort()
            result.success = False
            result.error_msg = 'planning or execution failed'
        return result


def main():
    rclpy.init()
    node = MoveToPoseServer()
    executor = MultiThreadedExecutor(num_threads=4)
    executor.add_node(node)
    try:
        executor.spin()
    finally:
        node.destroy_node()
        rclpy.shutdown()


if __name__ == '__main__':
    main()
```

### 2.7 `config/arm_config.yaml`

```yaml
move_to_pose_server:
  ros__parameters:
    joint_names: ['j1', 'j2', 'j3', 'j4', 'j5', 'j6']
    base_link: 'base_link'
    end_effector: 'grasp_frame'
    group_name: 'arm_group'   # ⚠️ 改成你的 SRDF 里的实际 group_name
    default_velocity_scale: 0.2
    default_acceleration_scale: 0.2
    named_poses:
      home: [0.0, -1.2, 1.5, 0.0, 1.2, 0.0]   # ⚠️ 改成你机器人的实际 home
      safe: [0.0, -1.2, 1.5, 0.0, 1.2, 0.0]
```

⚠️ **`group_name` 必须改成你 SRDF 里的实际名字**（可能是 `arm_group` / `manipulator` / `arm` 等）。可以用：

```bash
ros2 param get /move_group robot_description_semantic | head -50
# 或者
cat ~/my_S622/src/s622_moveit_config/config/*.srdf | grep "<group "
```

`named_poses.home` 也要改成你实际的 home 关节位置——从 SRDF 抄。

### 2.8 `launch/arm_actions.launch.py`

```python
import os
from launch import LaunchDescription
from launch_ros.actions import Node
from ament_index_python.packages import get_package_share_directory


def generate_launch_description():
    config = os.path.join(
        get_package_share_directory('s622_arm_actions'),
        'config', 'arm_config.yaml')

    return LaunchDescription([
        Node(
            package='s622_arm_actions',
            executable='move_to_pose_server',
            name='move_to_pose_server',
            output='screen',
            parameters=[config, {'use_sim_time': True}],
        ),
    ])
```

---

## Step 3：BT 端 `MoveToPoseAction` Action client

### 3.1 `include/s622_bt_manager/motion_nodes.hpp`

```cpp
#pragma once

#include <atomic>
#include <future>
#include <memory>
#include <string>

#include <behaviortree_cpp/action_node.h>
#include <rclcpp/rclcpp.hpp>
#include <rclcpp_action/rclcpp_action.hpp>
#include <geometry_msgs/msg/pose_stamped.hpp>

#include "s622_bt_manager/action/move_to_pose.hpp"

namespace s622_bt {

class MoveToPoseAction : public BT::StatefulActionNode {
public:
  using ActionT = s622_bt_manager::action::MoveToPose;
  using GoalHandle = rclcpp_action::ClientGoalHandle<ActionT>;

  MoveToPoseAction(const std::string& name,
                   const BT::NodeConfig& config,
                   rclcpp::Node::SharedPtr node);

  static BT::PortsList providedPorts() {
    return {
      BT::InputPort<geometry_msgs::msg::PoseStamped>("target_pose"),
      BT::InputPort<std::string>("named_pose", "", "if non-empty, use named pose"),
      BT::InputPort<float>("velocity_scale", 0.2f, ""),
      BT::InputPort<float>("acceleration_scale", 0.2f, ""),
      BT::InputPort<float>("timeout_sec", 20.0f, ""),
      BT::InputPort<bool>("ensure_servo_stopped", true, ""),
    };
  }

  BT::NodeStatus onStart() override;
  BT::NodeStatus onRunning() override;
  void onHalted() override;

private:
  rclcpp::Node::SharedPtr node_;
  rclcpp_action::Client<ActionT>::SharedPtr client_;

  std::shared_future<GoalHandle::SharedPtr> goal_handle_future_;
  GoalHandle::SharedPtr goal_handle_;
  std::shared_future<GoalHandle::WrappedResult> result_future_;

  bool result_received_ = false;
  GoalHandle::WrappedResult last_result_;

  rclcpp::Time start_time_;
  float timeout_sec_ = 20.0f;
};

void registerMotionNodes(BT::BehaviorTreeFactory& factory,
                         rclcpp::Node::SharedPtr node);

}  // namespace s622_bt
```

### 3.2 `src/motion_nodes.cpp`

```cpp
#include "s622_bt_manager/motion_nodes.hpp"

namespace s622_bt {

MoveToPoseAction::MoveToPoseAction(const std::string& name,
                                   const BT::NodeConfig& config,
                                   rclcpp::Node::SharedPtr node)
  : BT::StatefulActionNode(name, config), node_(std::move(node)) {
  client_ = rclcpp_action::create_client<ActionT>(node_, "move_to_pose");
}

BT::NodeStatus MoveToPoseAction::onStart() {
  geometry_msgs::msg::PoseStamped target;
  std::string named_pose;
  float v_scale = 0.2f, a_scale = 0.2f, timeout = 20.0f;
  bool stop_servo = true;

  // target_pose 仅在 named_pose 为空时必填
  getInput("named_pose", named_pose);
  if (named_pose.empty()) {
    if (!getInput("target_pose", target)) {
      RCLCPP_ERROR(node_->get_logger(),
                   "MoveToPose: target_pose required when named_pose empty");
      return BT::NodeStatus::FAILURE;
    }
  }
  getInput("velocity_scale", v_scale);
  getInput("acceleration_scale", a_scale);
  getInput("timeout_sec", timeout);
  getInput("ensure_servo_stopped", stop_servo);
  timeout_sec_ = timeout;

  if (!client_->wait_for_action_server(std::chrono::seconds(3))) {
    RCLCPP_ERROR(node_->get_logger(),
                 "move_to_pose action server not available");
    return BT::NodeStatus::FAILURE;
  }

  ActionT::Goal goal_msg;
  goal_msg.target_pose = target;
  goal_msg.named_pose = named_pose;
  goal_msg.velocity_scale = v_scale;
  goal_msg.acceleration_scale = a_scale;
  goal_msg.timeout_sec = timeout;
  goal_msg.ensure_servo_stopped = stop_servo;

  RCLCPP_INFO(node_->get_logger(),
              "MoveToPose: sending goal (named=%s, v=%.2f, a=%.2f, to=%.1fs)",
              named_pose.empty() ? "<pose>" : named_pose.c_str(),
              v_scale, a_scale, timeout);

  rclcpp_action::Client<ActionT>::SendGoalOptions opts;
  goal_handle_future_ = client_->async_send_goal(goal_msg, opts);

  goal_handle_.reset();
  result_received_ = false;
  start_time_ = node_->now();
  return BT::NodeStatus::RUNNING;
}

BT::NodeStatus MoveToPoseAction::onRunning() {
  // 超时
  if ((node_->now() - start_time_).seconds() > timeout_sec_) {
    RCLCPP_ERROR(node_->get_logger(),
                 "MoveToPose: timeout after %.1fs", timeout_sec_);
    onHalted();
    return BT::NodeStatus::FAILURE;
  }

  // 等待 goal handle
  if (!goal_handle_) {
    if (goal_handle_future_.wait_for(std::chrono::seconds(0))
        == std::future_status::ready) {
      goal_handle_ = goal_handle_future_.get();
      if (!goal_handle_) {
        RCLCPP_ERROR(node_->get_logger(), "MoveToPose: goal rejected");
        return BT::NodeStatus::FAILURE;
      }
      result_future_ = client_->async_get_result(goal_handle_);
      RCLCPP_INFO(node_->get_logger(), "MoveToPose: goal accepted");
    }
    return BT::NodeStatus::RUNNING;
  }

  // 等待结果
  if (result_future_.wait_for(std::chrono::seconds(0))
      == std::future_status::ready) {
    auto wrapped = result_future_.get();
    if (wrapped.code == rclcpp_action::ResultCode::SUCCEEDED
        && wrapped.result && wrapped.result->success) {
      RCLCPP_INFO(node_->get_logger(), "MoveToPose: SUCCESS");
      return BT::NodeStatus::SUCCESS;
    } else {
      std::string err = wrapped.result ? wrapped.result->error_msg : "no result";
      RCLCPP_ERROR(node_->get_logger(),
                   "MoveToPose: FAILED (code=%d, msg=%s)",
                   static_cast<int>(wrapped.code), err.c_str());
      return BT::NodeStatus::FAILURE;
    }
  }
  return BT::NodeStatus::RUNNING;
}

void MoveToPoseAction::onHalted() {
  if (goal_handle_) {
    RCLCPP_WARN(node_->get_logger(), "MoveToPose: halted, cancelling goal");
    client_->async_cancel_goal(goal_handle_);
  }
}

void registerMotionNodes(BT::BehaviorTreeFactory& factory,
                         rclcpp::Node::SharedPtr node) {
  factory.registerBuilder<MoveToPoseAction>(
    "MoveToPose",
    [node](const std::string& name, const BT::NodeConfig& config) {
      return std::make_unique<MoveToPoseAction>(name, config, node);
    });
}

}  // namespace s622_bt
```

### 3.3 在 `bt_executor_node.cpp` 注册

在 `init()` 末尾加：

```cpp
#include "s622_bt_manager/motion_nodes.hpp"
// ...
s622_bt::registerMotionNodes(factory_, shared_from_this());
```

---

## Step 4：测试树

### 4.1 `behavior_trees/detect_grasp_move_tree.xml`

```xml
<root BTCPP_format="4">
  <BehaviorTree ID="DetectGraspMoveTree">
    <Sequence>
      <CheckSystemReady/>
      <Wait sec="1.0"/>

      <!-- 先回 home 确保初始姿态可重复 -->
      <MoveToPose named_pose="home"
                  velocity_scale="0.3"
                  acceleration_scale="0.3"
                  timeout_sec="30.0"
                  ensure_servo_stopped="true"/>

      <RetryUntilSuccessful num_attempts="10">
        <DetectObject object_name="cube"
                      min_confidence="0.05"
                      max_age_sec="1.0"
                      output_u="{u}"
                      output_v="{v}"
                      output_yaw="{obj_yaw}"
                      output_confidence="{conf}"/>
      </RetryUntilSuccessful>

      <GenerateGraspCandidate u="{u}"
                              v="{v}"
                              object_yaw="{obj_yaw}"
                              tcp_grasp_pose="{grasp_pose}"
                              tcp_pregrasp_pose="{pregrasp_pose}"
                              grasp_yaw="{grasp_yaw}"/>

      <MoveToPose target_pose="{pregrasp_pose}"
                  velocity_scale="0.2"
                  acceleration_scale="0.2"
                  timeout_sec="30.0"
                  ensure_servo_stopped="true"/>

      <PrintMessage msg="Pregrasp reached"/>
    </Sequence>
  </BehaviorTree>
</root>
```

---

## Step 5：验证步骤

### 5.1 编译

```bash
cd ~/my_S622
rm -rf build install
colcon build --merge-install --symlink-install \
  --cmake-args "-DPython3_EXECUTABLE=$(which python3)"
source install/setup.bash
```

期望：两个包都能编出来。如果 `s622_arm_actions` 报找不到 `s622_bt_manager.action.MoveToPose`，那是构建顺序问题——`s622_bt_manager` 必须先编完。`colcon` 应该按 `package.xml` 的依赖自动排序。

### 5.2 分阶段验证

#### 验证 A：Action server 单独启动

```bash
# 终端 1：仿真
ros2 launch gz_launch s622_gazebo.launch.py

# 终端 2：Arm Actions
ros2 launch s622_arm_actions arm_actions.launch.py

# 终端 3：手动发 named pose goal 测试
ros2 action send_goal /move_to_pose s622_bt_manager/action/MoveToPose \
  "{named_pose: 'home', velocity_scale: 0.3, acceleration_scale: 0.3, timeout_sec: 30.0, ensure_servo_stopped: true}"
```

预期：机械臂回到 home，Action 返回 success。

⚠️ 如果报 `unknown named_pose: home`，说明 `arm_config.yaml` 的 `named_poses.home` 没正确传到参数。检查 launch 文件加载路径。

⚠️ 如果 MoveIt 报错 `Group "arm_group" not found`，改 `group_name` 为正确名字。

#### 验证 B：发 PoseStamped goal

```bash
ros2 action send_goal /move_to_pose s622_bt_manager/action/MoveToPose \
"{
  target_pose: {
    header: {frame_id: 'base_link'},
    pose: {
      position: {x: 0.35, y: 0.0, z: 0.25},
      orientation: {x: 1.0, y: 0.0, z: 0.0, w: 0.0}
    }
  },
  velocity_scale: 0.2,
  acceleration_scale: 0.2,
  timeout_sec: 30.0,
  ensure_servo_stopped: true
}"
```

预期：机械臂到达 `(0.35, 0, 0.25)`，夹爪朝下。

#### 验证 C：完整 BT 流程

```bash
# 终端 1：仿真
# 终端 2：YOLO
# 终端 3：Arm Actions
ros2 launch s622_arm_actions arm_actions.launch.py

# 终端 4：BT
ros2 launch s622_bt_manager single_arm_pick_place_bt.launch.py \
  tree_file:=detect_grasp_move_tree.xml tree_id:=DetectGraspMoveTree

# 终端 5：触发
ros2 topic pub /bt_trigger std_msgs/msg/Bool "data: true" --once
```

预期日志顺序：

```
=== Starting BT [DetectGraspMoveTree] ===
[MoveToPose: goal accepted]   ← go home
[MoveToPose: SUCCESS]
[DetectObject [cube]: ...]
[Grasp: uv=(...) -> base=(...)]
[MoveToPose: goal accepted]   ← go pregrasp
[MoveToPose: SUCCESS]
[PrintMessage]: Pregrasp reached
=== BT finished: SUCCESS ===
```

机械臂应停在 pregrasp 位置（cube 上方 13cm 偏远相机侧 3cm）。

### 5.3 验证 servo 生命周期

```bash
# 任务结束后，在 RViz2 用 MotionPlanning Plan&Execute 试一次
# 预期：能正常用，没有"Servo 抢占控制权"的问题
```

也手动 echo 一次 servo 状态：

```bash
ros2 service call /servo_node/status_servo std_srvs/srv/Trigger
# 或者直接观察 /servo_node/status 的输出（如果有）
```

---

## 验收清单

* [ ] 两个包都能 colcon build 过
* [ ] Action server 起来后能接收 named_pose goal，机械臂回 home
* [ ] 能接收 PoseStamped goal，机械臂到达指定位置且朝下
* [ ] BT 端 MoveToPose 节点能正确发 goal、等结果、返回 SUCCESS/FAILURE
* [ ] 完整树跑通：home → detect → grasp candidate → pregrasp
* [ ] 任务结束后 servo 没残留控制权（RViz 能 Plan&Execute）
* [ ] 中途 Ctrl+C 杀掉 BT 后，机械臂不会暴走（StatefulActionNode 的 onHalted 触发 cancel_goal）

---

## 常见踩坑

| 现象                                                    | 原因                        | 解决                                                                          |
| ------------------------------------------------------- | --------------------------- | ----------------------------------------------------------------------------- |
| 编译报 `s622_bt_manager/action/move_to_pose.hpp` 找不到 | rosidl 没生成或顺序问题     | `colcon build --packages-select s622_bt_manager` 单独编一次                   |
| Action server 启动后 MoveIt 报 group 找不到             | group_name 错               | 看 SRDF 改 `arm_config.yaml`                                                  |
| named pose 错位                                         | yaml 参数嵌套读取问题       | 把 `named_poses.home` 改成扁平参数 `home_joint_positions`（如果嵌套读不出来） |
| BT 节点永远 RUNNING                                     | callback group 单线程被阻塞 | bt_executor 用 MultiThreadedExecutor 至少 3 线程（已设）                      |
| Pose goal 机械臂运动方向错                              | yaw 约定问题                | 等 1.5 一起验证，本里程碑 home 朝向是固定的                                   |
| `pymoveit2` 找不到                                      | 没装                        | `pip install pymoveit2`（或者用源码包）                                       |

---

## 关于你需要确认的几个值

我用了一些假设默认值，**你需要根据实际改 `config/arm_config.yaml`**：

1. `joint_names`: `['j1','j2','j3','j4','j5','j6']` —— 看你 SRDF 实际名字
2. `group_name`: `'arm_group'` —— 看你 SRDF
3. `named_poses.home`: 看你 SRDF 的 home group_state 抄关节角

可以贴一下 SRDF 里 `<group>` 和 `<group_state name="home">` 这两段，我帮你确认。或者直接：

```bash
find ~/my_S622/src -name "*.srdf*" -exec grep -A5 "group name=" {} \;
find ~/my_S622/src -name "*.srdf*" -exec grep -A8 "group_state.*home" {} \;
```

把输出贴过来我帮你校对。
