已经核对 8 个文件，状态清晰。基于真实代码给出 M1.6 完整开发流程文档。

# M1.6 开发流程：放置流程 + PlanningScene attach

## 0. 前置状态核查

| 项                                                      | 状态   | 备注                                                                                    |
| ------------------------------------------------------- | ------ | --------------------------------------------------------------------------------------- |
| GoHome 是 joint-space                                   | ✅ 确认 | `move_to_pose_server.py` 走 `plan_to_joint_positions` → `moveit2.move_to_configuration` |
| `num_planning_attempts=10`, `allowed_planning_time=5.0` | ✅ 已加 | `moveit_planner.py` L36-37                                                              |
| `safe_joint_positions` 已配置                           | ✅      | `arm_config.yaml` 初值 `[0.0, -1.2, 1.5, 0.0, 1.2, 0.0]`，仅作 fallback                 |
| MoveToPose BT 节点支持 `named_pose` 端口                | ✅      | `motion_nodes.hpp`                                                                      |
| CMakeLists 已注册 AttachObject/DetachObject srv         | ❌ 待加 | M1.6.3                                                                                  |
| PlanningScene 是否有 table collision                    | ❓ 待查 | M1.6.3 起步前查                                                                         |
| Finger link 名称（attach 需要）                         | ❓ 待查 | M1.6.0 末尾顺手查                                                                       |

**结论**：M1.6.0 不需要改 `moveit_planner.py`，已经加好了。M1.6.0 只剩 safe pose 实测 + 顺手查两个待确认项。

---

## 1. 子里程碑总览

| 子里程碑   | 目标                                                          | 工作量 | 阻塞下一步的标准                                   |
| ---------- | ------------------------------------------------------------- | ------ | -------------------------------------------------- |
| **M1.6.0** | safe pose 实测，确认 lift→safe→home 可行；查 finger link 名   | 0.5h   | safe pose 能从典型 lift 后姿态到达 home，3/3       |
| **M1.6.1** | place 可达性 dry-run                                          | 1h     | home→pre_place→place→home 5/5 成功，关节不入 limit |
| **M1.6.2** | 完整 pick-place 不带 attach                                   | 2h     | 5 次跑下来 ≥ 3 次完整完成                          |
| **M1.6.3** | AttachObject/DetachObject service + BT 节点 + table collision | 3h     | RViz 中 attach/detach 视觉验证通过                 |
| **M1.6.4** | 集成 pick-place 带 attach                                     | 1h     | 5 次 ≥ 3 次完整完成（≥ 60%）                       |
| **M1.6.5** | CSV 日志 + 10 次连续验收                                      | 1h     | CSV 完整，成功率 ≥ 60%                             |

---

## 2. M1.6.0：safe pose 实测（0.5h）

### 2.1 目标

`safe_joint_positions` 当前是猜测值 `[0.0, -1.2, 1.5, 0.0, 1.2, 0.0]`。需要验证两件事：

* 从典型 lift 后关节角（`j1≈2.8, j6≈2.8`）能否规划到 safe
* 从 safe 能否规划到 home

如果两段都成功，safe pose 就有效；否则调值。

### 2.2 顺手做的事：查 finger link 名

```bash
ros2 param get /robot_state_publisher robot_description \
  | grep -oE '<link name="[^"]*finger[^"]*"' \
  | sort -u
```

预期输出类似 `<link name="finger1_link"` `<link name="finger2_link"`。**记下实际 link 名**，M1.6.3 attach 的 `touch_links` 要用。

之前看 URDF 时已经确认了：
```xml
<link name="finger1">
<link name="finger2">
```
Link 名就是 finger1 和 finger2，不带 _link 后缀。touch_links = [finger1, finger2, grasp_frame]。

### 2.3 测试树

新文件 `s622_bt_manager/behavior_trees/safe_pose_test_tree.xml`：

```xml
<root BTCPP_format="4">
  <BehaviorTree ID="SafePoseTest">
    <Sequence>
      <PrintMessage msg="go to safe"/>
      <MoveToPose named_pose="safe"
                  velocity_scale="0.20"
                  acceleration_scale="0.20"
                  timeout_sec="20.0"
                  ensure_servo_stopped="true"/>
      <Wait msec="1000"/>
      <PrintMessage msg="go to home"/>
      <MoveToPose named_pose="home"
                  velocity_scale="0.20"
                  acceleration_scale="0.20"
                  timeout_sec="20.0"
                  ensure_servo_stopped="false"/>
    </Sequence>
  </BehaviorTree>
</root>
```

### 2.4 验证步骤

1. 编译：

   ```bash
   cd ~/my_S622
   colcon build --merge-install --symlink-install --packages-select s622_bt_manager
   source install/setup.bash
   ```

2. 先复现"问题 A"的关节角起点。两种办法选一：

   **办法 A（推荐，最贴近实际）**：跑一遍 M1.5 的 `single_arm_grasp_test_tree.xml`，让它做完 lift 不要让它回 home（树里没有回 home 的话就跑完 lift 停住）。然后 `ros2 topic echo /joint_states --once` 记录当前关节角。

   **办法 B（快速）**：用 MoveIt RViz 手动拖到 `j1=2.8, j2=-0.5, j3=1.0, j4=0.0, j5=-0.8, j6=2.8`，Plan & Execute。

3. 跑 safe pose 测试树：

   ```bash
   ros2 launch s622_bt_manager single_arm_pick_place_bt.launch.py \
     tree_file:=safe_pose_test_tree.xml tree_id:=SafePoseTest
   ros2 topic pub /bt_trigger std_msgs/msg/Bool "data: true" --once
   ```

4. 预期日志：

   ```
   go to safe
   plan_to_joint_positions: [0.0, -1.2, 1.5, 0.0, 1.2, 0.0]
   ... MoveIt executed
   go to home
   plan_to_joint_positions: [0.5, -1.05, 1.05, -1.05, -0.8, 0.0]
   ... MoveIt executed
   ```

### 2.5 验收

* 从 `j1≈2.8, j6≈2.8` 起点 3 次重复，3/3 成功到达 home
* 如果失败：先看是 safe 段挂还是 home 段挂

  * safe 段挂：safe 关节角不合理，按"在起点和终点的中间地带"原则调，例如 `[1.5, -1.0, 1.2, 0.0, -0.8, 1.5]`
  * home 段挂：safe→home 还是远，加第二个中转 `safe2`

**实测后把最终 safe 值写回 `arm_config.yaml`**，并在文档里记一行"safe_joint_positions 实测值 = [...]"。

---

## 3. M1.6.1：place 可达性 dry-run（1h）

### 3.1 目标

验证 `place_pose=(0.35, -0.18)` 在 S622 工作空间内，不带 detect/servo/attach 干扰，纯 MoveIt 规划测试。

### 3.2 配置文件

新文件 `s622_bt_manager/config/pick_place_poses.yaml`：

```yaml
bt_executor_node:
  ros__parameters:
    # 全部扁平 + 全 float，参照 named_poses 的写法
    place_frame_id: "base_link"
    place_position:
      - 0.35
      - -0.18
      - 0.030
    place_rpy:
      - 3.14159
      - 0.0
      - 1.5708

    pre_place_frame_id: "base_link"
    pre_place_position:
      - 0.35
      - -0.18
      - 0.16
    pre_place_rpy:
      - 3.14159
      - 0.0
      - 1.5708
```

**注意 YAML 坑**：列表全用 block 形式（每行一个 `- 0.0`），数值全是 float（不能写 `0`）。

### 3.3 GeneratePlaceCandidate BT 节点

加到现有的 `grasp_nodes.hpp` 末尾（不新建文件，省 CMakeLists 改动）：

```cpp
// grasp_nodes.hpp 加在 GenerateGraspCandidate 后面
class GeneratePlaceCandidate : public BT::SyncActionNode
{
public:
    GeneratePlaceCandidate(const std::string &name,
                           const BT::NodeConfig &config,
                           rclcpp::Node::SharedPtr node);
    static BT::PortsList providedPorts()
    {
        return {
            BT::OutputPort<geometry_msgs::msg::PoseStamped>("place_pose"),
            BT::OutputPort<geometry_msgs::msg::PoseStamped>("pre_place_pose"),
        };
    }
    BT::NodeStatus tick() override;

private:
    rclcpp::Node::SharedPtr node_;
    geometry_msgs::msg::PoseStamped place_pose_;
    geometry_msgs::msg::PoseStamped pre_place_pose_;
};
```

`grasp_nodes.cpp` 末尾加实现：

```cpp
// helper: rpy -> quaternion
static void rpy_to_quat(double r, double p, double y, geometry_msgs::msg::Quaternion &q)
{
    double cr = std::cos(r * 0.5), sr = std::sin(r * 0.5);
    double cp = std::cos(p * 0.5), sp = std::sin(p * 0.5);
    double cy = std::cos(y * 0.5), sy = std::sin(y * 0.5);
    q.w = cr * cp * cy + sr * sp * sy;
    q.x = sr * cp * cy - cr * sp * sy;
    q.y = cr * sp * cy + sr * cp * sy;
    q.z = cr * cp * sy - sr * sp * cy;
}

static geometry_msgs::msg::PoseStamped
load_pose_from_params(rclcpp::Node::SharedPtr node, const std::string &prefix)
{
    node->declare_parameter<std::string>(prefix + "_frame_id", "base_link");
    node->declare_parameter<std::vector<double>>(
        prefix + "_position", {0.0, 0.0, 0.0});
    node->declare_parameter<std::vector<double>>(
        prefix + "_rpy", {0.0, 0.0, 0.0});

    auto frame = node->get_parameter(prefix + "_frame_id").as_string();
    auto pos = node->get_parameter(prefix + "_position").as_double_array();
    auto rpy = node->get_parameter(prefix + "_rpy").as_double_array();

    geometry_msgs::msg::PoseStamped ps;
    ps.header.frame_id = frame;
    ps.pose.position.x = pos[0];
    ps.pose.position.y = pos[1];
    ps.pose.position.z = pos[2];
    rpy_to_quat(rpy[0], rpy[1], rpy[2], ps.pose.orientation);
    RCLCPP_INFO(node->get_logger(),
                "loaded %s: (%.3f, %.3f, %.3f) rpy=(%.3f, %.3f, %.3f)",
                prefix.c_str(), pos[0], pos[1], pos[2], rpy[0], rpy[1], rpy[2]);
    return ps;
}

GeneratePlaceCandidate::GeneratePlaceCandidate(
    const std::string &name, const BT::NodeConfig &config,
    rclcpp::Node::SharedPtr node)
    : BT::SyncActionNode(name, config), node_(node)
{
    place_pose_ = load_pose_from_params(node_, "place");
    pre_place_pose_ = load_pose_from_params(node_, "pre_place");
}

BT::NodeStatus GeneratePlaceCandidate::tick()
{
    auto stamp = node_->now();
    place_pose_.header.stamp = stamp;
    pre_place_pose_.header.stamp = stamp;
    setOutput("place_pose", place_pose_);
    setOutput("pre_place_pose", pre_place_pose_);
    return BT::NodeStatus::SUCCESS;
}
```

在 `registerGraspNodes` 函数里加注册：

```cpp
factory.registerBuilder<GeneratePlaceCandidate>(
    "GeneratePlaceCandidate",
    [node](const std::string &name, const BT::NodeConfig &config)
    { return std::make_unique<GeneratePlaceCandidate>(name, config, node); });
```

### 3.4 测试树

新文件 `s622_bt_manager/behavior_trees/place_reachability_test_tree.xml`：

```xml
<root BTCPP_format="4">
  <BehaviorTree ID="PlaceReachabilityTest">
    <Sequence>
      <PrintMessage message="=== generate place poses ==="/>
      <GeneratePlaceCandidate place_pose="{place_pose}"
                              pre_place_pose="{pre_place_pose}"/>

      <PrintMessage message="=== go home ==="/>
      <MoveToPose named_pose="home"
                  velocity_scale="0.20"
                  acceleration_scale="0.20"
                  timeout_sec="20.0"
                  ensure_servo_stopped="true"/>

      <PrintMessage message="=== go pre_place ==="/>
      <MoveToPose target_pose="{pre_place_pose}"
                  velocity_scale="0.20"
                  acceleration_scale="0.20"
                  timeout_sec="20.0"
                  ensure_servo_stopped="true"/>

      <PrintMessage message="=== go place ==="/>
      <MoveToPose target_pose="{place_pose}"
                  velocity_scale="0.10"
                  acceleration_scale="0.10"
                  timeout_sec="15.0"
                  ensure_servo_stopped="true"/>

      <Wait msec="1000"/>

      <PrintMessage message="=== back to pre_place ==="/>
      <MoveToPose target_pose="{pre_place_pose}"
                  velocity_scale="0.20"
                  acceleration_scale="0.20"
                  timeout_sec="15.0"
                  ensure_servo_stopped="true"/>

      <PrintMessage message="=== back to home ==="/>
      <MoveToPose named_pose="home"
                  velocity_scale="0.20"
                  acceleration_scale="0.20"
                  timeout_sec="20.0"
                  ensure_servo_stopped="true"/>
    </Sequence>
  </BehaviorTree>
</root>
```

### 3.5 Launch 修改

`s622_bt_manager/launch/single_arm_pick_place_bt.launch.py` 里 `bt_executor_node` 的 `parameters` 加上 `pick_place_poses.yaml`。具体改动：

```python
# 假设原本是
# parameters=[{'use_sim_time': True, ...}],
# 改成

pick_place_config = os.path.join(
    get_package_share_directory('s622_bt_manager'),
    'config', 'pick_place_poses.yaml')

# Node 的 parameters
parameters=[
    pick_place_config,
    {'use_sim_time': True, ...其他原有参数},
],
```

`CMakeLists.txt` 安装 config 目录（如果之前没装）：

```cmake
install(DIRECTORY behavior_trees launch config DESTINATION share/${PROJECT_NAME})
```

### 3.6 验证步骤

```bash
# 因为加了 srv/action 没有变动，不需要 rm -rf build
colcon build --merge-install --symlink-install --packages-select s622_bt_manager
source install/setup.bash

ros2 launch s622_bt_manager single_arm_pick_place_bt.launch.py \
  tree_file:=place_reachability_test_tree.xml tree_id:=PlaceReachabilityTest

ros2 topic pub /bt_trigger std_msgs/msg/Bool "data: true" --once
```

预期日志关键行：

```
loaded place: (0.350, -0.180, 0.030) rpy=(3.142, 0.000, 1.571)
loaded pre_place: (0.350, -0.180, 0.160) rpy=(3.142, 0.000, 1.571)
=== go home ===
=== go pre_place ===
plan_to_pose: pos=(0.350,-0.180,0.160) quat=(...)
=== go place ===
=== back to pre_place ===
=== back to home ===
```

每段 MoveToPose 之后看 `ros2 topic echo /joint_states --once`，关注：

* j4 是否在 `[-1.3, 1.3]` 区间内（远离 ±1.47 limit）
* 没有任何关节绝对值 > 2.9

### 3.7 验收

* 连续 5 次全部成功
* 关节角始终在安全区
* 如失败：可能是 j1 转到负角度区受限。备选 place_pose：

  * `(0.30, -0.20, 0.030)`：稍微拉近
  * yaw 改 `1.5708` 改成 `0.0`：避开 j6 大角度

每改一次都重跑 5 次验证。**M1.6.1 不通过不进 M1.6.2。**

---

## 4. M1.6.2：完整 pick-place 不带 attach（2h）

### 4.1 目标

把 M1.5 的 `single_arm_grasp_test_tree.xml` 后面接 place 流程，**不引入 attach**。验证：

* place 流程在带 cube（夹爪闭合）情况下是否可行
* lift 后插入 safe 中转点是否有效
* 如果不带 attach 也能跑通，说明 attach 不是必需，加 attach 是为了让 MoveIt 知道 cube 提高规划质量
* 如果不带 attach 失败，根因是路径碰撞或者 IK 选错，attach 加上去会有帮助

### 4.2 测试树

新文件 `s622_bt_manager/behavior_trees/pick_place_no_attach_tree.xml`：

```xml
<root BTCPP_format="4">
  <BehaviorTree ID="PickPlaceNoAttach">
    <Sequence>
      <CheckSystemReady joint_states_max_age="2.0"/>

      <!-- ===== Pick (复用 M1.5 流程) ===== -->
      <SetGripper command="open" timeout_sec="5.0"/>
      <MoveToPose named_pose="home"
                  velocity_scale="0.30" acceleration_scale="0.30"
                  timeout_sec="20.0"
                  ensure_servo_stopped="true"/>

      <RetryUntilSuccessful num_attempts="5">
        <Sequence>
          <DetectObject object_name="cube"
                        min_confidence="0.05"
                        max_age_sec="1.0"
                        target_u="{target_u}"
                        target_v="{target_v}"
                        object_yaw="{object_yaw}"
                        confidence="{confidence}"/>
          <GenerateGraspCandidate target_u="{target_u}"
                                  target_v="{target_v}"
                                  object_yaw="{object_yaw}"
                                  grasp_pose_x="{grasp_x}"
                                  grasp_pose_y="{grasp_y}"
                                  grasp_yaw="{grasp_yaw}"
                                  pregrasp_pose="{pregrasp_pose}"
                                  grasp_pose="{grasp_pose}"/>
        </Sequence>
      </RetryUntilSuccessful>

      <LockTargetPixel input_u="{target_u}"
                       input_v="{target_v}"
                       locked_u="{locked_u}"
                       locked_v="{locked_v}"/>

      <MoveToPose target_pose="{pregrasp_pose}"
                  velocity_scale="0.20" acceleration_scale="0.20"
                  timeout_sec="20.0"
                  ensure_servo_stopped="true"/>

      <VisualAlign mode="xy"
                   target_u="{locked_u}" target_v="{locked_v}"
                   tolerance_px="12.0"
                   ensure_servo_started="true"
                   timeout_sec="25.0"
                   target_grasp_x="{grasp_x}"
                   target_grasp_y="{grasp_y}"/>

      <AlignLockCheck locked_u="{locked_u}" locked_v="{locked_v}"
                      tolerance_px="20.0"/>

      <VisualAlign mode="descend"
                   distance="0.13" speed="0.04"
                   timeout_sec="30.0"/>

      <SetGripper command="close" timeout_sec="5.0"/>

      <VerifyGrasp finger_min_position="0.005"
                   feedback_joint="finger1_joint"/>

      <VisualAlign mode="lift"
                   distance="0.10" speed="0.05"
                   timeout_sec="20.0"/>

      <!-- ===== 关键：lift 后插 safe 中转 ===== -->
      <MoveToPose named_pose="safe"
                  velocity_scale="0.20" acceleration_scale="0.20"
                  timeout_sec="20.0"
                  ensure_servo_stopped="true"/>

      <!-- ===== Place ===== -->
      <GeneratePlaceCandidate place_pose="{place_pose}"
                              pre_place_pose="{pre_place_pose}"/>

      <MoveToPose target_pose="{pre_place_pose}"
                  velocity_scale="0.20" acceleration_scale="0.20"
                  timeout_sec="20.0"
                  ensure_servo_stopped="true"/>

      <MoveToPose target_pose="{place_pose}"
                  velocity_scale="0.10" acceleration_scale="0.10"
                  timeout_sec="15.0"
                  ensure_servo_stopped="true"/>

      <SetGripper command="open" timeout_sec="5.0"/>

      <VisualAlign mode="lift"
                   distance="0.10" speed="0.05"
                   timeout_sec="20.0"/>

      <MoveToPose named_pose="home"
                  velocity_scale="0.20" acceleration_scale="0.20"
                  timeout_sec="20.0"
                  ensure_servo_stopped="true"/>
    </Sequence>
  </BehaviorTree>
</root>
```

**注意几点**：

* `DetectObject`、`GenerateGraspCandidate`、`LockTargetPixel` 的具体 port 名按 M1.5 现状（如果端口名和上面不一致请按你实际的改）
* lift 后用 `MoveToPose named_pose="safe"` 强制中转
* 第二段 lift 是为了 retreat（避免直接横移撞 cube 旁边）

### 4.3 验证步骤

```bash
colcon build --merge-install --symlink-install --packages-select s622_bt_manager
source install/setup.bash

# 5 个终端按 §9 顺序起，然后：
ros2 launch s622_bt_manager single_arm_pick_place_bt.launch.py \
  tree_file:=pick_place_no_attach_tree.xml tree_id:=PickPlaceNoAttach
ros2 topic pub /bt_trigger std_msgs/msg/Bool "data: true" --once
```

### 4.4 验收 + 调试矩阵

每次失败定位失败节点，对应处理：

| 失败节点                     | 原因猜测                    | 应对                                                                |
| ---------------------------- | --------------------------- | ------------------------------------------------------------------- |
| `MoveToPose pre_place`       | place pose 不可达           | M1.6.1 没测好，回 M1.6.1                                            |
| `MoveToPose place`           | pre→place 太陡，pilz 不支持 | velocity 降到 0.05，或者改 pre_place z 从 0.16 降到 0.10            |
| `MoveToPose home` (place 后) | place 关节角→home 远        | safe 中转应有效；如果还失败，需要在 place 后再插一次 safe           |
| 第二段 lift（mode=lift）     | servo 还没释放控制权        | 检查 SetGripper 后 servo 状态，确认 ensure_servo_started 自动起来了 |
| `VerifyGrasp`                | 实际没抓到 cube             | 是 M1.5 问题，不在 M1.6 范围                                        |

**5 次能跑通 ≥ 3 次进入 M1.6.3**。如果次次失败，先排查上面表格。

---

## 5. M1.6.3：AttachObject/DetachObject service + BT 节点（3h）

### 5.1 起步前的核查

```bash
# 1. 看当前 PlanningScene 里有什么 collision objects
ros2 topic echo /monitored_planning_scene --once \
  | grep -E "id:|frame_id:|operation:" | head -50

# 2. 看 grasp_frame 在 URDF 里相对哪个 link
ros2 run tf2_tools view_frames
# 或
ros2 run tf2_ros tf2_echo base_link grasp_frame
```

**记录两件事**：

* PlanningScene 当前有没有 table。如果没有，5.5 节会自动补
* M1.6.0 查到的 finger link 名（写到 5.4 的 `default_touch_links` 里）


都能从已有代码里直接看出来。

1. PlanningScene 有没有 table：没有。

planning_scene_service.py 还没写——那是 M1.6.3 的内容，你现在在 M1.6.2。当前没有任何节点往 /planning_scene 发 collision object。

2. Finger link 名：finger1 和 finger2。

URDF 里定义的是 `<link name="finger1"> 和 <link name="finger2">`，不带 _link 后缀。所以 touch_links 是：


default_touch_links: ['finger1', 'finger2', 'grasp_frame']

### 5.2 srv 定义

新文件 `s622_bt_manager/srv/AttachObject.srv`：

```
# 把 object 作为 attached collision object 挂到 link 上
string object_name             # "cube"
string link_name               # "grasp_frame"
geometry_msgs/Vector3 size     # box 三轴尺寸；全 0 用默认值
geometry_msgs/Pose pose_in_link    # 物体在 link 坐标系下的 pose
string[] touch_links           # 不视为碰撞的相邻 link；空用默认
---
bool success
string error_msg
```

新文件 `s622_bt_manager/srv/DetachObject.srv`：

```
# 从 attached 状态摘下；可选放回 world collision
string object_name
bool put_back_in_world         # true: 同时 add 回 world
geometry_msgs/Pose drop_pose   # 放回的位姿（base_link 系），仅 put_back=true 时用
---
bool success
string error_msg
```

### 5.3 CMakeLists 更新

`s622_bt_manager/CMakeLists.txt` 的 `rosidl_generate_interfaces`：

```cmake
rosidl_generate_interfaces(${PROJECT_NAME}
  "action/MoveToPose.action"
  "action/VisualAlign.action"
  "srv/SetGripper.srv"
  "srv/AttachObject.srv"
  "srv/DetachObject.srv"
  DEPENDENCIES geometry_msgs action_msgs std_msgs
)
```

`add_executable(bt_executor_node ...)` 加 `src/scene_nodes.cpp`：

```cmake
add_executable(bt_executor_node
  src/bt_executor_node.cpp
  src/dummy_nodes.cpp
  src/perception_nodes.cpp
  src/grasp_nodes.cpp
  src/motion_nodes.cpp
  src/gripper_nodes.cpp
  src/servo_nodes.cpp
  src/scene_nodes.cpp
)
```

`ament_target_dependencies` 加 `moveit_msgs`、`shape_msgs`（Python 端用，但 srv 里没有依赖它们）—— **C++ 端不需要 moveit_msgs**，只需要 srv 自定义类型。

### 5.4 Python service server

新文件 `s622_arm_actions/s622_arm_actions/planning_scene_service.py`：

```python
#!/usr/bin/env python3
"""PlanningScene attach/detach Service via /planning_scene topic (is_diff=true)."""
import rclpy
from rclpy.node import Node
from rclpy.callback_groups import ReentrantCallbackGroup
from rclpy.executors import MultiThreadedExecutor
from rclpy.qos import QoSProfile, QoSDurabilityPolicy, QoSReliabilityPolicy

from geometry_msgs.msg import Pose
from shape_msgs.msg import SolidPrimitive
from moveit_msgs.msg import (
    AttachedCollisionObject, CollisionObject, PlanningScene)

from s622_bt_manager.srv import AttachObject, DetachObject


class PlanningSceneService(Node):
    def __init__(self):
        super().__init__('planning_scene_service')

        self.declare_parameter('base_link', 'base_link')
        self.declare_parameter('default_object_size', [0.04, 0.04, 0.04])
        self.declare_parameter('default_touch_links',
                               ['finger1_link', 'finger2_link', 'grasp_frame'])
        self.declare_parameter('publish_table', True)
        self.declare_parameter('table_size', [1.2, 1.2, 0.02])
        self.declare_parameter('table_center', [0.4, 0.0, -0.011])  # z 略低于 0

        self._base = self.get_parameter('base_link').value
        self._default_size = [float(x) for x in
                              self.get_parameter('default_object_size').value]
        self._default_touch = [str(x) for x in
                               self.get_parameter('default_touch_links').value]

        # /planning_scene 用 TRANSIENT_LOCAL 让晚连接的订阅者也能拿到
        qos = QoSProfile(
            depth=10,
            durability=QoSDurabilityPolicy.TRANSIENT_LOCAL,
            reliability=QoSReliabilityPolicy.RELIABLE)
        self.scene_pub = self.create_publisher(PlanningScene, '/planning_scene', qos)

        cb = ReentrantCallbackGroup()
        self.attach_srv = self.create_service(
            AttachObject, 'attach_object', self._on_attach, callback_group=cb)
        self.detach_srv = self.create_service(
            DetachObject, 'detach_object', self._on_detach, callback_group=cb)

        # 启动幂等清理 + 可选 table
        self._attached = {}    # name -> (link, size)
        self.create_timer(0.5, self._initial_publish, callback_group=cb)
        self._initialized = False

        self.get_logger().info('planning_scene_service ready')

    def _initial_publish(self):
        if self._initialized:
            return
        self._initialized = True
        # 发布 table
        if self.get_parameter('publish_table').value:
            self._publish_table()

    def _publish_table(self):
        size = [float(x) for x in self.get_parameter('table_size').value]
        center = [float(x) for x in self.get_parameter('table_center').value]
        co = CollisionObject()
        co.header.frame_id = self._base
        co.id = 'table'
        co.operation = CollisionObject.ADD
        prim = SolidPrimitive()
        prim.type = SolidPrimitive.BOX
        prim.dimensions = size
        co.primitives = [prim]
        pose = Pose()
        pose.position.x = center[0]
        pose.position.y = center[1]
        pose.position.z = center[2]
        pose.orientation.w = 1.0
        co.primitive_poses = [pose]

        scene = PlanningScene()
        scene.is_diff = True
        scene.world.collision_objects = [co]
        self.scene_pub.publish(scene)
        self.get_logger().info(
            f'published table collision: size={size}, center={center}')

    def _on_attach(self, req, resp):
        try:
            size = [req.size.x, req.size.y, req.size.z]
            if size == [0.0, 0.0, 0.0]:
                size = self._default_size
            touch = list(req.touch_links) if req.touch_links else self._default_touch

            aco = AttachedCollisionObject()
            aco.link_name = req.link_name
            aco.touch_links = touch
            aco.object.header.frame_id = req.link_name
            aco.object.id = req.object_name
            aco.object.operation = CollisionObject.ADD

            prim = SolidPrimitive()
            prim.type = SolidPrimitive.BOX
            prim.dimensions = size
            aco.object.primitives = [prim]
            aco.object.primitive_poses = [req.pose_in_link]

            # 同时清掉 world 里同名 object（如果之前 detach 留下了）
            world_remove = CollisionObject()
            world_remove.header.frame_id = self._base
            world_remove.id = req.object_name
            world_remove.operation = CollisionObject.REMOVE

            scene = PlanningScene()
            scene.is_diff = True
            scene.robot_state.is_diff = True
            scene.robot_state.attached_collision_objects = [aco]
            scene.world.collision_objects = [world_remove]
            self.scene_pub.publish(scene)

            self._attached[req.object_name] = (req.link_name, size)
            resp.success = True
            resp.error_msg = ''
            self.get_logger().info(
                f'attach {req.object_name} to {req.link_name}, '
                f'size={size}, touch={touch}')
        except Exception as e:
            resp.success = False
            resp.error_msg = str(e)
            self.get_logger().error(f'attach exception: {e}')
        return resp

    def _on_detach(self, req, resp):
        try:
            info = self._attached.get(req.object_name)
            if info is None:
                resp.success = True
                resp.error_msg = 'not attached, no-op'
                return resp
            link, size = info

            aco = AttachedCollisionObject()
            aco.link_name = link
            aco.object.id = req.object_name
            aco.object.operation = CollisionObject.REMOVE

            scene = PlanningScene()
            scene.is_diff = True
            scene.robot_state.is_diff = True
            scene.robot_state.attached_collision_objects = [aco]

            if req.put_back_in_world:
                co = CollisionObject()
                co.header.frame_id = self._base
                co.id = req.object_name
                co.operation = CollisionObject.ADD
                prim = SolidPrimitive()
                prim.type = SolidPrimitive.BOX
                prim.dimensions = size
                co.primitives = [prim]
                co.primitive_poses = [req.drop_pose]
                scene.world.collision_objects = [co]

            self.scene_pub.publish(scene)
            del self._attached[req.object_name]
            resp.success = True
            resp.error_msg = ''
            self.get_logger().info(
                f'detach {req.object_name} (put_back={req.put_back_in_world})')
        except Exception as e:
            resp.success = False
            resp.error_msg = str(e)
            self.get_logger().error(f'detach exception: {e}')
        return resp


def main():
    rclpy.init()
    node = PlanningSceneService()
    executor = MultiThreadedExecutor(num_threads=2)
    executor.add_node(node)
    try:
        executor.spin()
    finally:
        node.destroy_node()
        rclpy.shutdown()


if __name__ == '__main__':
    main()
```

`s622_arm_actions/setup.py` 加 entry point：

```python
entry_points={
    'console_scripts': [
        ...
        'planning_scene_service = s622_arm_actions.planning_scene_service:main',
    ],
},
```

`s622_arm_actions/package.xml` 加依赖：

```xml
<depend>moveit_msgs</depend>
<depend>shape_msgs</depend>
```

`arm_actions.launch.py` 加 node，并把 finger link 名传入（用 M1.6.0 实测值替换）：

```python
Node(
    package='s622_arm_actions',
    executable='planning_scene_service',
    name='planning_scene_service',
    output='screen',
    parameters=[{
        'use_sim_time': True,
        'base_link': 'base_link',
        'default_object_size': [0.04, 0.04, 0.04],
        # 用 M1.6.0 grep 出来的实际 link 名
        'default_touch_links': ['finger1_link', 'finger2_link', 'grasp_frame'],
        'publish_table': True,
        'table_size': [1.2, 1.2, 0.02],
        'table_center': [0.4, 0.0, -0.011],
    }],
),
```

### 5.5 BT 端 scene_nodes

新文件 `s622_bt_manager/include/s622_bt_manager/scene_nodes.hpp`：

```cpp
#pragma once

#include <memory>
#include <string>

#include <behaviortree_cpp/bt_factory.h>
#include <rclcpp/rclcpp.hpp>
#include <geometry_msgs/msg/pose_stamped.hpp>
#include <geometry_msgs/msg/pose.hpp>

#include "s622_bt_manager/srv/attach_object.hpp"
#include "s622_bt_manager/srv/detach_object.hpp"

namespace s622_bt
{

class AttachObjectNode : public BT::SyncActionNode
{
public:
    AttachObjectNode(const std::string &name, const BT::NodeConfig &config,
                     rclcpp::Node::SharedPtr node);
    static BT::PortsList providedPorts()
    {
        return {
            BT::InputPort<std::string>("object_name", "cube", ""),
            BT::InputPort<std::string>("link_name", "grasp_frame", ""),
            BT::InputPort<double>("size_x", 0.04, ""),
            BT::InputPort<double>("size_y", 0.04, ""),
            BT::InputPort<double>("size_z", 0.04, ""),
            BT::InputPort<double>("offset_z", 0.02,
                                  "pose.z in link frame (cube center below TCP)"),
            BT::InputPort<double>("timeout_sec", 3.0, ""),
        };
    }
    BT::NodeStatus tick() override;

private:
    rclcpp::Node::SharedPtr node_;
    rclcpp::Client<s622_bt_manager::srv::AttachObject>::SharedPtr client_;
};

class DetachObjectNode : public BT::SyncActionNode
{
public:
    DetachObjectNode(const std::string &name, const BT::NodeConfig &config,
                     rclcpp::Node::SharedPtr node);
    static BT::PortsList providedPorts()
    {
        return {
            BT::InputPort<std::string>("object_name", "cube", ""),
            BT::InputPort<bool>("put_back_in_world", true, ""),
            BT::InputPort<geometry_msgs::msg::PoseStamped>(
                "drop_pose", "where to put it back (base_link frame)"),
            BT::InputPort<double>("timeout_sec", 3.0, ""),
        };
    }
    BT::NodeStatus tick() override;

private:
    rclcpp::Node::SharedPtr node_;
    rclcpp::Client<s622_bt_manager::srv::DetachObject>::SharedPtr client_;
};

void registerSceneNodes(BT::BehaviorTreeFactory &factory,
                        rclcpp::Node::SharedPtr node);

} // namespace s622_bt
```

新文件 `s622_bt_manager/src/scene_nodes.cpp`：

```cpp
#include "s622_bt_manager/scene_nodes.hpp"

#include <chrono>

using namespace std::chrono_literals;

namespace s622_bt
{

AttachObjectNode::AttachObjectNode(const std::string &name,
                                   const BT::NodeConfig &config,
                                   rclcpp::Node::SharedPtr node)
    : BT::SyncActionNode(name, config), node_(node)
{
    client_ = node_->create_client<s622_bt_manager::srv::AttachObject>("attach_object");
}

BT::NodeStatus AttachObjectNode::tick()
{
    double timeout = getInput<double>("timeout_sec").value_or(3.0);

    if (!client_->wait_for_service(std::chrono::milliseconds(
            static_cast<int>(timeout * 1000))))
    {
        RCLCPP_ERROR(node_->get_logger(), "attach_object service unavailable");
        return BT::NodeStatus::FAILURE;
    }

    auto req = std::make_shared<s622_bt_manager::srv::AttachObject::Request>();
    req->object_name = getInput<std::string>("object_name").value_or("cube");
    req->link_name = getInput<std::string>("link_name").value_or("grasp_frame");
    req->size.x = getInput<double>("size_x").value_or(0.04);
    req->size.y = getInput<double>("size_y").value_or(0.04);
    req->size.z = getInput<double>("size_z").value_or(0.04);
    req->pose_in_link.position.x = 0.0;
    req->pose_in_link.position.y = 0.0;
    req->pose_in_link.position.z = getInput<double>("offset_z").value_or(0.02);
    req->pose_in_link.orientation.w = 1.0;
    // touch_links 留空，server 用 default_touch_links

    auto future = client_->async_send_request(req);
    if (future.wait_for(std::chrono::duration<double>(timeout)) !=
        std::future_status::ready)
    {
        RCLCPP_ERROR(node_->get_logger(), "attach_object timeout");
        return BT::NodeStatus::FAILURE;
    }
    auto resp = future.get();
    if (!resp->success)
    {
        RCLCPP_ERROR(node_->get_logger(),
                     "attach failed: %s", resp->error_msg.c_str());
        return BT::NodeStatus::FAILURE;
    }
    RCLCPP_INFO(node_->get_logger(), "attached %s", req->object_name.c_str());
    return BT::NodeStatus::SUCCESS;
}

DetachObjectNode::DetachObjectNode(const std::string &name,
                                   const BT::NodeConfig &config,
                                   rclcpp::Node::SharedPtr node)
    : BT::SyncActionNode(name, config), node_(node)
{
    client_ = node_->create_client<s622_bt_manager::srv::DetachObject>("detach_object");
}

BT::NodeStatus DetachObjectNode::tick()
{
    double timeout = getInput<double>("timeout_sec").value_or(3.0);

    if (!client_->wait_for_service(std::chrono::milliseconds(
            static_cast<int>(timeout * 1000))))
    {
        RCLCPP_ERROR(node_->get_logger(), "detach_object service unavailable");
        return BT::NodeStatus::FAILURE;
    }

    auto req = std::make_shared<s622_bt_manager::srv::DetachObject::Request>();
    req->object_name = getInput<std::string>("object_name").value_or("cube");
    req->put_back_in_world = getInput<bool>("put_back_in_world").value_or(true);

    auto drop_ps = getInput<geometry_msgs::msg::PoseStamped>("drop_pose");
    if (drop_ps.has_value())
    {
        req->drop_pose = drop_ps.value().pose;
    }
    else
    {
        // 默认放在 base_link 原点附近（不太有意义，但避免 crash）
        req->drop_pose.orientation.w = 1.0;
    }

    auto future = client_->async_send_request(req);
    if (future.wait_for(std::chrono::duration<double>(timeout)) !=
        std::future_status::ready)
    {
        RCLCPP_ERROR(node_->get_logger(), "detach_object timeout");
        return BT::NodeStatus::FAILURE;
    }
    auto resp = future.get();
    if (!resp->success)
    {
        RCLCPP_ERROR(node_->get_logger(),
                     "detach failed: %s", resp->error_msg.c_str());
        return BT::NodeStatus::FAILURE;
    }
    RCLCPP_INFO(node_->get_logger(), "detached %s", req->object_name.c_str());
    return BT::NodeStatus::SUCCESS;
}

void registerSceneNodes(BT::BehaviorTreeFactory &factory,
                        rclcpp::Node::SharedPtr node)
{
    factory.registerNodeType<AttachObjectNode>(
        "AttachObject",
        [node](const std::string &name, const BT::NodeConfig &config)
        { return std::make_unique<AttachObjectNode>(name, config, node); });
    factory.registerNodeType<DetachObjectNode>(
        "DetachObject",
        [node](const std::string &name, const BT::NodeConfig &config)
        { return std::make_unique<DetachObjectNode>(name, config, node); });
}

} // namespace s622_bt
```

`bt_executor_node.cpp` 里加：

```cpp
#include "s622_bt_manager/scene_nodes.hpp"
// 在注册区
s622_bt::registerSceneNodes(factory, node);
```

### 5.6 单元测试树

新文件 `s622_bt_manager/behavior_trees/attach_detach_test_tree.xml`：

```xml
<root BTCPP_format="4">
  <BehaviorTree ID="AttachDetachTest">
    <Sequence>
      <PrintMessage message="=== attach cube ==="/>
      <AttachObject object_name="cube"
                    link_name="grasp_frame"
                    size_x="0.04" size_y="0.04" size_z="0.04"
                    offset_z="0.02"/>
      <Wait msec="3000"/>

      <PrintMessage message="=== generate place to know drop_pose ==="/>
      <GeneratePlaceCandidate place_pose="{place_pose}"
                              pre_place_pose="{pre_place_pose}"/>

      <PrintMessage message="=== detach cube ==="/>
      <DetachObject object_name="cube"
                    put_back_in_world="true"
                    drop_pose="{place_pose}"/>
      <Wait msec="3000"/>
    </Sequence>
  </BehaviorTree>
</root>
```

### 5.7 验证步骤

**因为加了 srv，必须清 build**：

```bash
rm -rf build/s622_bt_manager install/s622_bt_manager
colcon build --merge-install --symlink-install \
  --packages-select s622_bt_manager s622_arm_actions
source install/setup.bash
```

启动顺序按 §9，加上 RViz 打开 PlanningScene display。然后：

```bash
ros2 launch s622_bt_manager single_arm_pick_place_bt.launch.py \
  tree_file:=attach_detach_test_tree.xml tree_id:=AttachDetachTest
ros2 topic pub /bt_trigger std_msgs/msg/Bool "data: true" --once
```

**RViz 视觉验证**：

1. 启动后看见 table（绿色 plane）出现在 `(0.4, 0.0, ~0)`
2. `=== attach cube ===` 后，RViz PlanningScene 显示 cube **挂在 grasp_frame 上**（黄绿色 attached object）。手动 RViz 拖动 IK marker，cube 跟着 EE 走
3. `=== detach cube ===` 后，cube 从 EE 摘下，出现在 `place_pose=(0.35, -0.18, 0.03)` 位置（变成 world collision，颜色不同）

**话题验证**：

```bash
ros2 service call /attach_object s622_bt_manager/srv/AttachObject \
  "{object_name: 'cube', link_name: 'grasp_frame', size: {x: 0.04, y: 0.04, z: 0.04}, pose_in_link: {position: {z: 0.02}, orientation: {w: 1.0}}, touch_links: []}"
```

### 5.8 验收

* table 在 RViz 可见
* attach 后 cube 跟随 EE 移动
* detach 后 cube 在指定 drop_pose
* 重复 attach→detach→attach 不报错（幂等）

---

## 6. M1.6.4：集成 pick-place 带 attach（1h）

### 6.1 测试树

新文件 `s622_bt_manager/behavior_trees/pick_place_full_tree.xml`：

在 M1.6.2 的 `pick_place_no_attach_tree.xml` 基础上，**只加两处**：

```xml
      <VerifyGrasp finger_min_position="0.005"
                   feedback_joint="finger1_joint"/>

      <!-- 新增：抓到后立即 attach -->
      <AttachObject object_name="cube"
                    link_name="grasp_frame"
                    size_x="0.04" size_y="0.04" size_z="0.04"
                    offset_z="0.02"/>

      <VisualAlign mode="lift"
                   distance="0.10" speed="0.05"
                   timeout_sec="20.0"/>

      ...

      <GeneratePlaceCandidate place_pose="{place_pose}"
                              pre_place_pose="{pre_place_pose}"/>

      <MoveToPose target_pose="{pre_place_pose}" .../>
      <MoveToPose target_pose="{place_pose}" .../>

      <SetGripper command="open" timeout_sec="5.0"/>

      <!-- 新增：放下后 detach 回 world -->
      <DetachObject object_name="cube"
                    put_back_in_world="true"
                    drop_pose="{place_pose}"/>

      <VisualAlign mode="lift" .../>
      <MoveToPose named_pose="home" .../>
```

完整 XML 我不再重复，把上面两段插到 M1.6.2 树的对应位置即可。

### 6.2 验证

```bash
colcon build --merge-install --symlink-install --packages-select s622_bt_manager
source install/setup.bash

ros2 launch s622_bt_manager single_arm_pick_place_bt.launch.py \
  tree_file:=pick_place_full_tree.xml tree_id:=PickPlaceFull
```

**重点观察**：

* pick 后 RViz 中 cube attach 上 EE
* lift→safe 期间 cube 跟随
* pre_place 规划路径**不应该穿过 table**（之前不带 attach 可能穿过）
* place 后 cube 落在 `(0.35, -0.18, 0.03)`
* home 回去后 cube 留在 place 位置

### 6.3 验收

连续 5 次：

* ≥ 60% 成功率（3/5）
* Recovery 不强制（M1.7 做）

---

## 7. M1.6.5：CSV 日志 + 10 次验收（1h）

### 7.1 轻量日志：BT executor 末尾打印 + bash 抓

不引入完整 logging 框架，简化做法：

在 `bt_executor_node.cpp` 的 tree tick 完成处，无论 SUCCESS/FAILURE，打印固定前缀的一行日志：

```cpp
// 假设原来的 tick loop 是这样
auto status = tree.tickWhileRunning();
auto end_time = node->now();
double duration = (end_time - start_time).seconds();
RCLCPP_INFO(node->get_logger(),
            "[TRIAL_RESULT] status=%s duration=%.2f",
            (status == BT::NodeStatus::SUCCESS ? "SUCCESS" : "FAILURE"),
            duration);
```

跑 10 次：

```bash
# 启动一次 BT executor，然后循环发触发
for i in $(seq 1 10); do
    echo "=== trial $i ==="
    ros2 topic pub /bt_trigger std_msgs/msg/Bool "data: true" --once
    # 等待这一轮结束（按平均 60s 估）
    sleep 90
done
```

抓取日志到 CSV：

```bash
# 假设 BT 输出在 /tmp/bt_log.txt（用 tee 截取）
ros2 launch s622_bt_manager single_arm_pick_place_bt.launch.py \
  tree_file:=pick_place_full_tree.xml tree_id:=PickPlaceFull \
  2>&1 | tee /tmp/bt_log.txt &

# 跑完后提取
grep "\[TRIAL_RESULT\]" /tmp/bt_log.txt \
  | awk '{print NR","$2","$3}' \
  > experiments/results/phase1_m1_6_trials.csv
```

CSV 输出（示例）：

```
1,status=SUCCESS,duration=68.45
2,status=FAILURE,duration=22.10
...
```

### 7.2 验收

| 指标             | 标准                           |
| ---------------- | ------------------------------ |
| 连续 10 次       | 完成（中途允许重置 cube 位置） |
| 整体成功率       | ≥ 60%（6/10）                  |
| CSV 文件         | 10 行存在                      |
| RViz attach 视觉 | 全程正确（手动观察）           |

如果 < 60%：分析失败节点分布，找最高频的失败点处理。常见可能：

* `MoveToPose pre_place` 频繁失败 → place_pose 选择有问题
* `VisualAlign descend` 失败 → 不归 M1.6 管，留 M1.7
* `MoveToPose home (place 后)` → safe 中转值不够好

---

## 8. 已知坑预警

1. **rosidl 改了 srv 必须 `rm -rf build install/s622_bt_manager`** —— M1.6.3 起步时容易踩
2. **YAML 全 float**：`pick_place_poses.yaml` 里 `0.030` 不能写 `0.03`（虽然实际 ROS yaml parser 一般能接受，但保持习惯避免 corner case）
3. **`/planning_scene` topic QoS**：必须 `TRANSIENT_LOCAL`，否则 MoveIt 启动晚于 service 时拿不到 table
4. **finger link 名一定要查准**：写错 `touch_links`，attach 后 close gripper 会立即触发自碰撞，planning 全挂
5. **AttachObject 的 `pose_in_link`**：`offset_z=0.02` 是经验值，实际看 `grasp_frame` 是在指尖之间还是在手掌中央。M1.6.3 验证时 RViz 看 cube 位置和真实指间位置对不上就调这个值
6. **DetachObject `put_back_in_world=true`**：drop_pose 是 base_link 系，不是 link 系。BT 端 PoseStamped 传进来，cpp 里 `.pose` 已经是 base 系（前提是 PoseStamped.header.frame_id == base_link，GeneratePlaceCandidate 已经这样）
7. **safe pose 在 named_pose 上工作的前提是 `arm_config.yaml` 里 `named_pose_names` 列表包含 `safe`** —— 你这里已经包含了，没问题
8. **table_center 的 z**：写 `-0.011`，确保 table 顶面在 `z=0`（table_z），与抓取参数一致。table_size z=0.02，半厚 0.01

---

## 9. 交付物清单

完成 M1.6 后应该有：

```
s622_bt_manager/
├── srv/
│   ├── AttachObject.srv          # 新
│   └── DetachObject.srv          # 新
├── behavior_trees/
│   ├── safe_pose_test_tree.xml             # 新 (M1.6.0)
│   ├── place_reachability_test_tree.xml    # 新 (M1.6.1)
│   ├── pick_place_no_attach_tree.xml       # 新 (M1.6.2)
│   ├── attach_detach_test_tree.xml         # 新 (M1.6.3)
│   └── pick_place_full_tree.xml            # 新 (M1.6.4)
├── config/
│   └── pick_place_poses.yaml     # 新
├── include/s622_bt_manager/
│   └── scene_nodes.hpp           # 新
└── src/
    └── scene_nodes.cpp           # 新

s622_arm_actions/
└── s622_arm_actions/
    └── planning_scene_service.py # 新

experiments/results/
└── phase1_m1_6_trials.csv        # 新

arm_config.yaml: safe_joint_positions 调整为实测值
moveit_planner.py: 已加 num_planning_attempts=10 + allowed_planning_time=5.0
```

---

需要我现在就开始 M1.6.0 还是先看你对这份流程的反馈（特别是 §5.4 server 的实现风格 / §5.5 BT 节点 port 设计 / §7 日志方案这几处）？
