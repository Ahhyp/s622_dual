# M4 存量审计与执行记录（2026-09-04）

> 对应《M4_双臂松耦合交接规划.md》§3/§18 第 1 步（冻结基线）与第 2~6 步前置审计。
> 原则：**能复用一律复用**，只补真缺口。

## 1. 基线冻结

```text
M2.7_BASELINE = master @ 38530ec   （M2.7 全部定稿 + 10 项回归测试，含 m2.7-sim-final 语义）
M4_BASELINE   = tag m4-baseline @ 38530ec（与 M2.7_BASELINE 相同：M2.7 冻结后未再动标定/单臂主流程）
M4_START      = 本目录首个 M4 提交（自 m4-baseline 之后）
```

冻结口径：除非 M4 测试证明存在标定问题，否则不再同步修改手眼标定与单臂抓取主流程。

## 2. 存量代码审计（对照 M4 章节）

| M4 需求 | 现状（仓库内） | 复用决定 |
|---|---|---|
| §4.1 `dual_arm` 12-DOF group | `s622_dual_arm.srdf`：`dual_arm` 组 = left_arm + right_arm（12 关节）；含 `dual_home`/`dual_handover_ready` 命名位姿 | ✅ 复用 |
| §4.1 Left↔Right 碰撞 | SRDF 仅单臂内部 disable_collisions，**无** 左右互 disable → inter-arm 碰撞检查默认生效 | ✅ 复用（需运行时 T0 验证） |
| §4.1 人工碰撞测试 | 无现成记录 | ➕ 运行时执行并记录 |
| §4.2 DualMove 执行 | `s622_arm_actions/.../dual_move_server.py`：ompl/RRTConnect 12-DOF 联合规划 → 拆分左右 6-DOF 子轨迹（保留同一 time_from_start）→ 双线程并行发左右 FollowJointTrajectory | ✅ 复用（无 Δt_start/Δt_end 统计，后期按需加） |
| §5 固定 Handover Pose | `dual_handover_ready` 关节值在 SRDF 与 `dual_arm_config.yaml` 一致（dual_move_server 从参数读，服务 `/dual/move_to_joint_state`） | ✅ 复用（重复性 20 次记录待执行） |
| §6 最小 Handover 流程 | `pick_handover_place.xml`：Pick(左)→DualMove(handover)→RightClose→TransferObject→LeftOpen→…，**缺 VerifyRightGrasp**、缺 ownership、Recovery 为通用式 | ⚠️ 部分复用：六步闭环语义需重构出纯 handover 子树（T1/T2 用） |
| §7 VerifyRightGrasp | `gripper_nodes.cpp VerifyGraspNode`：三段式（EMPTY/MISS/GRASPED），最少 1.5s 稳定等待，读 finger1_joint | ✅ 复用（左右阈值分开标定：7.1 实验待做） |
| §8 Ownership | **无任何 object_owner 状态** | ➕ 新写（blackboard + 小节点） |
| §9 TransferObject | `planning_scene_service.py _on_transfer`：单次 diff 原子 detach+attach，无中间窗口；`scene_nodes.cpp TransferObjectNode` | ✅ 复用（运行验收 T2 做） |
| §10 Ownership-aware Recovery | `pick_handover_place.xml` Recovery = Stop 双 servo → 双开 → 删 cube → 回 home（**反模式**） | ➕ 重构为按 owner 分支 |
| §11 BT 重构 | BT executor 按 `tree_file/tree_id` 参数加载任意 XML；Blackboard 已有 `{arm}` 等 | ✅ 复用框架 |
| §12 Stabilization | `dummy_nodes.cpp` 已注册 `Wait`（sleep 秒） | ✅ 复用（200~300ms dwell） |
| §13 Inter-arm Distance Monitor | 无 | ⏸ 按需（M4 前段不做） |
| §14 测试阶梯 T0~T5 | 无实验框架 | ➕ 新建（运行脚本 + CSV 记录） |
| §15 Failure Injection | 无 | ⏸ T3 阶段做 |
| §16/§17 实验记录/指标 | BT 已打印 `[TRIAL_RESULT]`，无 CSV | ➕ 补 CSV runner |

## 3. 复用清单（明确不重写）

- `dual_move_server.py`（12-DOF 规划 + 拆分 + 并行执行）
- `planning_scene_service.py` 的 attach/detach/transfer 服务（TransferObject 已是原子单 diff）
- `VerifyGrasp` / `SetGripper` / `AttachObject` / `DetachObject` / `TransferObject` / `DualMoveToJointState` / `MoveToPose` / `Wait` / `PrintMessage` / `CheckSystemReady` BT 节点
- `s622_dual_arm.srdf`、`dual_arm_config.yaml`（含命名位姿）、`s622_dual_arm.launch.py` 双臂环境
- `dual_handover_ready` 固定交接构型（等 T1 重复性数据，不因失败率预判而改）

## 4. 差距清单（需新增）

| 编号 | 内容 | 对应 M4 | 实现方式 |
|---|---|---|---|
| G1 | ownership 状态：`object_owner` 写入/断言 | §8/§10/§11 | **已验证** BT.CPP 4.9 builtin `Script`/`ScriptCondition`/`SetBlackboard`（`:=` 赋值、`==`/`&&`/`!`、`if…then…end` 均可用）→ **零新增 C++**，纯 XML：`handover_min.xml` |
| G2 | 纯 handover BT（固定 cube 六步闭环，无视觉） | §6/§11 | 新 XML：DualMove(handover)→dwell→RightClose→VerifyRightGrasp→SetOwner(BOTH)→Transfer→SetOwner(RIGHT)→LeftOpen→DualRetreat，全程 timeout |
| G3 | handover 顺序约束：Transfer 前 RequireOwner(LEFT)；LeftOpen 前 RequireOwner(RIGHT)+transfer_success | §4.4/§9 | 复用 RequireOwner 断言节点 |
| G4 | ownership-aware Recovery（替换通用 Recovery） | §10 | BT 内按 owner 分支（LEFT/BOTH_PHYSICAL/RIGHT/UNKNOWN） |
| G5 | 实验 runner + CSV（§16 字段） | §14/§16 | Python runner 驱动 BT trigger + /joint_states + finger 采样 |
| G6 | Right 夹爪阈值标定（empty×20 / grasp×20） | §7.1 | 复用 VerifyGrasp + 记录脚本 |
| G7 | planning_scene touch_links 按臂过滤 | §8 语义 | planning_scene_service 内小改（default_touch_links 按 link 前缀过滤）——现 launch 给 6 个双臂链接混用，LEFT 归属期间右指也"触物"，过宽 |

## 5. 执行顺序（对齐 §18，前段无视觉）

```text
[✅] 1 冻结 M2.7 baseline（本文件 + tag m4-baseline）
[✅] 预  G7 已修（planning_scene_service touch_links 按臂前缀过滤，LEFT 归属不再含右指）
[✅] 预  G1~G4 → behavior_trees/handover_min.xml（六步闭环 + Script 门控 + ownership-aware recovery，XML 已过语法/节点校验，待运行时 T2 验证）
[✅] 2 T0 验收 dual_arm / SRDF 碰撞（12-DOF 规划 0.1s OK；人工互穿 goal → PLANNING_FAILED，无 成功+互穿）
[✅] 3 验收 DualMove 执行（30 趟往返全 SUCCESS；Δt_start≈0.13~0.27s 参考值，非硬指标）
[✅] 4 Handover Pose 重复性（30 次进入，^L T_R ≤1.6mm / 方向<0.003）
[✅] 5 T1 无物体往返 ×30（Collision=0）
[⚠️] 6 最小 handover BT 干跑：主序列+Recovery 调度验证 OK；`and`→`&&` 已修待重跑；
      RightClose 被 7.7 f1 infra bug 阻塞（重跑前需先解 f1）
[  ] 7 加入 Right VerifyGrasp（G6 阈值）——依赖 f1 修复
[  ] 8 T2 固定 cube 纯 Handover ×30（≥28 success，Drop=0）——依赖 f1 修复
[  ] … 后续按规划 §18 继续
```

## 6. 观察项（运行时重点核实）

- O1：planning_scene touch_links 当前为双臂 6 链接混合（G7），LEFT 归属期间右指也被视为 touch——先修后测，避免掩盖交接路径碰撞问题。
- O2：`pick_handover_place.xml` 现有 RightClose 用 `command="close"`（无 finger_position），右指全闭。需 T2 验证右夹 cube 时 VerifyGrasp 区间 [min,max] 是否落在默认带 [0.005, 0.022]。
- O3：sim 主机 RTF 低（~0.1-0.2），每轮试验耗时长；正式 100 次留参数冻结后批量跑。

## 7. 运行时进度（2026-09-04）

### 7.1 仿真环境
- `s622_dual_arm.launch.py` 完整启动 OK（move_group_fairino/kdl、servo×2、dual_move_server、
  planning_scene_service、bt_executor、retime、RViz、gz GUI）。
- 按 M2.7 经验杀掉 `obb_node`（T0~T2 无视觉需求）；保留 gz GUI + RViz（完整 GUI 模式，用户选定）。
- 注意：双臂关节在控制器接管前由 gz 占位值（j≈(0.5,-1.05,1.05,-1.05,-0.8)）跳到
  controller 初始位 = dual_home（0,-1.5708,0,…）；以 /joint_states 为准。

### 7.2 T0 规划验收（12-DOF dual_arm group）
- plan-only 探测（ompl/RRTConnect，15 attempts/10s）：当前位 → `dual_home` error_code=1（2 pts）；
  当前位 → `dual_handover_ready` error_code=1（88 pts），wall ≈0.1s。**12-DOF 联合规划可用**。
- **人工碰撞测试 PASS**（t0b_v3，基于 handover 构型）：
  - P1 home→handover 规划 OK（88 pts）；
  - FK：`right_grasp_frame`@handover world ≈ (-0.020, 0.200, 0.351)；
  - 左臂 IK 到右 TCP 位姿可达（avoid_collisions=False 纯可达解）；
  - P2（左 TCP 送入右爪同点位，right 保持 handover）→ **error_code=-2 PLANNING_FAILED**。
  - 结论：互穿 goal 被规划器拒绝，**未出现"规划成功+轨迹互穿"**。M4.1 Gate 通过。
- 教训（本机 DDS 无 SHM 慢发现）：同一 node 提前创建 compute_fk/ik client 会令随后
  plan_kinematic_path 调用超时；规避 = 以 plan_probe 骨架先做 plan 再懒建 fk/ik client，
  或每个 client 用前 wait_for_service。另规划请求必须带 velocity/acceleration scaling
  （默认 0.0 会让 MoveIt 卡住）。

### 7.3 T1 无物体往返
- v1/v2 共 15 趟全 SUCCESS，wall ≈48-53s/趟/向。
- ^L T_R @ handover_ready（right TCP in left_grasp_frame，mm）15 样本：
  x∈[-0.54,0.66]（1.2mm）、y∈[-1.30,-0.38]（0.92mm）、z∈[40.28,41.04]（0.76mm），
  方向≈单位四元数（偏差<0.003）→ **sub-mm 重复性**（M4.2 目标：不依赖手眼，只观察重复性——已达标量级）。
- 交接几何：左右爪沿各自 z（≈world Y）相距 ≈40mm，cube（40mm）置中于两爪之间。
- **同步差 Δt**（v3，基于 /joint_states 运动检测；action status topic 本机 QoS/DDS 异常无法订阅，
  已用可靠替代源）：Δt_start ≈ 0.18~0.27s；Δt_end 多数未能检出（到位阈值 0.2° 在慢 RTF 下不稳）。
  注：M4.2 工程目标（start<50ms/end<100ms）为**非硬指标**；本机 RTF≈0.1 下启动差含
  控制器受理延迟与物理慢放，记录为参考值，不为此重构 controller。
- T1 共 18+12 趟（30 次往返，含 30 个 ^L T_R 样本）全部 SUCCESS，无碰撞 → **T1 Gate（30 次 Collision=0）PASS**；
  ^L T_R 全样本：x∈[-0.92,0.66]、y∈[-1.50,-0.04]、z∈[40.15,41.13] mm（≈1.6mm 内），方向偏差<0.003。
- FK 几何：handover 下 left_grasp=world(0.020,0.200,0.351)、right_grasp=(-0.020,0.200,0.351)，
  两爪沿 X 距 40mm（cube 置中），z=0.351 → 物体离桌面 ~35cm ✓。base：left(0.35,0,0)yaw=π、right(-0.35,0,0)。

### 7.6 handover_min BT 空载干跑（无 cube）
- 树加载/触发 OK；流程验证：CheckSystemReady→Script init→owner=LEFT gate→DualMove(handover) SUCCESS(~50s)→
  Wait dwell→RightClose **失败**→主序列 FAIL→Recovery 进入：StopServo 双 OK→OwnerDispatch 按序尝试
  BOTH/RIGHT/UNKNOWN guard 全 FAIL→LEFT 分支执行中 **崩溃**。
- 崩溃根因：BT++ 脚本**不支持 `and`**（需 `&&`），XML 3 处已修（`&amp;&amp;`，离线 test5 语义验证通过，
  已拷入 install）。恢复分支后续（RIGHT/UNKNOWN/LEFT 动作）待重跑验证。
- RightClose 失败根因：见 7.7 infra bug —— 与 f1 不动直接相关（MoveIt 给 f1 生成退化轨迹：
  `right_hand_controller: Time between points not strictly increasing` → execute 被拒 → gripper close FAILED）。
  直接调 hand controller 动作可用（open/close 返回 SUCCESS，f2 正常运动）。

### 7.7 🚨 infra bug：双臂仿真 finger1_joint 完全不动（阻塞 T2+ 抓取）
- 现象：left/right_finger1_joint 恒为 ≈0（其下极限 -1e-6 处），对任何 position 命令无响应；
  finger2_joint 正常（open→-0.025 / close→0，RTF 慢属正常）。gz 侧 joint_state 证实 f1 物理上未动
  （非 ros2_control 状态读取问题）。
- 排查已排除：硬件接口齐全（ros2 control list_hardware_interfaces 四指 position 均 available+claimed）；
  左右组件名已自动加前缀（left_/right_GazeboSimSystem，无同名冲突）；与位姿无关（home/handover 均不动）；
  与摩擦 mu1/mu2=500 疑似相关但 f2 同配置能动 → 矛盾，根因未定（疑 gz 侧 f1 joint PID/limit 层）。
- 影响：f1=0 = 该侧夹爪永久闭合 → cube 无法进入爪内 → 左拾取/右咬均不可行 → **M4 T2/T4 及任何抓取依赖阻塞**。
- 连带：VerifyGrasp 读 `{arm}_finger1_joint`（恒 0 → 永远判 EMPTY），历史日志无任何 GRASPED 记录
  （单臂时代是否真通过存疑，M1.7 未正式收尾一致）。
- 待决策（见会话）：深修 gz f1（改 xacro 摩擦/kp/effort 或 gz_ros2_control 行为，需多轮 relaunch 试验）
  vs 换 feedback 到 f2+镜像阈值（单活动爪能否夹持 cube 存疑）vs 先确认单臂仿真 f1 是否同样故障。

### 7.7b 单臂对照实验结论（2026-09-04，用户选定方向）
- **单臂仿真（s622_gazebo.launch.py）finger1_joint 正常**：双指轨迹（f1→0.030, f2→-0.030，5s sim）
  被接受执行，f1 全程采样 0→0.0297 到达目标（RTF 慢属正常）。单/双臂摩擦配置相同（mu 500/kp 30000/kd 15）。
- 双臂右 f1 不动的证据为**已接受且执行中的轨迹**（finger_live 双指轨迹：f2 爬行、f1 每帧恒 0）——
  非目标被拒（此前 f1_solo 单指目标被 controller 拒绝"Joints don't match"，那些测试无效，已排除）。
- 双臂左 f1 本会话从未被命令（无轨迹发出），其 ≈0 只是初始闭合态——**范围未定（左 f1 可能也坏）**。
- 推断：问题在**双臂双硬件组件 gz_ros2_control 架构**（单臂单组件正常）：双臂关节用 velocity
  接口（正常执行），手指用 position 接口（右 f1 失效）——疑似插件对第二组件 position 写入/实体映射缺陷。
- 下一步：深修双臂 ros2_control/插件结构（多轮 relaunch 试验），或先补测双臂左 f1 界定范围。

### 7.7c 🎯 f1 故障未复现（新会话重启双臂仿真后）（2026-09-04）
- **重启双臂仿真（同 launch、同配置）后：左右 f1/f2 全部正常**——双指轨迹被接受执行，
  f1 对称跟踪 0→0.0275（右 27s、左 27s 采样），与单臂行为一致。
- 4 趟双臂往返（4×dual move）后复测右 f1 仍正常 → 上会话 f1 不动为**会话性/瞬时故障**（配置层面
  无法复现，7.7/7.7b 的"双组件架构缺陷"推断不成立，配置本身是好的）。
- 处置：会话性异常告一段落；后续抓取依赖试验前加 **f1 自检**（双指轨迹快速探测）作保险。

### 7.8 handover_min BT 干跑重跑（`&&` 修复后）— PASS
- 主序列：init(owner=LEFT) → DualMove(handover) SUCCESS → Wait → RightClose SUCCESS(f1=0.0013 空夹)
  → **VerifyGrasp 正确判 EMPTY FAIL**（无 cube，Left 未释放，M4.4 语义 ✓）→ 主序列中止。
- Recovery：StopServo ×2 → OwnerDispatch 四 guard 依次 FAIL/FAIL/FAIL/SUCCESS（`&&` 生效无崩溃）→
  RecoverLeft：RightOpen(f1=0.0235) → DualMove(home) SUCCESS → Recovery DONE → AlwaysFailure →
  整树 FAILURE，`[TRIAL_RESULT] status=FAILURE`。**失败路径全链路验证通过**。

### 7.9 Right 空夹阈值标定（§7.1 empty 组）— 20/20
- 20 次 open→close（无 cube）：f1 ∈ [0.00058, 0.00148]（均值≈0.0009）、f2 ∈ [-0.0015, -0.0004]。
- VerifyGrasp EMPTY 阈值 finger_min_position=0.005：空夹上限 0.0015 < 0.005（裕量 >3×）✓。
- grasp 组（夹 40mm cube 的 f1 分布）待 T2 固定 cube 试验采集。

### 7.10 🚨 左臂定点拾取几何调查（T2 前置，2026-09-04）
- 机械链验证：MoveToPose(pregrasp)→VisualAlign descend(开环 z)→SetGripper close 全链路**机械可用**
  （pregrasp 0.1s 规划 OK、descend 到 z=0.0515、close 成功）。
- **但顶部下压式抓取几何不成立**：
  - 目标 cube world(-0.15, 0, 0.02)（40mm 立方，桌面 z=0）；左臂 TCP 定位到其正上方（XY 偏差<0.5mm）；
  - M1.x 约定姿态 rpy(π,0,0) 下，IK(avoid_collision) 最低可达 TCP z≈0.05（z<0.05 全 -31）；
  - TF 实测：**手指 pad 在 TCP 上方 ~50mm**（grasp_frame 在 wrist3 下 0.2168，finger 关节在 wrist3 下 0.166
    → pads 恒在 grasp_frame 上方）；TCP z=0.0515 时 pads 世界 z∈[0.041,0.083]，**整段高于 cube 顶(0.04)**
    → 闭合 EMPTY（f1=0.0007，实测确认）。
  - 即：顶部下压 + 该 gripper 模型 → pads 永远够不到桌面 cube（IK 又不让 TCP 更低）。
- 历史佐证：全仓库**无任何 VerifyGrasp GRASPED 记录**——单臂视觉抓取（M1.x，rpy(π)@z=0.03 约定）很可能
  在此仿真里从未真正夹起过 cube（与 M1.7 未正式收尾相互印证）。
- 待用户方向：真实机器人/robotarm 的抓取姿态约定？还是改物块/桌高/侧向抓取实验？（见会话）
- 现场状态：双臂 dual_home、左爪曾闭/状态需复位、cube 原位未动。

### 7.11 ✅ 左臂真实抓取打通 + 🚨 handover 构型几何缺陷（2026-09-04 续）
**7.11a 抓取机制打通（重大进展）**
- 场地真相：dual world **无实体桌**（M2.7 去桌场景，地面 z=0 即"桌面"）；MoveIt 的 table 仅碰撞对象。
- 抓取配方（左臂，40mm cube 于地面）：平面 rpy(π,0,0) pregrasp（z 0.18，MoveToPose 左 base 系 OK）→
  **开环 servo descend 到 TCP z≈0.0365**（低于 IK 规划下限 0.05，但 servo 不受限）→ close →
  **f1=0.0056 GRASPED** → lift 0.10 成功（cube z 0.02→0.145 随爪升起）→ 全程（含多次双臂往返）cube 保持被抓。
- pads 几何修正：pads 横跨 TCP 上下（底缘 TCP-11mm、顶缘 +31mm）；TCP z=0.0515 时 pad 底缘 0.041 距
  cube 顶 0.04 只差 1mm → 需 TCP≈0.037 才能咬住 cube 上部（此前 7.10"pads 恒在上方 50mm"表述不精确，
  实为 pads 以 TCP 为中心 ±(11..31)mm；抓不到是"TCP 差 ~14mm"而非结构性不可能）。

**7.11b 🚨 交接构型几何缺陷（dual_handover_ready 需调整）**
- 交接位实测（左右爪同 y≈0.2、z≈0.35，grasp frame 沿 X 距 40mm）：
  - 左 pad 列 x∈[0.010,0.051]（中心 0.0305）；右 pad 列 x∈[-0.051,-0.009]（中心 -0.030）；
  - **两 pad 列相距 60mm、中间 19mm 空隙** → 40mm cube 不可能同时落入两爪咬合区。
  - 左持 cube 时 cube 中心 = 左 pad 中心 ≈0.030；右闭合（沿 Z 向咬合，pad 列不变）落空 → 实测右 f1=0.001 EMPTY。
- 右臂在交接位 +x 方向已到工作空间边界（IK ±3cm 均 -31，avoid on/off 同）；左臂可 -x 平移（IK 至 -0.055 OK）。
- **调整方案**：重定 `dual_handover_ready` —— 左臂沿 -x 移 ~60mm（左 grasp world x 0.020→-0.040，
  cube 中心 → 右 pad 列 -0.030），右臂不动；更新 SRDF/dual_arm_config.yaml 关节值后按 T1 流程复验。
- 工具教训（防再踩坑）：本套 MoveIt compute_ik **frame 语义不可靠**（左臂偶发把值当 world/当 left_base，
  导致镜像偏移）；`MoveToPose` server 对 left_base 处理正确但 fairino 管线对非常规位姿易规划失败；
  **ompl 关节空间（dual_move_server 模式）+ MoveToPose(平面) 是目前可靠执行器**。
- 清理项：误撞飞/残留 cube `m4_cube`@(0.46,0.27)（name 占用致 remove 失败，待清）；地面拾取点干净。

### 7.11c 🚨 handover 姿态需重新设计（咬合轴冲突，2026-09-04 收尾）
- 实测（TF pads 包络）：交接位**左右爪 pads 均沿 world Z（竖直）咬合**（左 f1 z0.362/f2 z0.336，
  右 f1 z0.374-0.410/f2 z0.306-0.341）→ 两爪都想上下夹 cube 顶/底，无法同时夹同一 40mm cube；
  且侧面 x 列仍错位（左列中心 0.030、右列中心 -0.030）。
- 右臂 +x 已到边界（IK -31，avoid on/off 同）；左臂 -x 平移与右爪碰撞（ompl pose-goal 99999）。
- MoveIt compute_ik/compute_fk 本环境 frame 语义不可靠（把值当 world/left_base 混乱解释，FK 校验也失真）→
  无法可靠"算"新位姿；可靠执行器仅 MoveToPose(平面/左base) 与 ompl 关节空间（dual_move_server）。
- **遗留设计任务（下次会话）**：重设计交接姿态——左爪以水平咬合轴（≈pick 姿态）夹 cube、右爪竖直咬合，
  两轴垂直互不干涉；需先用可靠手段确定真实咬合轴/指尖接触几何（网格角点法不可靠），再定双爪世界位姿、
  反解关节（可能需绕过 compute_ik，用 MoveToPose 逐步逼近或数值搜索）→ 更新 SRDF/config → 复验。
- 现场：双臂 dual_handover_ready、cube(m4_pick) 仍在左爪、右爪处于闭合态（下次先 open）；
  残留 m4_cube@(0.46,0.27)。

### 7.5 T2 固定 cube 纯 Handover 方案（30 次，T2 前不接视觉）
每轮由 python orchestrator 驱动：
1. Left 从固定桌面点无视觉拾取：MoveToPose(pregrasp, 左系固定位姿) → VisualAlign descend（开环 z 伺服）→
   SetGripper close → VerifyGrasp → AttachObject(left) → VisualAlign lift（复用 M1.x 单臂套路，去掉 YOLO/xy/yaw）。
2. 触发 `handover_min.xml`（HandoverMin 树）：DualMove(handover)→dwell→RightClose→VerifyRightGrasp→
   Transfer→LeftOpen→DualRetreat（ownership 门控，失败走 ownership-aware recovery）。
3. Right 把 cube 放回固定桌面点（MoveToPose place + open + DetachObject put_back_in_world）→ 进入下一轮。
- 前置：校准固定拾取点（cube world 坐标 + 左臂 pregrasp/grasp 位姿，一次）并验证拾取成功率。

### 7.4 代码侧已提交项
- G7：`planning_scene_service.py` 增加 `_arm_prefix_of_link`/`_touch_links_for`（默认 touch_links
  按 attach/transfer 目标 link 的臂前缀过滤；req.touch_links 显式传入时仍优先）。
- G1~G4：`behavior_trees/handover_min.xml`（BT ID=HandoverMin，六步闭环 + Script 门控 +
  ownership-aware recovery；XML 已过 well-formed 与节点 ID 校验，待运行时验证）。
