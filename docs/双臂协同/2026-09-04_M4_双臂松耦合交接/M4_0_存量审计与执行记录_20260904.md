# M4 存量审计与执行记录（2026-09-04）

> 对应《M4_双臂松耦合交接规划.md》§3/§18 第 1 步（冻结基线）与第 2~6 步前置审计。
> 原则：**能复用一律复用**，只补真缺口。

## 1. 基线冻结

```text
M2.7_BASELINE = master @ 38530ec   （M2.7 全部定稿 + 10 项回归测试，含 m2.7-sim-final 语义）
M4_BASELINE   = tag m4-baseline @ 38530ec（与 M2.7_BASELINE 相同：M2.7 冻结后未再动标定/单臂主流程）
M4_START      = 本目录首个 M4 提交（自 m4-baseline 之后）
```

冻结口径：除非 M4 测试证明存在标定问题，否则不再同步修改手眼标定与单臂抓取主流程。

## 2. 存量代码审计（对照 M4 章节）

| M4 需求 | 现状（仓库内） | 复用决定 |
|---|---|---|
| §4.1 `dual_arm` 12-DOF group | `s622_dual_arm.srdf`：`dual_arm` 组 = left_arm + right_arm（12 关节）；含 `dual_home`/`dual_handover_ready` 命名位姿 | ✅ 复用 |
| §4.1 Left↔Right 碰撞 | SRDF 仅单臂内部 disable_collisions，**无** 左右互 disable → inter-arm 碰撞检查默认生效 | ✅ 复用（需运行时 T0 验证） |
| §4.1 人工碰撞测试 | 无现成记录 | ➕ 运行时执行并记录 |
| §4.2 DualMove 执行 | `s622_arm_actions/.../dual_move_server.py`：ompl/RRTConnect 12-DOF 联合规划 → 拆分左右 6-DOF 子轨迹（保留同一 time_from_start）→ 双线程并行发左右 FollowJointTrajectory | ✅ 复用（无 Δt_start/Δt_end 统计，后期按需加） |
| §5 固定 Handover Pose | `dual_handover_ready` 关节值在 SRDF 与 `dual_arm_config.yaml` 一致（dual_move_server 从参数读，服务 `/dual/move_to_joint_state`） | ✅ 复用（重复性 20 次记录待执行） |
| §6 最小 Handover 流程 | `pick_handover_place.xml`：Pick(左)→DualMove(handover)→RightClose→TransferObject→LeftOpen→…，**缺 VerifyRightGrasp**、缺 ownership、Recovery 为通用式 | ⚠️ 部分复用：六步闭环语义需重构出纯 handover 子树（T1/T2 用） |
| §7 VerifyRightGrasp | `gripper_nodes.cpp VerifyGraspNode`：三段式（EMPTY/MISS/GRASPED），最少 1.5s 稳定等待，读 finger1_joint | ✅ 复用（左右阈值分开标定：7.1 实验待做） |
| §8 Ownership | **无任何 object_owner 状态** | ➕ 新写（blackboard + 小节点） |
| §9 TransferObject | `planning_scene_service.py _on_transfer`：单次 diff 原子 detach+attach，无中间窗口；`scene_nodes.cpp TransferObjectNode` | ✅ 复用（运行验收 T2 做） |
| §10 Ownership-aware Recovery | `pick_handover_place.xml` Recovery = Stop 双 servo → 双开 → 删 cube → 回 home（**反模式**） | ➕ 重构为按 owner 分支 |
| §11 BT 重构 | BT executor 按 `tree_file/tree_id` 参数加载任意 XML；Blackboard 已有 `{arm}` 等 | ✅ 复用框架 |
| §12 Stabilization | `dummy_nodes.cpp` 已注册 `Wait`（sleep 秒） | ✅ 复用（200~300ms dwell） |
| §13 Inter-arm Distance Monitor | 无 | ⏸ 按需（M4 前段不做） |
| §14 测试阶梯 T0~T5 | 无实验框架 | ➕ 新建（运行脚本 + CSV 记录） |
| §15 Failure Injection | 无 | ⏸ T3 阶段做 |
| §16/§17 实验记录/指标 | BT 已打印 `[TRIAL_RESULT]`，无 CSV | ➕ 补 CSV runner |

## 3. 复用清单（明确不重写）

- `dual_move_server.py`（12-DOF 规划 + 拆分 + 并行执行）
- `planning_scene_service.py` 的 attach/detach/transfer 服务（TransferObject 已是原子单 diff）
- `VerifyGrasp` / `SetGripper` / `AttachObject` / `DetachObject` / `TransferObject` / `DualMoveToJointState` / `MoveToPose` / `Wait` / `PrintMessage` / `CheckSystemReady` BT 节点
- `s622_dual_arm.srdf`、`dual_arm_config.yaml`（含命名位姿）、`s622_dual_arm.launch.py` 双臂环境
- `dual_handover_ready` 固定交接构型（等 T1 重复性数据，不因失败率预判而改）

## 4. 差距清单（需新增）

| 编号 | 内容 | 对应 M4 | 实现方式 |
|---|---|---|---|
| G1 | ownership 状态：`object_owner` 写入/断言 | §8/§10/§11 | blackboard `object_owner`（NONE/LEFT/BOTH_PHYSICAL/RIGHT/UNKNOWN）+ `SetOwner`/`RequireOwner` 小节点（或用 BT.CPP builtin Script/ScriptCondition） |
| G2 | 纯 handover BT（固定 cube 六步闭环，无视觉） | §6/§11 | 新 XML：DualMove(handover)→dwell→RightClose→VerifyRightGrasp→SetOwner(BOTH)→Transfer→SetOwner(RIGHT)→LeftOpen→DualRetreat，全程 timeout |
| G3 | handover 顺序约束：Transfer 前 RequireOwner(LEFT)；LeftOpen 前 RequireOwner(RIGHT)+transfer_success | §4.4/§9 | 复用 RequireOwner 断言节点 |
| G4 | ownership-aware Recovery（替换通用 Recovery） | §10 | BT 内按 owner 分支（LEFT/BOTH_PHYSICAL/RIGHT/UNKNOWN） |
| G5 | 实验 runner + CSV（§16 字段） | §14/§16 | Python runner 驱动 BT trigger + /joint_states + finger 采样 |
| G6 | Right 夹爪阈值标定（empty×20 / grasp×20） | §7.1 | 复用 VerifyGrasp + 记录脚本 |
| G7 | planning_scene touch_links 按臂过滤 | §8 语义 | planning_scene_service 内小改（default_touch_links 按 link 前缀过滤）——现 launch 给 6 个双臂链接混用，LEFT 归属期间右指也"触物"，过宽 |

## 5. 执行顺序（对齐 §18，前段无视觉）

```text
[✅] 1 冻结 M2.7 baseline（本文件 + tag m4-baseline）
[  ] 2 T0 验收 dual_arm / SRDF 碰撞（含人工穿臂测试）
[  ] 3 验收 DualMove 执行（记录 Δt 起止）
[  ] 4 Handover Pose 重复性（20 次进入，记录 ^L T_R）
[  ] 5 T1 无物体往返 ×30（Collision=0）
[  ] 6 最小 handover BT（G1~G4 落地）
[  ] 7 加入 Right VerifyGrasp（G6 阈值）
[  ] 8 T2 固定 cube 纯 Handover ×30（≥28 success，Drop=0）
[  ] … 后续按规划 §18 继续
```

## 6. 观察项（运行时重点核实）

- O1：planning_scene touch_links 当前为双臂 6 链接混合（G7），LEFT 归属期间右指也被视为 touch——先修后测，避免掩盖交接路径碰撞问题。
- O2：`pick_handover_place.xml` 现有 RightClose 用 `command="close"`（无 finger_position），右指全闭。需 T2 验证右夹 cube 时 VerifyGrasp 区间 [min,max] 是否落在默认带 [0.005, 0.022]。
- O3：sim 主机 RTF 低（~0.1-0.2），每轮试验耗时长；正式 100 次留参数冻结后批量跑。
