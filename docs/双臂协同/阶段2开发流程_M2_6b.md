## M2.6b: 方案 D 完整方案

**核心策略**:

* SRDF 加 `dual_arm` group 复合左右臂,加 `dual_home` group_state
* 新建 `DualMoveToJointState` action(在 s622_bt_manager 里,统一 action 目录)
* 新建 `dual_move_server.py` 在 `/dual` namespace,提供 `/dual/move_to_joint_state` action
* 内部走 `/plan_kinematic_path`(group=`dual_arm`)+ `/execute_trajectory` action, MoveIt 自动拆分下发到两个 controller

**不改动**:`MoveItPlanner` 类。dual_arm 没有单一 EE,和 MoveItPlanner 假设不匹配。dual_move_server 独立实现 planning+execute,复用 M2.6a 里的 sim-time wait 逻辑。

---

## 产出

### 1. `s622_dual_arm.srdf` 追加

在 right arm 段之后,注释"双臂之间 disable_collisions"之前插入:

```xml
    <!-- ==================== Dual Arm 复合 group (方案 D) ==================== -->
    <group name="dual_arm">
        <group name="left_arm" />
        <group name="right_arm" />
    </group>

    <group_state name="dual_home" group="dual_arm">
        <joint name="left_j1"  value="0" />
        <joint name="left_j2"  value="-1.5708" />
        <joint name="left_j3"  value="0" />
        <joint name="left_j4"  value="0" />
        <joint name="left_j5"  value="0" />
        <joint name="left_j6"  value="0" />
        <joint name="right_j1" value="0" />
        <joint name="right_j2" value="-1.5708" />
        <joint name="right_j3" value="0" />
        <joint name="right_j4" value="0" />
        <joint name="right_j5" value="0" />
        <joint name="right_j6" value="0" />
    </group_state>
```

**M2.8 前**再加 `dual_ready_handover` 等 group_state。

**关于 disable_collisions**:先不加双臂间的。初始 home 位姿两臂 base 在 (±0.35, 0, 0),bounding box 不重叠,MoveIt self-collision check 应该通过。M2.8 handover 时如遇 "两臂显然安全的 link 对被判碰撞",再针对性加。

### 2. 新建 `s622_bt_manager/action/DualMoveToJointState.action`

```
# Goal
string named_pose            # 优先, 从 dual_arm_config.yaml 读预设
float64[] left_positions     # 6 个, 若非空且 right_positions 也非空则覆盖 named_pose
float64[] right_positions    # 6 个
float32 velocity_scale
float32 acceleration_scale
float32 timeout_sec
---
# Result
bool success
int32 error_code
string error_msg
---
# Feedback
float32 progress             # 0.0 - 1.0 大致进度
```

### 3. `s622_bt_manager/CMakeLists.txt` 补 action

找到 `rosidl_generate_interfaces` 段,加上新 action:

```cmake
rosidl_generate_interfaces(${PROJECT_NAME}
  "action/MoveToPose.action"
  "action/VisualAlign.action"
  "action/DualMoveToJointState.action"    # ← 新增
  DEPENDENCIES geometry_msgs
)
```

### 4. 新建 `s622_arm_actions/s622_arm_actions/dual_move_server.py`

```python
"""Dual-arm 联合规划执行 server (方案 D).

规划: /plan_kinematic_path service, group='dual_arm', 12 joint 约束
执行: /execute_trajectory action, MoveIt 内部拆分下发到 left/right controller

用于 handover 等需要双臂时间同步的场景.
"""
import threading
import time
from typing import Dict, List, Optional

import rclpy
from rclpy.node import Node
from rclpy.action import ActionServer, ActionClient, GoalResponse, CancelResponse
from rclpy.callback_groups import ReentrantCallbackGroup
from rclpy.executors import MultiThreadedExecutor
from rclpy.duration import Duration

from sensor_msgs.msg import JointState
from moveit_msgs.srv import GetMotionPlan
from moveit_msgs.action import ExecuteTrajectory
from moveit_msgs.msg import (
    RobotState, Constraints, JointConstraint,
)

from s622_bt_manager.action import DualMoveToJointState


LEFT_JOINTS = [f'left_j{i}' for i in range(1, 7)]
RIGHT_JOINTS = [f'right_j{i}' for i in range(1, 7)]
ALL_JOINTS = LEFT_JOINTS + RIGHT_JOINTS


class DualMoveServer(Node):
    def __init__(self):
        super().__init__('dual_move_server')

        # ---- 参数 ----
        self.declare_parameter('group_name', 'dual_arm')
        self.declare_parameter('planner_id', 'RRTConnect')
        self.declare_parameter('pipeline_id', 'ompl')
        self.declare_parameter('default_velocity_scale', 0.2)
        self.declare_parameter('default_acceleration_scale', 0.2)
        self.declare_parameter('allowed_planning_time', 8.0)
        self.declare_parameter('num_planning_attempts', 10)

        # named poses: 每个 name 12 个数, 顺序 [left_j1..6, right_j1..6]
        self.declare_parameter('named_pose_names', ['dual_home'])
        # 各 pose 的 joint values 从各自 parameter 读, 参照 move_to_pose_server 的做法
        self._group_name = self.get_parameter('group_name').value
        self._planner_id = self.get_parameter('planner_id').value
        self._pipeline_id = self.get_parameter('pipeline_id').value
        self._default_v = self.get_parameter('default_velocity_scale').value
        self._default_a = self.get_parameter('default_acceleration_scale').value
        self._planning_time = self.get_parameter('allowed_planning_time').value
        self._planning_attempts = self.get_parameter('num_planning_attempts').value

        pose_names = list(self.get_parameter('named_pose_names').value)
        self._named_poses: Dict[str, List[float]] = {}
        for name in pose_names:
            param_name = f'{name}_joint_positions'
            try:
                self.declare_parameter(param_name, [0.0] * 12)
            except Exception:
                pass
            raw = self.get_parameter(param_name).value
            if len(raw) != 12:
                self.get_logger().error(
                    f'{param_name} must have 12 values (got {len(raw)}), skipping')
                continue
            self._named_poses[name] = [float(x) for x in raw]
        self.get_logger().info(
            f'loaded dual named poses: {list(self._named_poses.keys())}')

        # ---- 状态 ----
        self._latest_js: Optional[JointState] = None
        self._js_lock = threading.Lock()

        # ---- callback group + clients ----
        cb = ReentrantCallbackGroup()

        self._js_sub = self.create_subscription(
            JointState, '/joint_states', self._on_joint_state, 10,
            callback_group=cb)

        self._plan_cli = self.create_client(
            GetMotionPlan, '/plan_kinematic_path', callback_group=cb)
        self._exec_cli = ActionClient(
            self, ExecuteTrajectory, '/execute_trajectory',
            callback_group=cb)

        # ---- action server ----
        self._action_server = ActionServer(
            self, DualMoveToJointState, 'move_to_joint_state',
            execute_callback=self._execute,
            goal_callback=lambda g: GoalResponse.ACCEPT,
            cancel_callback=lambda gh: CancelResponse.ACCEPT,
            callback_group=cb,
        )

        self.get_logger().info(
            f'dual_move_server ready: group={self._group_name}, '
            f'planner={self._planner_id}, pipeline={self._pipeline_id}')

    # ==================== callbacks ====================
    def _on_joint_state(self, msg: JointState):
        with self._js_lock:
            self._latest_js = msg

    def _get_current_12(self) -> Optional[List[float]]:
        with self._js_lock:
            msg = self._latest_js
        if msg is None:
            return None
        name_to_pos = dict(zip(msg.name, msg.position))
        try:
            return [name_to_pos[n] for n in ALL_JOINTS]
        except KeyError as e:
            self.get_logger().warn(f'joint not in /joint_states: {e}')
            return None

    # ==================== future waiters ====================
    @staticmethod
    def _wait_future_wall(future, timeout_s: float):
        done = threading.Event()
        future.add_done_callback(lambda _: done.set())
        if not done.wait(timeout=timeout_s):
            future.cancel()
            return None
        return future.result()

    def _wait_future_sim(self, future, timeout_sim_s: float):
        start = self.get_clock().now()
        timeout = Duration(seconds=timeout_sim_s)
        while rclpy.ok():
            if future.done():
                return future.result()
            if self.get_clock().now() - start > timeout:
                future.cancel()
                return None
            time.sleep(0.02)
        return None

    # ==================== planning ====================
    def _plan_dual(self, target_12: List[float], current_12: List[float],
                    velocity_scale: float, acceleration_scale: float):
        if not self._plan_cli.service_is_ready():
            if not self._plan_cli.wait_for_service(timeout_sec=2.0):
                self.get_logger().error('/plan_kinematic_path unavailable')
                return None

        req = GetMotionPlan.Request()
        mpr = req.motion_plan_request
        mpr.group_name = self._group_name
        mpr.planner_id = self._planner_id
        mpr.pipeline_id = self._pipeline_id
        mpr.num_planning_attempts = int(self._planning_attempts)
        mpr.allowed_planning_time = float(self._planning_time)
        mpr.max_velocity_scaling_factor = float(velocity_scale)
        mpr.max_acceleration_scaling_factor = float(acceleration_scale)

        rs = RobotState()
        rs.joint_state.name = list(ALL_JOINTS)
        rs.joint_state.position = list(current_12)
        rs.is_diff = False
        mpr.start_state = rs

        goal = Constraints()
        for name, pos in zip(ALL_JOINTS, target_12):
            jc = JointConstraint()
            jc.joint_name = name
            jc.position = float(pos)
            jc.tolerance_above = 0.001
            jc.tolerance_below = 0.001
            jc.weight = 1.0
            goal.joint_constraints.append(jc)
        mpr.goal_constraints.append(goal)

        future = self._plan_cli.call_async(req)
        res = self._wait_future_wall(future, timeout_s=self._planning_time + 3.0)
        if res is None:
            self.get_logger().error('/plan_kinematic_path timeout')
            return None
        code = res.motion_plan_response.error_code.val
        if code != 1:
            self.get_logger().warn(
                f'dual planning failed: error_code={code}')
            return None
        return res.motion_plan_response.trajectory

    # ==================== execution ====================
    def _execute_dual(self, trajectory, timeout_sim_s: float) -> bool:
        if not self._exec_cli.wait_for_server(timeout_sec=3.0):
            self.get_logger().error('/execute_trajectory action unavailable')
            return False

        goal = ExecuteTrajectory.Goal()
        goal.trajectory = trajectory
        # 重置 stamp, 让 controller 用 now 作为起点
        goal.trajectory.joint_trajectory.header.stamp.sec = 0
        goal.trajectory.joint_trajectory.header.stamp.nanosec = 0

        goal_future = self._exec_cli.send_goal_async(goal)
        goal_handle = self._wait_future_wall(goal_future, timeout_s=3.0)
        if goal_handle is None:
            self.get_logger().error('execute send_goal timeout')
            return False
        if not goal_handle.accepted:
            self.get_logger().error('execute goal rejected')
            return False

        result_future = goal_handle.get_result_async()
        wrapped = self._wait_future_sim(result_future,
                                          timeout_sim_s=timeout_sim_s)
        if wrapped is None:
            self.get_logger().error(
                f'execute timeout ({timeout_sim_s:.1f}s sim)')
            goal_handle.cancel_goal_async()
            return False

        code = wrapped.result.error_code.val
        if code != 1:
            self.get_logger().error(
                f'execute failed: error_code={code}')
            return False
        return True

    # ==================== main execute ====================
    def _execute(self, goal_handle):
        req = goal_handle.request
        result = DualMoveToJointState.Result()

        # ---- 组装 target_12 ----
        target_12: Optional[List[float]] = None
        if len(req.left_positions) == 6 and len(req.right_positions) == 6:
            target_12 = list(req.left_positions) + list(req.right_positions)
            self.get_logger().info(f'dual_move: explicit 12 joint target')
        elif req.named_pose:
            if req.named_pose not in self._named_poses:
                msg = f'unknown named_pose: {req.named_pose}'
                self.get_logger().error(msg)
                goal_handle.abort()
                result.success = False
                result.error_code = -1
                result.error_msg = msg
                return result
            target_12 = self._named_poses[req.named_pose]
            self.get_logger().info(
                f'dual_move: named_pose={req.named_pose}')
        else:
            msg = 'neither left/right_positions nor named_pose provided'
            self.get_logger().error(msg)
            goal_handle.abort()
            result.success = False
            result.error_code = -2
            result.error_msg = msg
            return result

        # ---- 起点 ----
        current_12 = self._get_current_12()
        if current_12 is None:
            msg = 'no /joint_states available yet'
            self.get_logger().error(msg)
            goal_handle.abort()
            result.success = False
            result.error_code = -3
            result.error_msg = msg
            return result

        v = float(req.velocity_scale) if req.velocity_scale > 0 else self._default_v
        a = float(req.acceleration_scale) if req.acceleration_scale > 0 else self._default_a

        # ---- plan ----
        t0 = time.time()
        traj = self._plan_dual(target_12, current_12, v, a)
        t_plan = time.time() - t0
        if traj is None:
            result.success = False
            result.error_code = -10
            result.error_msg = 'planning failed'
            goal_handle.abort()
            return result

        n_pts = len(traj.joint_trajectory.points)
        dur = traj.joint_trajectory.points[-1].time_from_start
        dur_s = dur.sec + dur.nanosec / 1e9
        self.get_logger().info(
            f'dual plan OK: {n_pts} pts, {dur_s:.2f}s traj, {t_plan*1000:.0f}ms plan')

        # ---- execute ----
        exec_timeout = max(float(req.timeout_sec) if req.timeout_sec > 0
                            else dur_s * 2 + 5.0, dur_s * 2 + 5.0)
        ok = self._execute_dual(traj, timeout_sim_s=exec_timeout)

        if ok:
            result.success = True
            result.error_code = 0
            result.error_msg = ''
            goal_handle.succeed()
        else:
            result.success = False
            result.error_code = -20
            result.error_msg = 'execution failed'
            goal_handle.abort()
        return result


def main():
    rclpy.init()
    node = DualMoveServer()
    executor = MultiThreadedExecutor()
    executor.add_node(node)
    try:
        executor.spin()
    finally:
        node.destroy_node()
        rclpy.shutdown()


if __name__ == '__main__':
    main()
```

### 5. 新建 `s622_arm_actions/config/dual_arm_config.yaml`

```yaml
/dual/dual_move_server:
  ros__parameters:
    use_sim_time: true
    group_name: 'dual_arm'
    planner_id: 'RRTConnect'
    pipeline_id: 'ompl'
    default_velocity_scale: 0.2
    default_acceleration_scale: 0.2
    allowed_planning_time: 8.0
    num_planning_attempts: 10

    named_pose_names: ['dual_home']

    # dual_home: 双臂都竖直向上, 与 SRDF 里 group_state 一致
    dual_home_joint_positions:
      - 0.0        # left_j1
      - -1.5708    # left_j2
      - 0.0        # left_j3
      - 0.0        # left_j4
      - 0.0        # left_j5
      - 0.0        # left_j6
      - 0.0        # right_j1
      - -1.5708    # right_j2
      - 0.0        # right_j3
      - 0.0        # right_j4
      - 0.0        # right_j5
      - 0.0        # right_j6
```

### 6. `arm_actions_dual.launch.py` 追加 dual group

在文件末尾 `return LaunchDescription([...])` 之前,复用现有 `moveit_remaps` 加一个 dual group:

```python
    dual_cfg = os.path.join(pkg, 'config', 'dual_arm_config.yaml')

    dual_group = GroupAction([
        PushRosNamespace('dual'),
        Node(
            package='s622_arm_actions',
            executable='dual_move_server',
            name='dual_move_server',
            output='screen',
            parameters=[dual_cfg, {'use_sim_time': True}],
            remappings=moveit_remaps,   # 复用: 让 dual node 找到全局 MoveIt 服务
        ),
    ])

    return LaunchDescription([
        make_arm_group('left', left_cfg),
        make_arm_group('right', right_cfg),
        dual_group,          # ← 新增
    ])
```

### 7. `s622_arm_actions/setup.py` 注册

`entry_points.console_scripts` 加一行:

```python
    entry_points={
        'console_scripts': [
            'move_to_pose_server = s622_arm_actions.move_to_pose_server:main',
            'gripper_service = s622_arm_actions.gripper_service:main',
            'visual_align_server = s622_arm_actions.visual_align_server:main',
            'planning_scene_service = s622_arm_actions.planning_scene_service:main',
            'dual_move_server = s622_arm_actions.dual_move_server:main',   # ← 新增
        ],
    },
```

---

## 编译顺序

**关键**:先 build `s622_bt_manager`(生成 action msg 代码),再 build `s622_arm_actions`:

```bash
cd ~/my_S622

# 1. 生成 action 代码
colcon build --packages-select s622_bt_manager --merge-install --symlink-install

# 2. 生成 arm_actions (依赖上面的 action)
colcon build --packages-select s622_arm_actions --merge-install --symlink-install

# 3. moveit_config 有 SRDF 变化
colcon build --packages-select s622_moveit_config --merge-install --symlink-install

source install/setup.bash
```



## 启动

```bash
pkill -f "ros2 launch"
ros2 launch gz_launch s622_dual_arm.launch.py
# 等 30s (dual_move_server 需要 execute_trajectory action 就绪, 稍慢)
```

## 验收

### V1. dual_move_server 起来

```bash
ros2 node list | grep dual
# 期望: /dual/dual_move_server

ros2 action list | grep dual
# 期望: /dual/move_to_joint_state

ros2 param get /dual/dual_move_server group_name
# 期望: dual_arm
```

日志里应看到:

```
[dual_move_server]: loaded dual named poses: ['dual_home']
[dual_move_server]: dual_move_server ready: group=dual_arm, planner=RRTConnect, pipeline=ompl
```

### V2. move_group 认识 dual_arm group

```bash
# RViz MotionPlanning 面板 → Planning Group 下拉 → 应该有 dual_arm
# 或者:
ros2 topic echo /joint_states --once | head -2
ros2 service call /query_planner_interface moveit_msgs/srv/QueryPlannerInterfaces "{}" 2>&1 | grep -A5 group
```

### V3. 单个 named_pose 双臂联合运动

先手动让左臂偏一下(方案 A 单臂):

```bash
ros2 action send_goal /left/move_to_pose s622_bt_manager/action/MoveToPose \
  '{named_pose: "safe"}' --feedback

ros2 action send_goal /right/move_to_pose s622_bt_manager/action/MoveToPose \
  '{named_pose: "safe"}' --feedback
```

然后 dual 联合回 home:

```bash
ros2 action send_goal /dual/move_to_joint_state \
  s622_bt_manager/action/DualMoveToJointState \
  '{named_pose: "dual_home", velocity_scale: 0.2}' --feedback
```

**期望**:双臂**同时**从 safe 位联合规划一条 12DOF traj,同步运动到 dual_home。日志:

```
[dual_move_server]: dual_move: named_pose=dual_home
[dual_move_server]: dual plan OK: 30 pts, 3.10s traj, 250ms plan
[left_arm_controller]: Received new action goal
[right_arm_controller]: Received new action goal    ← 同时!
```

两条 "Received new action goal" 时间戳应该差 < 20ms(MoveIt 拆分下发几乎同步)。

### V4. 显式 12 维目标

```bash
ros2 action send_goal /dual/move_to_joint_state \
  s622_bt_manager/action/DualMoveToJointState \
  '{left_positions:  [0.5, -1.2, 0.5, 0, 0, 0],
    right_positions: [-0.5, -1.2, 0.5, 0, 0, 0],
    velocity_scale: 0.2}' --feedback
```

期望:双臂对称摆姿势。

### V5. 时间同步测量(方案 D 的核心价值)

```bash
# 一边:每 100ms 采样一次 left_j2 和 right_j2 的位置
ros2 topic echo /joint_states --field position --once
# 触发 dual 动作
ros2 action send_goal /dual/move_to_joint_state \
  s622_bt_manager/action/DualMoveToJointState \
  '{named_pose: "dual_home"}'
```

从 `/joint_states` 观察左 j2 和右 j2 在整个运动过程中**归零的时刻**——方案 D 保证同时完成,两侧 j2 的时间差 < 100ms。方案 A 并发,两臂完成时间可能差 100-500ms。

不过最终算下来 100ms < 时间差 < 200ms
---

## 常见坑

### 1. `<group name="left_arm"/>` 复合语法

MoveIt SRDF 允许 group 引用其他 group 名。写法:

```xml
<group name="dual_arm">
    <group name="left_arm" />
    <group name="right_arm" />
</group>
```

不是 `<subgroup>` 或 `<include>`。RViz 里 planning group 下拉能看到 `dual_arm` 就是对的。

### 2. dual_arm group 没 IK

MoveIt 不会为复合 group 自动配 IK(没单一 tip_link)。**不要**在 `dual_arm_kinematics.yaml` 里加 dual_arm 条目,否则 move_group 加载时会 warn 甚至崩溃。

现在文件里应只有:

```yaml
left_arm:
  kinematics_solver: kdl_kinematics_plugin/KDLKinematicsPlugin
  ...
right_arm:
  kinematics_solver: kdl_kinematics_plugin/KDLKinematicsPlugin
  ...
```

**没有** `dual_arm:` 条目——这就对了。

### 3. `execute_trajectory` 需要 remap

`dual_move_server` 在 `/dual` namespace 下,ActionClient 用相对话题 `execute_trajectory` 会被 namespace 化成 `/dual/execute_trajectory`。moveit_remaps 里已有 `action_remaps('execute_trajectory')` 展开的 5 条,继承 M2.6a 的处理,不需要额外改。

### 4. Named pose joint 顺序

`dual_home_joint_positions` **必须严格按** `[left_j1..left_j6, right_j1..right_j6]` 12 个数。dual_move_server 里 `ALL_JOINTS` 是硬编码的,顺序必须一致。yaml 里我加了注释标明。

### 5. Planning pipeline 用 ompl 而非 fairino

M2.6a 单臂 planner 用 `fairino`(带解析 IK 的定制 pipeline)。dual_arm 没 IK,只能走 joint-space OMPL。所以 config 里 `pipeline_id: 'ompl'`。若日志报 "pipeline fairino not found for group dual_arm",就是这个原因。

### 6. `error_code=1` 是 SUCCESS(moveit 侧)

`moveit_msgs/MoveItErrorCodes.SUCCESS = 1`。我在 planning 和 execute 里都判 `code != 1`。**别跟 `FollowJointTrajectory.SUCCESSFUL=0` 混淆**——M2.6a 直连 controller 走的是 FollowJointTrajectory,方案 D 走 `/execute_trajectory` 是 moveit_msgs/ExecuteTrajectory,两者错误码语义不同。

### 7. 双臂协同 planning 时间

Dual 空间 12DOF,OMPL RRTConnect 首次 planning 可能 200-800ms(比单臂长 3-5 倍)。若超过 `allowed_planning_time: 8.0` 就失败。慢的话调大到 15.0。

### 8. sim RTF 影响 execute timeout

execute 用 sim time 等 result,不受 RTF 影响,承接 M2.6a 的 fix。ok。

### 9. moveit_msgs.action.ExecuteTrajectory 导入

`from moveit_msgs.action import ExecuteTrajectory` 在 Humble 存在。若报 import 错,可能是 moveit_msgs 没装。检查:

```bash
python3 -c "from moveit_msgs.action import ExecuteTrajectory; print('OK')"
```

### 10. `dual_home` 在 SRDF 和 config 里重复

SRDF 里的 group_state 主要给 RViz Motion Planning 面板下拉选用。dual_move_server 走 yaml 参数,不读 SRDF group_state。两处都定义是为了兼容 RViz 手动测试。**保持两处一致,以 yaml 为准**——SRDF 的作用只是文档 + RViz UI。

---

跑完 V1-V5,M2.6b 完成。特别关注 V3 和 V5——V3 验证双臂能联合规划执行,V5 验证时间同步(方案 D 的杀手锏)。

有问题贴日志。M2.6b 通过就意味着:

* 方案 A(独立并行)在 M2.7 pick-place 里用
* 方案 D(联合规划)在 M2.8 handover 里用

底座就都齐了。
