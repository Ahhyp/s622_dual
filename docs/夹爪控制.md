```txt
arm_executor.py (你的代码)
  ↓ 调用 pymoveit2.MoveIt2.plan() / .execute()
FollowJointTrajectory action
  ↓
controller_manager
  ↓ 路由到 hand_controller
joint_trajectory_controller (hand_controller)
  ↓ 写 position 命令到 hardware interface
ros2_control 框架
  ↓ 调用
gz_ros2_control/GazeboSimSystem (硬件接口插件)
  ↓ 写 Gazebo 关节力/位置
Gazebo 物理引擎
  ↓ 关节运动
joint_state_broadcaster
  ↓ 发布 /joint_states
robot_state_publisher
  ↓ 更新 TF
RViz / move_group 感知到夹爪位置
```
注意检查控制链路。

当前缺的是红色两段：

| 层                                         | 状态               |
| ------------------------------------------ | ------------------ |
| ros2_controllers.yaml hand_controller 定义 | ✅ 已有             |
| initial_positions.yaml finger 初值         | ✅ 已有             |
| ros2_control xacro 里定义 finger 关节      | ❌ 缺               |
| launch 文件 spawn hand_controller          | ❌ 缺               |
| robot_gazebo.urdf.xacro 引用 xacro         | ⚠️ 你新开了一个文件 |

你的 s622_gazebo_ros2_control.xacro 是在 gz_launch/config/ 下的。原来用的是 s622_moveit_config/config/s622_gz_moveit_descriptions.ros2_control.xacro。要统一到你的新文件还是继续用原来的？

应该要统一到 gz_launch/config 里面， 因为这个是用来仿真的。


改完 xacro 和 launch 之后，测试只需要一行命令：

```bash


# 确认手抓控制器 loaded
# 看 hand_controller 相关话题
ros2 topic list | grep hand
ros2 topic list | grep finger
ros2 topic list | grep hand
ros2 action list | grep hand



ros2 topic pub -1 /hand_controller/joint_trajectory trajectory_msgs/msg/JointTrajectory \
  "{joint_names: [finger1_joint, finger2_joint], points: [{positions: [0, 0], time_from_start: {sec: 1, nanosec: 0}}]}"

#
ros2 topic pub -1 /hand_controller/joint_trajectory trajectory_msgs/msg/JointTrajectory \
  "{joint_names: [finger1_joint, finger2_joint], points: [{positions: [0.025, -0.025], time_from_start: {sec: 1, nanosec: 0}}]}"


ros2 action send_goal /hand_controller/follow_joint_trajectory \
  control_msgs/action/FollowJointTrajectory \
  "{trajectory: {joint_names: [finger1_joint, finger2_joint], points: [{positions: [0.025, -0.025], time_from_start: {sec: 1, nanosec: 0}}]}}"


```


# 重启仿真或重 spawn controller：
ros2 run controller_manager spawner -p /path/to/ros2_controllers.yaml hand_controller

# 再发命令
ros2 action send_goal /hand_controller/follow_joint_trajectory \
  control_msgs/action/FollowJointTrajectory \
  "{trajectory: {joint_names: [finger1_joint, finger2_joint], points: [{positions: [0.025, -0.025], time_from_start: {sec: 1, nanosec: 0}}]}}"

# 确认两个都变了
ros2 topic echo /joint_states --once
