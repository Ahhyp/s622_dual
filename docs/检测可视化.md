对，先不要盲目调 IK。现在应该加两类调试：

```text id="1hxjmr"
1. YOLO 检测可视化：确认框真的画在目标物体上
2. visual_servo 坐标打印：确认 target 像素、深度、camera 坐标、base_link 坐标是否合理
```

---

# 一、给 `yolov8_obb_node.py` 加检测可视化

## 1. 在 `__init__` 参数区加

```python id="ux8js1"
self.declare_parameter("publish_debug_image", True)
self.declare_parameter("debug_image_topic", "/yolov8/obb_debug_image")
self.declare_parameter("print_detections", True)
```

## 2. 在读取参数区加

```python id="t1jp1i"
self.publish_debug_image = bool(self.get_parameter("publish_debug_image").value)
self.debug_image_topic = self.get_parameter("debug_image_topic").value
self.print_detections = bool(self.get_parameter("print_detections").value)
```

## 3. 在 publisher 创建区加

放在 `self.pub = self.create_publisher(...)` 后面：

```python id="4xpp9r"
if self.publish_debug_image:
    self.debug_pub = self.create_publisher(
        Image,
        self.debug_image_topic,
        10,
    )
    self.get_logger().info(
        f"debug image enabled: {self.debug_image_topic}"
    )
else:
    self.debug_pub = None
```

## 4. 在 `image_callback()` 里，YOLO 推理之后加

找到这一段：

```python id="qjtwse"
results = self.model.predict(
    source=cv_image,
    conf=self.confidence_threshold,
    imgsz=self.imgsz,
    device=self.device,
    max_det=self.max_det,
    verbose=self.yolo_verbose,
)
```

后面立刻加：

```python id="flfbts"
# ------------------------------------------------------------
# 发布 YOLO 检测可视化图像
# ------------------------------------------------------------
if self.publish_debug_image and self.debug_pub is not None and results:
    try:
        debug_img = results[0].plot()
        debug_msg = self.bridge.cv2_to_imgmsg(debug_img, encoding="bgr8")
        debug_msg.header = msg.header
        self.debug_pub.publish(debug_msg)
    except Exception as e:
        self.get_logger().warning(
            f"failed to publish debug image: {e}",
            throttle_duration_sec=2.0,
        )
```

## 5. 在解析出 detections 后打印检测结果

找到：

```python id="pa3dnr"
if results:
    detections = self.parse_yolo_obb_result(results[0])
```

后面加：

```python id="6in1j9"
if self.print_detections:
    self.get_logger().info(
        f"YOLO detections: {len(detections)}",
        throttle_duration_sec=1.0,
    )

    for det in detections:
        self.get_logger().info(
            f"det | class={det.class_name} "
            f"conf={det.confidence:.3f} "
            f"cx={det.center_x:.1f} cy={det.center_y:.1f} "
            f"w={det.width:.1f} h={det.height:.1f} "
            f"angle={det.angle:.3f}",
            throttle_duration_sec=1.0,
        )
```

然后运行rqt_image_view:
但是这个我环境没配， 所以直接在 RViz 看就行。
```bash id="a8goel"
ros2 run rqt_image_view rqt_image_view
```

```text
这是从 接真yolo.md 摘抄过来的指令
ros2 run ros_gz_sim create \
  -name target_box \
  -file ~/my_S622/src/gz_launch/models/target_box/model.sdf \
  -x 0.45 -y -0.40 -z 0.04 \
  -R 0 -P 0 -Y 0.5

并且

MODEL=$HOME/my_S622/src/yolov8_obb/models/yolo-obb-gazebo.pt

ros2 run yolov8_obb yolov8_obb_node \
  --ros-args \
  -p model_path:="$MODEL" \
  -p image_topic:=/camera/color/image_raw \
  -p detections_topic:=/yolov8/obb_detections \
  -p confidence_threshold:=0.05 \
  -p device:=auto \
  -p imgsz:=1024 \
  -p publish_empty:=true

ros2 run visual_servo visual_servo_node \
  --ros-args \
  -p enable_motion:=false \
  -p enable_grasp_sequence:=false

ros2 topic pub --once /servo_trigger std_msgs/msg/Bool "{data: true}"
```
得先运行 obb node。
选择：

```text id="gqget6"
/yolov8/obb_debug_image
```

你要确认：**检测框是否真的压在目标物体上，而不是机械臂、背景、桌面。**

确认成功。
---

# 二、给 `visual_servo_node.py` 打印 target 坐标

IK failed 最大概率出在这里：

```text id="kpc8q8"
YOLO 像素点 → 深度 → camera 坐标 → base_link 坐标
```

所以我们要打印：

```text id="wvf35u"
检测像素 center_x / center_y
该像素附近深度值
xyz_cam
xyz_base
hover_pt
IK 请求姿态
IK error_code
```

---

## 1. 在 `__init__` 参数区加

放到调试参数附近：

```python id="qndyzk"
self.declare_parameter("debug_print_target", True)
self.declare_parameter("debug_print_ik", True)
self.declare_parameter("det_timeout", 2.0)
```

## 2. 在读取参数区加

```python id="9984wg"
self.debug_print_target = bool(
    self.get_parameter("debug_print_target").value)
self.debug_print_ik = bool(
    self.get_parameter("debug_print_ik").value)
self.det_timeout = float(
    self.get_parameter("det_timeout").value)
```

## 3. 在缓存区加

你现在有：

```python id="lyw1tw"
self.latest_det: Optional[Yolov8Inference] = None
```

后面加：

```python id="vzyw8t"
self.latest_det_time: Optional[float] = None
```

## 4. 修改 `cb_det`

把你现在的：

```python id="g8i4t0"
def cb_det(self, msg: Yolov8Inference):
    self.latest_det = msg
```

替换成：

```python id="9f6710"
def cb_det(self, msg: Yolov8Inference):
    self.latest_det = msg
    self.latest_det_time = self._now_sec()

    if len(msg.results) == 0:
        self.get_logger().info(
            "det received: empty results",
            throttle_duration_sec=1.0,
        )
        return

    target = max(msg.results, key=lambda r: r.confidence)

    self.get_logger().info(
        f"det received | n={len(msg.results)} "
        f"best={target.class_name} "
        f"conf={target.confidence:.3f} "
        f"px=({target.center_x:.1f},{target.center_y:.1f}) "
        f"size=({target.width:.1f},{target.height:.1f}) "
        f"angle={target.angle:.3f} "
        f"frame={msg.header.frame_id}",
        throttle_duration_sec=1.0,
    )
```

---

## 5. 加一个深度调试函数

放在 `_compute_target_in_base()` 前面即可：

```python id="93jo0f"
def _debug_depth_at_pixel(self, u: float, v: float):
    """打印检测中心附近的深度原始值，帮助判断深度是否正常。"""
    if self.depth_img is None:
        return

    h, w = self.depth_img.shape[:2]
    x = int(round(u))
    y = int(round(v))

    if x < 0 or x >= w or y < 0 or y >= h:
        self.get_logger().warning(
            f"target pixel out of depth image: "
            f"u={u:.1f}, v={v:.1f}, depth_size=({w},{h})",
            throttle_duration_sec=1.0,
        )
        return

    # 取中心附近 5x5 小窗口，避免单个深度点刚好无效
    x0 = max(0, x - 2)
    x1 = min(w, x + 3)
    y0 = max(0, y - 2)
    y1 = min(h, y + 3)

    patch = self.depth_img[y0:y1, x0:x1]
    finite = patch[np.isfinite(patch)]

    if finite.size == 0:
        self.get_logger().warning(
            f"depth patch invalid at pixel ({x},{y})",
            throttle_duration_sec=1.0,
        )
        return

    raw_center = self.depth_img[y, x]
    raw_min = np.min(finite)
    raw_max = np.max(finite)
    raw_mean = np.mean(finite)

    self.get_logger().info(
        f"depth debug | pixel=({x},{y}) "
        f"dtype={self.depth_img.dtype} "
        f"center={raw_center} "
        f"patch_mean={raw_mean:.4f} "
        f"patch_min={raw_min:.4f} "
        f"patch_max={raw_max:.4f} "
        f"image_size=({w},{h})",
        throttle_duration_sec=1.0,
    )
```

---

## 6. 修改 `_compute_target_in_base()`

把你现在的 `_compute_target_in_base()` 替换成下面这个版本：

```python id="6wkvcu"
def _compute_target_in_base(self) -> Optional[np.ndarray]:
    """像素 → 相机系 → base_link，返回目标物体位置（不含 hover_z）。
    如果 debug_target 非零则直接用硬编码目标，跳过像素管线。
    """
    debug = list(self.get_parameter("debug_target").value)
    if any(abs(v) > 1e-6 for v in debug):
        target_debug = np.array(debug, dtype=float)
        self.get_logger().info(
            f"use debug_target in base_link: "
            f"({target_debug[0]:+.4f}, {target_debug[1]:+.4f}, {target_debug[2]:+.4f})",
            throttle_duration_sec=1.0,
        )
        return target_debug

    if self.depth_img is None:
        self.get_logger().warning(
            "no depth image yet",
            throttle_duration_sec=1.0,
        )
        return None

    if self.latest_det is None:
        self.get_logger().warning(
            "no detection msg yet",
            throttle_duration_sec=1.0,
        )
        return None

    if self.latest_det_time is not None:
        age = self._now_sec() - self.latest_det_time
        if age > self.det_timeout:
            self.get_logger().warning(
                f"detection timeout: age={age:.2f}s > {self.det_timeout:.2f}s",
                throttle_duration_sec=1.0,
            )
            return None

    if len(self.latest_det.results) == 0:
        self.get_logger().warning(
            "latest detection has empty results",
            throttle_duration_sec=1.0,
        )
        return None

    if not self.estimator.has_intrinsics():
        self.get_logger().warning(
            "camera intrinsics not ready",
            throttle_duration_sec=1.0,
        )
        return None

    # 取最高置信度目标
    target = max(self.latest_det.results, key=lambda r: r.confidence)

    if self.debug_print_target:
        self.get_logger().info(
            f"target pixel | class={target.class_name} "
            f"conf={target.confidence:.3f} "
            f"u={target.center_x:.1f} v={target.center_y:.1f} "
            f"w={target.width:.1f} h={target.height:.1f} "
            f"angle={target.angle:.3f}",
            throttle_duration_sec=1.0,
        )
        self._debug_depth_at_pixel(target.center_x, target.center_y)

    # 像素 + 深度 → 相机坐标
    xyz_cam = self.estimator.pixel_to_camera(
        target.center_x,
        target.center_y,
        self.depth_img,
    )

    if xyz_cam is None:
        self.get_logger().warning(
            "pixel_to_camera failed: invalid depth or intrinsics",
            throttle_duration_sec=1.0,
        )
        return None

    if self.debug_print_target:
        self.get_logger().info(
            f"target in camera | "
            f"x={xyz_cam[0]:+.4f}, y={xyz_cam[1]:+.4f}, z={xyz_cam[2]:+.4f} "
            f"frame={self.camera_frame}",
            throttle_duration_sec=1.0,
        )

    pt = PointStamped()
    pt.header.frame_id = self.camera_frame

    # 如果 TF 时间对不上，可以先用 Time() 获取最新 TF。
    # 你现在用 depth_stamp 也可以，但调试阶段 latest TF 更容易排除时间同步问题。
    pt.header.stamp = rclpy.time.Time().to_msg()

    pt.point.x = float(xyz_cam[0])
    pt.point.y = float(xyz_cam[1])
    pt.point.z = float(xyz_cam[2])

    try:
        pt_base = self.tf_buffer.transform(
            pt,
            self.base_frame,
            timeout=Duration(seconds=0.1),
        )
    except Exception as e:
        self.get_logger().warning(
            f"TF target camera→base failed: {e}",
            throttle_duration_sec=1.0,
        )
        return None

    target_base = np.array([
        pt_base.point.x,
        pt_base.point.y,
        pt_base.point.z,
    ])

    if self.debug_print_target:
        hover_pt = target_base + np.array([0.0, 0.0, self.hover_z])
        descend_pt = target_base + np.array([0.0, 0.0, self.descend_z])

        self.get_logger().info(
            f"target in base | "
            f"x={target_base[0]:+.4f}, y={target_base[1]:+.4f}, z={target_base[2]:+.4f}",
            throttle_duration_sec=1.0,
        )

        self.get_logger().info(
            f"hover target | "
            f"x={hover_pt[0]:+.4f}, y={hover_pt[1]:+.4f}, z={hover_pt[2]:+.4f} "
            f"hover_z={self.hover_z:.3f}",
            throttle_duration_sec=1.0,
        )

        self.get_logger().info(
            f"descend target | "
            f"x={descend_pt[0]:+.4f}, y={descend_pt[1]:+.4f}, z={descend_pt[2]:+.4f} "
            f"descend_z={self.descend_z:.3f}",
            throttle_duration_sec=1.0,
        )

    return target_base
```

注意我这里把：

```python id="34bmrv"
pt.header.stamp = self.depth_stamp
```

临时改成了：

```python id="q2jl7j"
pt.header.stamp = rclpy.time.Time().to_msg()
```

这是为了调试时使用最新 TF，先排除时间同步问题。等稳定后，如果你要严格时间同步，再改回 depth stamp。

---

# 三、打印 IK 请求信息

你的 `_check_reachable()` 里，在发 IK 请求前加：

```python id="8v8f50"
if self.debug_print_ik:
    self.get_logger().info(
        f"IK request | group={self.move_group_name} "
        f"frame={self.base_frame} "
        f"pos=({position_in_base[0]:+.4f}, "
        f"{position_in_base[1]:+.4f}, "
        f"{position_in_base[2]:+.4f}) "
        f"quat=(x=1.0, y=0.0, z=0.0, w=0.0) "
        f"avoid_collisions={self.avoid_collisions}",
        throttle_duration_sec=1.0,
    )
```

在这里：

```python id="hsu58n"
code = future.result().error_code.val
if code != 1:
    self.get_logger().warning(f"IK failed, error_code={code}")
    return False
```

替换成：

```python id="m9fp4x"
code = future.result().error_code.val

if code != 1:
    self.get_logger().warning(
        f"IK failed | error_code={code} "
        f"target=({position_in_base[0]:+.4f}, "
        f"{position_in_base[1]:+.4f}, "
        f"{position_in_base[2]:+.4f}) "
        f"avoid_collisions={self.avoid_collisions}"
    )
    return False

self.get_logger().info(
    f"IK success | target=({position_in_base[0]:+.4f}, "
    f"{position_in_base[1]:+.4f}, "
    f"{position_in_base[2]:+.4f})",
    throttle_duration_sec=1.0,
)

return True
```

---

# 四、建议临时加一个避碰开关

你现在写死了：

```python id="601bwq"
self.avoid_collisions = True
```

调试 IK 时建议改成参数。

在 `__init__` 前面参数区加：

```python id="fmy3nh"
self.declare_parameter("avoid_collisions", True)
```

然后把：

```python id="scak3h"
self.avoid_collisions = True
```

改成：

```python id="y0f73o"
self.avoid_collisions = bool(
    self.get_parameter("avoid_collisions").value)
```

这样可以这样测试：

```bash id="yz4fw8"
ros2 run visual_servo visual_servo_node \
  --ros-args \
  -p enable_motion:=false \
  -p enable_grasp_sequence:=false \
  -p avoid_collisions:=false \
  -p debug_print_target:=true \
  -p debug_print_ik:=true
```

如果：

```text id="j16rm8"
avoid_collisions:=false 时 IK 成功
avoid_collisions:=true 时 IK 失败
```

说明问题是碰撞检测/规划场景。

如果两者都失败，说明大概率是：

```text id="js22y3"
target 坐标不合理
姿态不可达
group / ee frame / IK 配置问题
```

---

# 五、调试时重点看这些数值

```text
这是从 接真yolo.md 摘抄过来的指令
ros2 run ros_gz_sim create \
  -name target_box \
  -file ~/my_S622/src/gz_launch/models/target_box/model.sdf \
  -x 0.45 -y -0.40 -z 0.04 \
  -R 0 -P 0 -Y 0.5

并且

MODEL=$HOME/my_S622/src/yolov8_obb/models/yolo-obb-gazebo.pt

ros2 run yolov8_obb yolov8_obb_node \
  --ros-args \
  -p model_path:="$MODEL" \
  -p image_topic:=/camera/color/image_raw \
  -p detections_topic:=/yolov8/obb_detections \
  -p confidence_threshold:=0.05 \
  -p device:=auto \
  -p imgsz:=1024 \
  -p publish_empty:=true

ros2 run visual_servo visual_servo_node \
  --ros-args \
  -p enable_motion:=false \
  -p enable_grasp_sequence:=false \
  -p debug_print_target:=true \
  -p debug_print_ik:=true

ros2 topic pub --once /servo_trigger std_msgs/msg/Bool "{data: true}"
```

打印出来后，你重点看：

## 1. target in camera

例如合理情况：

```text id="mcgfm4"
target in camera | x=+0.02, y=+0.10, z=+0.65
```

其中 `z` 是目标离相机的距离。一般应该是几十厘米到一两米。

如果出现：

```text id="l3mibf"
z=650.0
```

那说明深度单位错了，可能 mm 没转 m。

如果出现：

```text id="p42d8o"
z=0.0
z=nan
z=inf
```

说明深度无效。

---

## 2. target in base

合理情况可能类似：

```text id="a9nsfs"
target in base | x=+0.45, y=+0.00, z=+0.04
```

如果你物体放在机械臂前方 45cm，地面/桌面上，高度 4cm，这就很合理。

异常情况：

```text id="vzi6pa"
x=-1.2
y=+3.5
z=-0.8
```

这种必然 IK failed。

---

## 3. hover target

你的 IK 检查用的是：

```python id="pr9rli"
hover_pt = target + np.array([0.0, 0.0, self.hover_z])
```

如果 target 是：

```text id="e3g2wz"
x=0.45, y=0.0, z=0.04
```

hover 应该是：

```text id="ogpivy"
x=0.45, y=0.0, z=0.14
```

这个通常可达。

如果 hover 是：

```text id="aqxyp9"
x=1.8, y=-0.7, z=-0.2
```

那 IK 失败正常。

---

# 六、推荐你的测试顺序

## Step 1：看 YOLO 可视化

```bash id="xkz9ih"
rqt_image_view
```

选择：

```text id="8nnyis"
/yolov8/obb_debug_image
```

确认框在目标物体上。

## Step 2：看检测消息

```bash id="7f5e0k"
ros2 topic echo --once /yolov8/obb_detections
```

确认：

```text id="66nr41"
center_x / center_y 在目标中心附近
angle 合理
confidence 不太低
```

## Step 3：启动 visual_servo，先关闭运动和避碰

```bash id="gx0o37"
ros2 run visual_servo visual_servo_node \
  --ros-args \
  -p enable_motion:=false \
  -p enable_grasp_sequence:=false \
  -p avoid_collisions:=false \
  -p debug_print_target:=true \
  -p debug_print_ik:=true
```

## Step 4：触发

```bash id="907arf"
ros2 topic pub --once /servo_trigger std_msgs/msg/Bool "{data: true}"
```

## Step 5：观察日志

你要看到类似：

```text id="55oyrc"
target pixel | ...
depth debug | ...
target in camera | ...
target in base | ...
hover target | ...
IK request | ...
IK success / IK failed
```

---

# 七、如果还是 IK failed，优先判断这三类问题

## A. target base 坐标明显不对

优先查：

```text id="wf18m9"
深度单位
camera_frame
TF camera_color_optical_frame → base_link
像素是否落在目标上
```

## B. 坐标合理，但 IK failed

例如：

```text id="7bufmw"
target in base | x=0.45, y=0.00, z=0.04
hover target | x=0.45, y=0.00, z=0.14
```

但 IK 仍失败，则查：

```text id="dbpckg"
move_group_name 是否正确
ee_frame 是否正确
IK 姿态是否太苛刻
avoid_collisions 是否导致失败
SRDF / kinematics.yaml 是否配置正确
```

## C. `avoid_collisions:=false` 成功，`true` 失败

说明目标或机械臂与 PlanningScene 里的碰撞物体冲突。

---

你现在最需要的不是马上修 IK，而是先拿到这一组日志：

```text id="rpmvk8"
target pixel
depth raw
target in camera
target in base
hover target
IK request
IK error_code
```

有了这些，基本就能判断 IK failed 是坐标错、深度错、TF 错，还是 MoveIt 配置/碰撞问题。
