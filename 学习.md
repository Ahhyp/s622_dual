# 项目学习指南

## 一、整体构建流程

### 编译依赖层次

项目分为四层，colcon 会自动按依赖顺序编译：

```
第 1 层（纯 C++ 库，无 ROS 依赖）
  fairino_planning_core        ← 规划算法核心，仅依赖 Eigen3 + nanoflann

第 2 层（ROS 2 基础层）
  fairino_msgs                 ← 自定义消息/服务定义
  fairino_hardware             ← ros2_control 硬件接口插件
  fairino_planning_ros         ← 将规划核心封装为 MoveIt2 插件

第 3 层（配置与描述层）
  fairino_description          ← URDF 机械臂模型
  s622_moveit_descriptions     ← S622 URDF/SRDF
  fairino3_v6_moveit2_config   ← MoveIt2 规划组、碰撞矩阵、控制器配置
  s622_moveit_config           ← S622 MoveIt2 配置

第 4 层（应用层）
  gz_launch                    ← Gazebo 仿真启动脚本
  visual_servo                 ← 视觉伺服抓取节点
  yolov8_obb / yolov8_grasping ← YOLO 检测节点
  trajectory_retime_server     ← 轨迹重新参数化服务
  control_servers              ← FastAPI Web 控制接口
```

### 构建命令

```bash
cd /home/yep/S622_robotarm

# 全量构建
colcon build --merge-install --symlink-install --cmake-args "-DCMAKE_BUILD_TYPE=Release"

# 只构建某一层（例如只改了规划核心）
colcon build --packages-select fairino_planning_core fairino_planning_ros

# 加载环境（每次新终端都需要）
source install/setup.bash
```

---

## 二、仿真启动流程

### Launch 文件依赖树

```
gazebo.launch.py                          ← 基础：Gazebo + MoveIt2 + RViz
└── gazebo_yolo.launch.py                 ← 加 YOLO 检测 + MoveIt Servo
    ├── pick_block.launch.py              ← 加路径规划演示（预定义点位运动）
    ├── servo_yolo_grasping_gz.launch.py  ← 加视觉伺服自动抓取
    ├── pen_box_system.launch.py          ← 笔盒抓取场景
    └── yolo_pick.launch.py               ← UI 控制抓取
```

### gazebo_yolo.launch.py 启动顺序

```
T=0s  Gazebo 空世界 (ros_gz_sim)
T=0s  机械臂加载到 Gazebo（位置 x=0, y=0, z=1.02）
T=0s  robot_state_publisher（发布 /tf, /tf_static）
T=0s  ros2_control_node + 控制器
        ├── joint_state_broadcaster  → 发布 /joint_states
        ├── robot_arm_controller     → 接收 /robot_arm_controller/joint_trajectory
        └── hand_controller          → 夹爪控制
T=0s  MoveIt move_group（规划组: robot_arm）
T=0s  RealSense 相机桥接
        ├── /camera/camera/color/image_raw
        ├── /camera/camera/aligned_depth_to_color/image_raw
        └── /clock（仿真时间同步）
T=0s  MoveIt Servo（接收速度命令，输出关节轨迹）
T=3s  YOLO OBB 检测节点（订阅相机图像，发布检测结果）
T=8s  视觉伺服抓取节点（等待其他节点就绪后启动）
```

### 关键话题

| 话题 | 类型 | 用途 |
|------|------|------|
| `/joint_states` | sensor_msgs/JointState | 关节状态反馈 |
| `/robot_arm_controller/joint_trajectory` | trajectory_msgs/JointTrajectory | 关节轨迹命令 |
| `/servo_node/delta_twist_cmds` | geometry_msgs/TwistStamped | 笛卡尔速度命令（视觉伺服用） |
| `/camera/camera/color/image_raw` | sensor_msgs/Image | 相机 RGB 图像 |
| `/camera/camera/aligned_depth_to_color/image_raw` | sensor_msgs/Image | 对齐深度图 |
| `/task_state` | std_msgs/String | 任务状态（节点间协调） |
| `/clock` | rosgraph_msgs/Clock | 仿真时间（所有节点 use_sim_time=True） |

---

## 三、源码阅读路径

### 第 1 步：理解启动流程（30 分钟）

**目标**：搞清楚运行 `ros2 launch gz_launch gazebo_yolo.launch.py` 时发生了什么。

```
src/gz_launch/launch/gazebo.launch.py       ← 先读这个，最基础
src/gz_launch/launch/gazebo_yolo.launch.py  ← 再读这个，看它在基础上加了什么
```

重点关注：每个节点的名字、它订阅/发布哪些话题、启动延迟是多少。

---

### 第 2 步：理解用 MoveIt2 控制机械臂（1 小时）

**目标**：搞清楚如何用代码让机械臂运动到指定位置。

```
src/gz_launch/scripts/demo_pathplanning_node.py   ← 最清晰的示例
src/pymoveit2/pymoveit2/moveit2.py                ← MoveIt2 Python 接口封装
```

重点关注：
- `MoveIt2` 类的初始化参数（规划组名、末端执行器、坐标系）
- `move_to_configuration()` 和 `move_to_pose()` 的调用方式
- 规划器配置：`pipeline_id="fairino"`, `planner_id="BiRRTStar"`

---

### 第 3 步：理解自定义规划算法（2 小时）

**目标**：搞清楚 BiRRT* 如何作为 MoveIt2 插件工作。

```
src/fairino_planning_core/include/fairino_planning_core/types.h
  └─ 数据结构：JointConfig（6维关节角）、Transform4d（4x4变换矩阵）

src/fairino_planning_core/src/ik/fairino_ik.cpp
  └─ 解析 IK：给定末端位姿 → 求 6 个关节角

src/fairino_planning_core/src/algorithms/bi_rrt_star.cpp
  └─ BiRRT* 主逻辑：plan() → planWithFallback() → tryConnect()

src/fairino_planning_ros/src/fairino_planner_manager.cpp
  └─ MoveIt2 插件入口：FairinoPlanningContext::solve()
     接收 MoveIt 的规划请求 → 调用 BiRRTStar::plan() → 返回轨迹
```

关键函数调用链：

```
MoveIt2 规划请求
  → FairinoPlanningContext::solve()
    → 提取起始/目标关节角
    → BiRRTStar::plan()
      → 随机采样 + IK 求解
      → KD 树近邻查询（nanoflann）
      → 碰撞检查（MoveItCollisionChecker）
      → 树连接 tryConnect()
    → 路径后处理（快捷方式 + 平滑）
  → 返回 MotionPlanResponse（关节轨迹）
```

---

### 第 4 步：理解硬件接口（1 小时）

**目标**：搞清楚关节轨迹命令如何传到真实机器人（或仿真）。

```
这个文件，主要是添加了针对慧灵的爪夹(finger1, finger2)
src/fairino_hardware/src/fairino_hardware_interface.cpp
  └─ ros2_control 插件：
     on_init()  ← 读取 URDF 中的硬件参数
     read()     ← 从 SDK 读取当前关节角 → 写入 state_interfaces
     write()    ← 从 command_interfaces 读取目标关节角 → 发送给 SDK

src/fairino_hardware/src/command_server.cpp
  └─ 接收关节指令，转发给 libfairino.so SDK
```

仿真时，`libfairino.so` 被 Gazebo 的 ros2_control 插件替代，数据流相同。

---

### 第 5 步：理解视觉伺服抓取（2 小时）

**目标**：搞清楚从"看到物体"到"抓起来"的完整流程。

```
src/visual_servo/visual_servo/servo_yolo_grasping_node.py
  └─ 状态机（TaskState）：
     IDLE → SEARCHING → MOVING_TO_TARGET_ABOVE
     → SERVO_TRACK_ABOVE（视觉伺服跟踪）
     → MOVING_TO_GRASP_GLOBAL → GRASPING
     → LIFTING_TARGET → MOVING_TO_BOX
     → RELEASING → RETURNING_HOME → COMPLETED

src/visual_servo/visual_servo/controllers/servo_controller.py
  └─ 将目标位置误差转换为 /servo_node/delta_twist_cmds（笛卡尔速度）

src/gz_launch/scripts/yolo_Kalman_detector_obb_node.py
  └─ YOLO OBB 检测 + Kalman 滤波 → 发布目标 3D 位置
```

关键数据流：

```
相机图像 /camera/camera/color/image_raw
  → YOLO OBB 检测 + Kalman 滤波
  → 发布 /pen_position_3d, /cube_position_3d（目标 3D 坐标）
    → servo_yolo_grasping_node 订阅
      → 状态机决策
      → 视觉伺服：发布 /servo_node/delta_twist_cmds
        → MoveIt Servo → /robot_arm_controller/joint_trajectory
          → ros2_control → 机械臂运动
```

---

### 各模块关键文件速查

| 想了解什么 | 读哪个文件 |
|-----------|-----------|
| 如何用代码控制机械臂运动 | `src/gz_launch/scripts/demo_pathplanning_node.py` |
| BiRRT* 算法实现 | `src/fairino_planning_core/src/algorithms/bi_rrt_star.cpp` |
| 解析 IK 实现 | `src/fairino_planning_core/src/ik/fairino_ik.cpp` |
| MoveIt2 插件入口 | `src/fairino_planning_ros/src/fairino_planner_manager.cpp` |
| 硬件接口实现 | `src/fairino_hardware/src/fairino_hardware_interface.cpp` |
| 视觉伺服状态机 | `src/visual_servo/visual_servo/servo_yolo_grasping_node.py` |
| 仿真启动配置 | `src/gz_launch/launch/gazebo_yolo.launch.py` |
| MoveIt2 Python 接口 | `src/pymoveit2/pymoveit2/moveit2.py` |
| YOLO 检测节点 | `src/gz_launch/scripts/yolo_Kalman_detector_obb_node.py` |
| 机械臂 URDF 模型 | `src/s622_moveit_descriptions/urdf/` |



## 运行指南

### 前置：安装依赖与构建

```bash
# ROS 2 系统包
sudo apt install ros-$ROS_DISTRO-moveit ros-$ROS_DISTRO-ros-gz-*
sudo apt install ros-$ROS_DISTRO-moveit-servo ros-$ROS_DISTRO-realsense2-camera
sudo apt install ros-$ROS_DISTRO-cv-bridge ros-$ROS_DISTRO-hardware-interface

# Python 依赖
pip install ultralytics torch torchvision opencv-python fastapi uvicorn numpy

# 构建
cd /home/yep/S622_robotarm
colcon build --merge-install --symlink-install --cmake-args "-DCMAKE_BUILD_TYPE=Release"
source install/setup.bash
```

> 每次新开终端都必须先 `source install/setup.bash`。

---

### 一、Gazebo 仿真运行

仿真不需要真实硬件，从简单到复杂有 6 种启动方式：

#### 1.1 最基础：机械臂 + MoveIt2 + RViz

```bash
ros2 launch gz_launch gazebo.launch.py
```

**启动内容：** Gazebo 空世界 + Fairino3_v6 机械臂 + RealSense D435（仿真） + ros2_control 控制器 + MoveIt2 move_group + RViz

**能做什么：** 在 RViz 的 MotionPlanning 面板手动拖拽目标位姿，点 Plan & Execute，看 Gazebo 中机械臂动。

---

#### 1.2 加了 YOLO 检测的仿真

```bash
ros2 launch gz_launch gazebo_yolo.launch.py
```

**比 1.1 多了：** YOLO OBB 检测节点 + MoveIt Servo（实时速度控制）+ 图像/深度桥接

---

#### 1.3 路径规划演示（推荐先跑这个）

```bash
ros2 launch gz_launch pick_block.launch.py
```

**比 1.1 多了：** 仿真场景中加载桌子 + 障碍物 + 盒子 + 案例模型，`demo_pathplanning_node.py` 自动执行预设的 6 个目标位姿运动。

**演示流程：** 机械臂自动从 HOME 位姿出发，依次前往 6 个预定义点位，在每个点暂停 5 秒。

---

#### 1.4 视觉伺服自动抓取（最完整）

```bash
ros2 launch gz_launch servo_yolo_grasping_gz.launch.py
```

**启动内容（按时间顺序）：**

| 时间 | 节点 | 作用 |
|------|------|------|
| T=0s | gazebo_yolo.launch.py | 完整仿真环境 |
| T=2s | cube_controller_node | 控制仿真中的立方体运动 |
| T=3s | yolo_Kalman_detector_obb_node | YOLO 检测 + Kalman 滤波追踪 |
| T=8s | servo_gazebo_grasping_node | 视觉伺服抓取主节点 |
| T=8s | retime_server | 轨迹重新定时 |

**完整数据流：** 相机图像 → YOLO 检测 → Kalman 滤波 → 目标 3D 坐标 → 视觉伺服状态机 → 笛卡尔速度命令 → MoveIt Servo → 关节轨迹 → 机械臂运动 → 抓取

---

#### 1.5 笔盒系统

```bash
ros2 launch gz_launch pen_box_system.launch.py
```

特定应用场景：检测笔和盒子，抓取笔放入盒子。

---

#### 1.6 YOLO UI 控制抓取

```bash
ros2 launch gz_launch yolo_pick.launch.py
```

启动 UI 界面，人工选择目标后自动规划抓取。

---

### 二、真机运行

真机运行需要连接 Fairino S622 机械臂硬件、相机、以及装好必要的驱动。

#### 2.1 前提条件

1. 机械臂控制器与上位机在同一网络（默认 IP：`192.168.58.2`）
2. `libfairino.so` 已放置在 `src/fairino_hardware/libfairino/lib/`
3. RealSense D435 或 OAK-D 相机已连接 USB

#### 2.2 启动硬件接口（不含规划）

```bash
ros2 launch fairino_hardware fairino_hardware.launch.py
```

仅启动 ros2_control 硬件接口，使上位机能读关节状态、发关节指令。此时可以通过 `ros2 topic` 或 `control_servers` 的 Web API 手动控制。

#### 2.3 完整真机规划与控制

```bash
ros2 launch s622_moveit_config move_group.launch.py
```

启动 MoveIt2 move_group + RViz，用法与仿真类似——在 RViz 中拖拽规划，通过真机执行。

#### 2.4 真机视觉伺服抓取

```bash
ros2 launch visual_servo servo_yolo_grasping.launch.py
```

**与仿真版的区别：** 使用真实 RealSense/OAK-D 相机驱动替代仿真相机桥接，其余 YOLO 检测和视觉伺服逻辑相同。

#### 2.5 真机 OctoMap 融合抓取

```bash
ros2 launch octomap_yolo_grasping octomap_yolo_grasping.launch.py
```

在视觉伺服基础上加入 OctoMap 三维障碍物地图。

#### 2.6 手眼标定（真机）

```bash
# 标定
ros2 launch hand_eye_calibration calibrate.launch.py

# 验证标定结果
ros2 launch hand_eye_calibration validate.launch.py
```

标定流程：手持 ArUco 标定板在相机视野中 → 移动机械臂采集多组数据 → easy_handeye2 解算 `camera → end_effector` 变换 → 发布到 `/tf_static`。

---

### 三、Web 控制接口（真机/仿真均可）

```bash
ros2 run control_servers joint_control_app
```

启动 FastAPI 服务，通过浏览器访问 `http://localhost:8000` 控制机械臂关节。

---

### 四、所有 Launch 文件速查

| Launch 文件 | 环境 | 功能 |
|------------|------|------|
| `gz_launch gazebo.launch.py` | 仿真 | 基础：Gazebo + MoveIt2 + RViz |
| `gz_launch gazebo_yolo.launch.py` | 仿真 | 加 YOLO 检测 + Servo |
| `gz_launch pick_block.launch.py` | 仿真 | 路径规划演示（推荐入门） |
| `gz_launch servo_yolo_grasping_gz.launch.py` | 仿真 | 视觉伺服自动抓取 |
| `gz_launch pen_box_system.launch.py` | 仿真 | 笔盒抓取场景 |
| `gz_launch yolo_pick.launch.py` | 仿真 | UI 控制抓取 |
| `gz_launch stopmotion.launch.py` | 仿真 | 停止所有运动 |
| `visual_servo servo_yolo_grasping.launch.py` | 真机 | 视觉伺服抓取 |
| `octomap_yolo_grasping octomap_yolo_grasping.launch.py` | 真机 | OctoMap 融合抓取 |
| `hand_eye_calibration calibrate.launch.py` | 真机 | 手眼标定 |
| `hand_eye_calibration validate.launch.py` | 真机 | 标定结果验证 |

---

第一步：安装依赖
确保系统已安装 ROS 2 Humble/Iron，然后安装关键依赖：


# ROS 2 相关
sudo apt install ros-$ROS_DISTRO-moveit ros-$ROS_DISTRO-ros-gz-*
sudo apt install ros-$ROS_DISTRO-moveit-servo ros-$ROS_DISTRO-realsense2-camera
sudo apt install ros-$ROS_DISTRO-cv-bridge ros-$ROS_DISTRO-hardware-interface

# Python 依赖
pip install ultralytics -i https://mirrors.aliyun.com/pypi/simple/ --timeout 100
pip install torch torchvision opencv-python fastapi uvicorn numpy -i https://mirrors.aliyun.com/pypi/simple/ --timeout 100
第二步：构建项目

cd /home/yep/S622_robotarm
colcon build --merge-install --symlink-install --cmake-args "-DCMAKE_BUILD_TYPE=Release"
source install/setup.bash
第三步：启动仿真
项目有几个层次的仿真 launch，从简单到复杂：

1. 最基础 — 只看机械臂 + MoveIt2 + RViz：


ros2 launch gz_launch gazebo.launch.py
启动 Gazebo 空世界 + Fairino3_v6 机械臂 + RealSense 相机 + RViz + MoveIt2。

2. 加入 YOLO 检测：


ros2 launch gz_launch gazebo_yolo.launch.py
在基础环境上增加 YOLOv8 OBB 检测和 MoveIt Servo 实时控制。

3. 完整抓取演示（推荐学习用）：


# 路径规划演示（预定义点位运动）
ros2 launch gz_launch pick_block.launch.py

# 视觉伺服自动抓取（最完整的 pipeline）
ros2 launch gz_launch servo_yolo_grasping_gz.launch.py
Launch 文件依赖关系

gazebo.launch.py          ← 基础（Gazebo + MoveIt2 + RViz）
└── gazebo_yolo.launch.py ← 加 YOLO + MoveIt Servo
    ├── pick_block.launch.py          ← 加路径规划演示
    ├── servo_yolo_grasping_gz.launch.py  ← 加视觉伺服抓取
    ├── pen_box_system.launch.py      ← 笔盒抓取场景
    └── yolo_pick.launch.py           ← UI 控制抓取
学习路径建议
先跑 gazebo.launch.py，在 RViz 里用 MoveIt2 的 MotionPlanning 面板手动规划运动，理解机械臂基本控制
看 fairino_planning_core，理解自定义 BiRRT*/IK 如何作为 MoveIt2 插件工作
跑 pick_block.launch.py，看 demo_pathplanning_node.py 如何用 pymoveit2 编程控制机械臂
跑 servo_yolo_grasping_gz.launch.py，理解 YOLO 检测 → 视觉伺服 → 抓取的完整 pipeline
关键脚本都在 src/gz_launch/scripts/，是学习整个系统最好的入口。


