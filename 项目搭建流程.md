# 项目搭建流程

本文档从零开始，按合理的开发顺序，还原该项目一步步搭建到当前状态的全过程。

---

## 阶段 1：机器人模型定义

**前提：** 从厂家获取 Fairino S622 的 CAD 模型（STL 网格文件）和 DH 参数表。

**要做的事：**

1. 通过 SolidWorks to URDF Exporter 插件导出 S622 机械臂 URDF，含夹爪
2. 从 CAD 导出 STL 网格文件放入 `meshes/`
3. 定义关节名、类型、限位、原点偏移、惯性参数
4. 定义 6 个转动关节 + 2 个夹爪关节（finger1/finger2）
5. 创建自定义消息包 `fairino_msgs`，定义：
   - `RobotNonrtState`（机器人非实时状态）
   - `RemoteCmdInterface`（远程命令接口）
   - `RemoteScriptContent`（远程脚本内容）

**产出：**

| 包 | 类型 |
|----|------|
| `fairino_description` | URDF + STL 网格 |
| `s622_moveit_descriptions` | URDF（SolidWorks 导出） |
| `fairino_msgs` | 自定义 ROS 2 消息/服务 |

**验证：** RViz 中启动 `robot_state_publisher`，确认 URDF 可视化正确。

---

## 阶段 2：核心运动学与规划算法（纯 C++，脱离 ROS）

**前提：** 阶段 1 的 URDF/DH 参数。

**要做的事：**

1. 从 URDF joint origin 提取标准 DH 参数：
   ```
   d     = {0.140, 0,     0,     0.102, 0.102, 0.100}
   a     = {0,    -0.280, -0.240, 0,     0,     0}
   alpha = {pi/2,  0,      0,     pi/2, -pi/2,  0}
   ```
2. 实现正运动学（FK）：DH 变换矩阵级联
3. 实现解析逆运动学（IK）：基于腕前点分离的封闭解，共最多 8 组解
4. 实现 IK 选择器（加权评分：运动代价、限位惩罚、奇异规避、肘部高度）
5. 实现 RRT* 单树规划算法
6. 实现 BiRRT* 双向搜索规划算法（带回退策略）
7. 集成 nanoflann KD 树加速近邻搜索
8. 实现混合采样器、姿态约束检查、路径快捷方式、轨迹平滑

**产出：**

| 包 | 语言 | 依赖 |
|----|------|------|
| `fairino_planning_core` | C++17 | Eigen3, nanoflann（header-only） |

**关键设计决策：** 此包完全无 ROS 依赖，可以脱离 ROS 环境独立编译和单元测试。

**验证：** 编写测试验证 `FK(IK(pose)) == pose`，正逆运动学互逆性。

---

## 阶段 3：MoveIt2 配置与规划插件

**前提：** 阶段 1（URDF）+ 阶段 2（planning_core 编译通过）。

**要做的事：**

1. 用 MoveIt Setup Assistant 加载 URDF，生成 SRDF：
   - 定义规划组（`fairino3_v6_group` 用于手臂，`hand` 用于夹爪）
   - 定义末端执行器（`tool0` / `grasp_frame`）
   - 生成碰撞禁用矩阵（15 对关节间的碰撞豁免）
   - 配置虚拟关节（world → base_link）
2. 编写 `fairino_planning_ros`，封装为 MoveIt2 插件：
   - `FairinoPlannerManager`：规划器管理类
   - `FairinoPlanningContext::solve()`：接收规划请求 → 调 BiRRT* → 后处理 → 返回轨迹
   - `FairinoIKPlugin`：把解析 IK 注册为 MoveIt 的 IK 求解器
   - `MoveItCollisionChecker`：包装 MoveIt PlanningScene 的碰撞检测
3. 配置 `kinematics.yaml`，指向自定义 IK 插件
4. 配置 `fairino_planning.yaml` 规划器参数
5. 配置 `ros2_controllers.yaml` 和 `moveit_controllers.yaml`
6. 将 `pymoveit2`（外部项目，Andrej Orsula 的 MoveIt2 Python 接口）放入 `src/`

**产出：**

| 包 | 说明 |
|----|------|
| `fairino3_v6_moveit2_config` | Fairino3 v6 的 MoveIt2 全套配置 |
| `s622_moveit_config` | S622 的 MoveIt2 全套配置（主力用） |
| `fairino_planning_ros` | MoveIt2 规划器 + IK 插件 |
| `pymoveit2` | 外部项目，MoveIt2 Python 接口 |

**验证：** RViz MotionPlanning 面板对机械臂做拖拽规划，路径生成正确无碰撞。

---

## 阶段 4：Gazebo 仿真环境

**前提：** 阶段 1（URDF）+ 阶段 3（MoveIt2 配置完成）。

**要做的事：**

1. 编写 `robot_gazebo.urdf.xacro`：在原始 URDF 上添加 Gazebo 仿真标签
   - `gz_ros2_control` 插件（用仿真实现硬件接口）
   - 关节摩擦和阻尼参数
   - 传感器发布（JointState, Pose, 触碰）
2. 集成 Intel RealSense D435 相机仿真模型
3. 配置仿真控制器：`joint_state_broadcaster` + `joint_trajectory_controller` + `gripper_controller`
4. 搭建仿真场景 SDF（空世界、桌面场景 `arm_on_the_table.sdf`）
5. 创建环境物体模型（盒子 `box.urdf`、案例 `case.urdf`、障碍物 `obstacle.urdf`、立方体 `cube.sdf`）
6. 编写 launch 文件，串联启动顺序：
   ```
   Gazebo（空世界）
   → S622 机械臂 spawn 到 Gazebo
   → robot_state_publisher
   → ros2_control_node + controller_spawner（3 个控制器）
   → ros_gz_bridge（时钟同步、图像/深度桥接）
   → MoveIt move_group
   → RViz
   ```
7. 配置 MoveIt Servo（实时笛卡尔速度控制）
8. 编写演示脚本 `demo_pathplanning_node.py`：指定关节目标点，调 MoveIt2 规划并执行

**产出：**

| 包 | 说明 |
|----|------|
| `gz_launch` | launch 文件、仿真配置、场景模型、演示脚本 |
| `realsense2_gz_description` | RealSense 相机仿真 URDF（外部项目） |

**验证：** `ros2 launch gz_launch gazebo.launch.py` 能启动，RViz 中手动规划运动，Gazebo 中机械臂实际动。

---

## 阶段 5：硬件接口层（真机驱动）

**前提：** 阶段 1（fairino_msgs）+ Fairino 厂家 SDK（`libfairino.so`）。

**要做的事：**

1. 实现 `FairinoHardwareInterface` 类，继承 `hardware_interface::SystemInterface`：
   - `on_init()`：解析 URDF ros2_control 标签，检查 6 关节 + 夹爪接口
   - `on_activate()`：通过 SDK RPC 连接控制器（默认 IP 192.168.58.2），读初始关节角
   - `read()`：`GetActualJointPosDegree()` 读当前关节角（度转弧度），写回 state_interfaces
   - `write()`：`ServoJ()` 下发关节位置指令（弧度转度），`SetDO()` 控制夹爪电磁阀
2. 实现 `command_server.cpp`：接收外部关节指令，转发给 SDK
3. 夹爪状态机：DO0 控制电磁阀 → 气缸，带滞回逻辑（OPEN/CLOSE）
4. 放置预编译 SDK：`src/fairino_hardware/libfairino/lib/libfairino.so.2.2.5`

**产出：**

| 包 | 说明 |
|----|------|
| `fairino_hardware` | ros2_control 硬件接口插件 + command_server |

**验证：** 真机上 `ros2 control` 加载硬件接口，关节可运动。

---

## 阶段 6：视觉检测管道

**前提：** 阶段 3（MoveIt2 集成可用）。

**要做的事：**

1. 集成 OAK-D 或 RealSense 相机驱动，发布 RGB + 深度图像
2. 实现 YOLOv8 目标检测节点（Python），训练自定义模型检测笔、盒子、石头
3. 实现 YOLOv8 OBB（有向边界框）检测（C++ 实现，GPU 推理）
4. 定义 OBB 检测结果的消息类型 `yolov8_obb_msgs`
5. 采集真实数据，标注，训练 `.pt` 模型文件
6. 集成 `ros2_aruco`（外部项目）用于标记检测
7. 实现 `trajectory_retime_server` 轨迹重新定时服务

**产出：**

| 包 | 说明 |
|----|------|
| `yolov8_grasping` | YOLOv8 检测抓取节点 |
| `yolov8_obb` | YOLOv8 OBB 检测（C++） |
| `yolov8_obb_msgs` | OBB 检测消息定义 |
| `ros2_aruco` | ArUco 标记检测（外部项目） |
| `trajectory_retime_server` | 轨迹重新定时服务 |

**模型文件（根目录）：**
- `best.pt`、`best_stone.pt`：自定义训练模型
- `yolov8n.pt`、`yolov8n-obb.pt`：Nano 基础模型
- `yolo_obb*.pt`：OBB 模型

**验证：** 相机图像 `→` YOLO 检测 `→` `/yolo_detections` topic 能收到检测结果。

---

## 阶段 7：手眼标定

**前提：** 阶段 5（真机能动）+ 阶段 6（ArUco 检测就绪）。

**要做的事：**

1. 集成 `easy_handeye2` 外部库（硬件无关的手眼标定，含 GUI + 评估器）
2. 编写标定 launch：启动 RealSense 相机 + ArUco 标定板检视 + 机械臂
3. 执行 eye-on-hand 标定流程：采集多组 robot_pose × marker_pose
4. 用 Tsai/Lenoy 算法解得 `camera → end_effector` 变换
5. 发布标定结果到 `/tf_static`（handeye_publisher 节点）

**产出：**

| 包 | 说明 |
|----|------|
| `easy_handeye2` | 手眼标定库（外部项目，ros2_control 分支） |
| `hand_eye_calibration` | 标定集成包（launch + TF 发布） |

---

## 阶段 8：视觉伺服与自主抓取

**前提：** 阶段 6（YOLO 检测）+ 阶段 7（手眼标定）+ 阶段 3（MoveIt2 规划）。

**要做的事：**

1. 实现视觉伺服抓取主节点 `servo_yolo_grasping_node.py`：
   - 13 状态状态机：`IDLE → SEARCHING → MOVING_TO_TARGET_ABOVE → SERVO_TRACK_ABOVE → MOVING_TO_GRASP_GLOBAL → GRASPING → LIFTING_TARGET → MOVING_TO_BOX → RELEASING → RETURNING_HOME → COMPLETED`
   - 多坐标系变换（相机 → base_link → 末端执行器）
   - 目标检测缓存 + 滤波
2. 实现伺服控制器：将位姿误差转为笛卡尔速度命令发布到 `/servo_node/delta_twist_cmds`
3. 实现 PD 控制器
4. 实现 OctoMap + YOLO 融合抓取（三维障碍物地图 + 目标检测）
5. 实现 GraspNet 抓取位姿规划（可选）
6. 编写完整抓取 launch：
   ```
   Gazebo 仿真环境
   → YOLO/Kalman 检测
   → cube_controller（物体运动控制）
   → 视觉伺服抓取节点
   → trajectory_retime_server
   ```

**产出：**

| 包 | 说明 |
|----|------|
| `visual_servo` | 视觉伺服 + 抓取状态机 |
| `octomap_yolo_grasping` | OctoMap 融合抓取 |
| `graspnet_grasping` | GraspNet 抓取位姿规划 |

**验证：** 完整闭环运行——检测到物体 → 视觉伺服跟踪 → 规划路径 → 抓取 → 放置，成功。

---

## 阶段 9：Web 控制与数据监控

**前提：** 阶段 5（硬件控制能力可用），可独立开发后接入。

**要做的事：**

1. 用 FastAPI + Jinja2 搭建 Web 控制后端：
   - `/api/joint/move`：关节移动
   - `/api/joint/stop`：停止
   - `/api/joint/state`：查询状态
   - 前端页面（Jinja2 模板渲染）
2. 实现 `data_monitor` 节点：订阅 `/joint_states`，记录轨迹数据到文件
3. 可用模拟数据独立开发测试

**产出：**

| 包 | 说明 |
|----|------|
| `control_servers` | FastAPI Web 控制接口 |
| `data_monitor` | 关节轨迹监控与数据记录 |

---

## 阶段 10：GraphExecuter 工作流编排（独立并行开发）

**前提：** 无，完全独立的项目，可与阶段 1-9 并行开发。

**要做的事：**

1. 基于 PySide6 + NodeGraphQt 搭建可视化节点图编辑器
2. 实现拖放式编程：拖拽功能节点 → 连线 → 配置参数 → 执行
3. 实现多种功能节点：
   - 运动规划节点（调 pymoveit2）
   - YOLO 检测节点（调 ultralytics）
   - LLM 调用节点（调 OpenAI API）
   - 语音识别/合成节点（vosk、ChatTTS、edge-tts）
   - OCR 公式识别节点（pix2tex）
4. 实现多线程执行器，支持并行/串行执行
5. 通过 `rclpy` 与 ROS 2 系统通信（非 ROS2 包，无编译依赖）

**产出：**

| 项目 | 说明 |
|------|------|
| `GraphExecuter` | PySide6 可视化工作流系统（独立项目，不在 colcon workspace 内） |

---

## 各阶段依赖关系图

```
阶段1: 机器人模型 (URDF + MSGS)
  │
  ├──→ 阶段2: 运动学与规划 (planning_core)
  │      │
  │      └──→ 阶段3: MoveIt2 配置 (planning_ros + moveit_configs)
  │               │
  │               ├──→ 阶段4: 仿真环境 (gz_launch)
  │               │
  │               ├──→ 阶段6: 视觉检测 (yolo, aruco)
  │               │        │
  │               │        └──→ 阶段7: 手眼标定 (hand_eye_calibration)
  │               │                 │
  │               │                 └──→ 阶段8: 视觉伺服抓取
  │               │
  │               └──→ (规划能力被阶段8/9/10使用)
  │
  └──→ 阶段5: 硬件接口 (fairino_hardware)
           │
           ├──→ 阶段7: 手眼标定（需真机）
           ├──→ 阶段8: 视觉伺服抓取（真机版）
           └──→ 阶段9: Web 控制 + 数据监控

阶段10: GraphExecuter ←── 完全独立，与以上并行开发
```

---

## 模块来源分类

| 来源 | 包/模块 |
|------|---------|
| **自行开发（15 个）** | `fairino_planning_core`, `fairino_planning_ros`, `fairino_hardware`, `fairino_description`, `s622_moveit_descriptions`, `s622_moveit_config`, `fairino3_v6_moveit2_config`, `fairino_msgs`, `gz_launch`, `yolov8_grasping`, `yolov8_obb`, `yolov8_obb_msgs`, `visual_servo`, `hand_eye_calibration`, `octomap_yolo_grasping`, `graspnet_grasping`, `control_servers`, `data_monitor`, `trajectory_retime_server` |
| **外部复制/引用** | `pymoveit2`（Andrej Orsula）、`easy_handeye2`（IFL-CAMP）、`ros2_aruco`（ravijo）、`realsense2_gz_description`、`GraphExecuter` |
| **厂家提供** | `libfairino.so` SDK、机器人 CAD 模型/STL 网格 |
